RTSBUILDING DETAILED PROJECT RECORD
Last Rewrite: 2026-04-24
Project Root: E:\RTSbuilding
Runtime Target: Minecraft 1.21.1 + NeoForge 21.1.219 + Java 21

================================================================
0. Purpose
================================================================
This file is the current project map for future coding sessions.
It is intentionally practical:
- what the mod does
- where the important code lives
- how the major systems talk to each other
- which behavior is currently implemented
- which risks or constraints matter during changes

================================================================
1. Product Summary
================================================================
RTSBuilding is a Minecraft mod that turns the player into a top-down
RTS-style builder/operator while keeping the server authoritative.

Main product goals:
- remote camera with bounded action radius
- storage-network style item access without full AE-style automation
- remote placement, interaction, mining, and crafting support
- keep vanilla/modded item behavior as intact as possible

Current implemented gameplay surface:
- RTS camera toggle and movement
- full-width bottom RTS builder UI with storage, hotbar/pins, categories,
  recent panel, craft panel, and a craft/GUIs dock
- craft-dock GUI binding shortcuts with persistent server-backed slots
  - AE2 terminal/provider binds now fall back to the original AE item icon
    when clone-stack/block-item preview lookup comes back blank
- linked storage browsing with search, category filtering, paging, and sort
- linked storage modes:
  - left-click link: extract + store
  - right-click link: extract only
- storage recent panel:
  - recent placed / used / crafted entries
  - includes fluid actions
  - recent history now persists across logout/rejoin as player session data
- remote block placement from linked storage or current hotbar slot
- remote fluid placement from linked/internal fluid storage
  - now resolves only clicked block or clicked-face adjacent block
  - no longer auto-biases side clicks upward
  - placement validation/context keeps the original hit face instead of
    collapsing to `Direction.UP`
  - linked-container drain now falls back to generic `drain(amount)` for
    handlers that reject exact-stack drain calls
  - selected fluids can now use the staged shape flow for
    `LINE / SQUARE / CIRCLE / WALL / BOX`
- remote interaction with blocks/entities using hotbar or linked items
- craft-dock GUI binds now persist:
  - target face
  - cached icon item id
  so face-mounted terminals can reopen more reliably
- AE2 crafting/pattern/pattern-access style GUI binds fall back to icon lookup
  from the resolved menu/part name when normal clone-stack preview lookup is
  blank
- remote-opened Sophisticated Storage menus keep their original menu type and
  use a dedicated mixin-backed still-valid bypass because their menu validity
  check does not use `ContainerLevelAccess`
- remote mining with tool slot selection and progress sync
- ultimine follows the FTB-style mature interaction model:
  - holding left mouse mines the seed block only
  - a connected same-block target set is highlighted while mining
  - releasing before the seed block breaks aborts without breaking the batch
  - when the seed block finishes, the cached target set is broken as one
    chained operation
- craft terminal + workbench-linked refill/import flows
- craftables search panel with paging
- draggable storage overlay on container/crafting screens
- funnel mode and funnel buffer panel
- AE2 network-backed item access through optional compat
- FTB quests detect bridge for non-consume item tasks

================================================================
2. Tech Stack
================================================================
Build/runtime:
- Gradle
- NeoForge ModDevGradle `2.0.140`
- Java toolchain 21
- NeoForge `21.1.219`
- Parchment mappings for MC 1.21.1

Workspace companion scaffold:
- `sister-projects/rtsbuilding-forge-1.20.1/`
  - Forge `47.4.16` on Minecraft `1.20.1`
  - ForgeGradle `6.x`
  - Java 17 baseline
  - official MDK scaffold renamed from `examplemod` to `rtsbuilding`
  - local docs in `README.md` and `docs/PORTING.md`
  - verified with `gradlew.bat compileJava` using local JDK 17

Optional compat:
- JEI API/runtime integration
- AE2 storage-service bridge via optional reflection compat
- FTB Quests / FTB Teams via reflection-based optional compat
- Sophisticated Storage remote-menu still-valid bypass compat for RTS opens
  via mixin + tracked menu ids

Important project files:
- `build.gradle`
- `gradle.properties`
- `src/main/resources/rtsbuilding.mixins.json`
- `src/main/templates/META-INF/neoforge.mods.toml`
- `sister-projects/rtsbuilding-forge-1.20.1/README.md`
- `sister-projects/rtsbuilding-forge-1.20.1/docs/PORTING.md`

================================================================
3. Top-Level Code Map
================================================================
Main mod/bootstrap:
- `src/main/java/com/rtsbuilding/rtsbuilding/RtsbuildingMod.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/Config.java`

Forge 1.20.1 sister scaffold:
- `sister-projects/rtsbuilding-forge-1.20.1/src/main/java/com/rtsbuilding/rtsbuilding/RtsbuildingMod.java`
- `sister-projects/rtsbuilding-forge-1.20.1/src/main/java/com/rtsbuilding/rtsbuilding/Config.java`
- `sister-projects/rtsbuilding-forge-1.20.1/src/main/resources/META-INF/mods.toml`
- `sister-projects/rtsbuilding-forge-1.20.1/upstream/forge-1.20.1-47.4.16-mdk.zip`

Client:
- `client/ClientRtsController.java`
  - client-side RTS state hub
  - storage page state, craftables state, selected item/fluid, quick pins
  - quick pins, GUI binding labels, and GUI binding icon item ids are
    server-backed and synced through the storage page payload
  - remote menu compat now delegates to `RtsClientRemoteMenuCompat`
  - C2S packet emission now delegates to `RtsClientPacketGateway`
  - owns persistent normalized storage-overlay layout state
  - high-level client actions still live here, but raw payload construction no
    longer does
- menu handoff guard so delayed container GUI opens do not get preempted by
  `BuilderScreen`
- death/respawn handoff guard:
  - if the player dies while BuilderScreen, RTS home, RTS progression, or RTS
    craft terminal panels are open, the client clears the RTS screen, restores
    the normal camera, and sends a server RTS-toggle shutdown
  - the builder screen is not auto-restored after respawn; the player must
    explicitly enter RTS again
- `client/BuilderScreen.java`
  - main RTS full-screen UI
  - top bar modes
  - compact top-bar mode icons for Interact / Link / Funnel / Rotate are
    drawn procedurally in the screen so they scale with the existing button
    state colors instead of depending on texture loads
  - bottom-anchored storage/craft dock UI
  - craft-terminal button + 8-slot GUI bind/open/clear ring
  - shape placement flow for both items and selected RTS fluids
- `client/RtsClientInputGate.java`
  - draggable overlay UI for container/crafting screens
  - intercepts input for linked pickup/import/crafting shortcuts
- `client/RtsClientUiUtil.java`
  - shared client UI primitives
  - panel-frame drawing, text trimming, compact item counts, compact fluid
    amounts
- `client/RtsCraftablesUiHelper.java`
  - shared craftables UI glue
  - craft dialog open/submit flow and search-draft normalization shared
    between `BuilderScreen` and `RtsClientInputGate`
- `client/RtsClientRemoteMenuCompat.java`
  - shared client remote-menu compat helper
  - menu/screen pair safety guard plus reflection-based validation patch
    extraction from `ClientRtsController`
  - coordinates Sophisticated Storage remote-menu tracking for the mixin
    still-valid bypass path
- `client/RtsClientPacketGateway.java`
  - shared client packet gateway
  - owns C2S payload construction/dispatch previously embedded inside
    `ClientRtsController`
- `client/RtsClientLayoutStore.java`
  - persists normalized storage overlay layout to local config JSON
- `client/RtsCraftTerminalScreen.java`
  - custom crafting screen with linked-storage side panel
- `client/ClientInputHandler.java`
  - event entry point for key/input behavior
- `client/RtsBoundaryRenderer.java`
  - bounds + cursor/raycast helpers

Server:
- `server/RtsCameraManager.java`
  - camera activation and authoritative movement
- `server/RtsStorageManager.java`
  - largest behavior hub
  - linked storage session state
  - persistent quick pins, recent history, and GUI bindings in player NBT
  - AE2-aware linked item-handler resolution
  - AE2 write-back/import paths also route through the network compat handler
  - remote menu validation now stays on the no-teleport path
  - remote-opened Sophisticated Storage menus are tracked so the dedicated
    mixin bypass only applies to RTS-opened menus
  - browsing/search/paging
  - placement / interaction / mining / crafting / fluid handling
  - recent-entry tracking
  - quest detect trigger path
- `server/RtsCraftTerminalMenu.java`
  - custom crafting menu with refill + craft-output tracking
- `server/data/PlacedBlockTrackerData.java`
  - tracks RTS-placed blocks

Network:
- `network/RtsPayloadRegistrar.java`
- `network/RtsNetworkHandlers.java`
- `network/*Payload.java`

Compat:
- `compat/ae2/RtsAe2Compat.java`
  - optional AE2 storage compat plus GUI-binding icon fallback resolution for
    AE terminal/provider menu parts
- `compat/jei/*`
- `compat/ftb/RtsFtbCompat.java`
- `compat/ftb/RtsFtbCompatImpl.java`
- `compat/sophisticatedstorage/RtsSophisticatedStorageCompat.java`

Mixins:
- `mixin/compat/sophisticatedstorage/StorageContainerMenuMixin.java`
  - injects Sophisticated Storage `stillValid(...)`
  - forces `true` only for tracked RTS-opened remote menus

================================================================
4. Core Runtime Model
================================================================
4.1 Client authority vs server authority
- Client predicts camera feel and drives UI state.
- Server owns actual placement, extraction, storage mutation, crafting,
  fluid transfer, and mining completion.
- Most visible UI refreshes come from server `requestPage(...)` or
  craftables payload refreshes.

4.2 Session model
`RtsStorageManager.Session` is the persistent per-player RTS state:
- linked storage refs (dimension + position)
- linked names
- linked modes (bidirectional vs extract-only)
- current page/search/category/sort
- craftables search/toggles
- internal fluid buffer
- funnel state
- recent entry deque
- RTS quick pin item ids
- GUI binding slots (target position + dimension + display label + clicked
  face + cached icon item id)

Persistence behavior:
- session data is loaded from player persistent NBT on first RTS access
- session data is saved on logout / RTS disable and on selected mutation paths
- recent history, quick pins, and GUI bindings now survive logout/rejoin
- active mining state
- active ultimine state:
  - cached connected target positions
  - normal single-block mining progress on the seed block
  - batch break of the cached target set after the seed block completes
- quest detect cooldown
- recent entry deque

Persisted to player persistent NBT:
- linked storage refs in `linked_entries`
  - `dimension`
  - `pos`
  - `mode`
- legacy linked positions / linked dimension fields are still read for migration
- linked link modes
- internal fluids
- recent entries
- quick pin item ids
- GUI bindings

Client-only layout persistence:
- `RtsClientLayoutStore.StoragePanelLayout`
- saved at `config/rtsbuilding-client-layout.json`
- current overlay uses the normalized X/Y position directly
- width/height stay in the schema so client layout state remains forward-compatible

4.3 Linked storage behavior
Linked storage identity is dimension-aware. The session no longer treats all
links as belonging to one global `linked_dimension`; every link carries its
own dimension key and block position. Changing dimensions therefore hides
links that are not in the current level, but it does not delete them from the
player's persistent RTS session.

Storage page payloads still send packed block positions for client highlight
rendering, but only for links in the player's current dimension. Links in
other dimensions remain saved and become active again when the player returns
to that dimension.

Linked item handlers are wrapped so extract-only links still expose:
- slot reads
- extraction
but reject:
- insertion

Linked fluid handlers are wrapped the same way:
- drain allowed
- fill blocked for extract-only links

This means most existing extraction logic can still read from all links
without special-case branching, while store paths naturally stop writing
into protected production outputs.

4.4 RTS camera vertical bounds
- camera Y is clamped relative to the player anchor position
- current lower bound allows camera movement down to `anchorY - 5`
- current upper bound remains `anchorY + 80`
- client local prediction and server authoritative camera movement use the
  same vertical bounds so camera motion does not snap on reconciliation
- the vertical clamp is applied again after distance normalization so the
  minimum-distance rule cannot push camera Y below the configured floor
- the server camera session now stores the last authoritative camera position
  and recreates the RTS camera entity if it disappears across distance/chunk
  boundaries instead of stopping the whole RTS session
- the RTS camera entity client tracking range is `128` blocks so it remains
  tracked across the full supported camera/action envelope

4.5 Survival progression / balance backend
Survival progression is optional and defaults off through:
- `enableSurvivalProgression=false`

When enabled, server-side RTS actions are gated by progression effects:
- camera / interaction
- storage link + browser
- remote place / break / rotate
- funnel + auto-store mined drops
- fluid handling
- remote GUI binding
- craft terminal + JEI transfer
- ultimine

Progression state is stored separately from storage session data:
- per-player root: `rtsbuilding_progression`
- optional shared-team root: `rtsbuilding_shared_progression`

Team sharing is optional and defaults off through:
- `shareSurvivalProgressionWithTeams=false`

When sharing is enabled:
- FTB Teams is used first when available
- vanilla scoreboard teams are used as fallback
- unlocked nodes and RTS home anchors are stored in world `SavedData`
- a player's existing personal unlocked nodes are merged into the team record
  on login so enabling sharing on an existing world does not reset progress
- if the shared team has no home yet, the first logging member's existing
  personal home initializes the shared home

Fluid balancing is now connected to progression:
- `RtsProgressionManager.getFluidCapacityBuckets(player)` controls the
  internal RTS fluid pool capacity
- `fluid_buffer` currently unlocks `FLUID_HANDLING` and sets internal capacity
  to `100` buckets
- existing saved internal fluids are preserved even if current progression
  capacity is lower; new insertion is blocked until capacity allows it

================================================================
5. Major Feature Chains
================================================================
5.1 Storage page refresh
Entry point:
- client sends `C2SRtsRequestStoragePagePayload`
- server `RtsStorageManager.requestPage(...)`

Server page build includes:
- linked item counts
- optional player main inventory contribution for browse/craft availability
- linked/internal fluid summaries
- category list generation from namespace + creative tabs
- filtered page item list
- total item counts map
- recent action list
- funnel buffer summary

Client `applyStoragePage(...)` stores:
- current page entries
- total item counts cache
- fluid entries
- recent entries
- linked summary and positions

5.2 Bottom RTS UI
Main builder bottom area now contains:
- category list
- search + paging
- hotbar row
- pin row
- fluid strip if present
- left storage grid
- right recent grid
- craft panel
- craft dock:
  - center `C` button opens the RTS craft terminal
  - 8 surrounding GUI binding slots open / arm / clear saved targets

Builder bottom panel is now bottom-anchored and full-width instead of a
free-floating dragged window.
Current size controls:
- `+` / `-` buttons resize panel height in row-sized steps
- category/search/tool/grid/craft areas stretch to the available width
- craft panel now uses the full dock height instead of matching storage rows exactly

5.3 Hotbar + pin behavior
Hotbar row:
- normal left click selects the clicked hotbar slot
- clicking an empty hotbar slot still switches to empty hand
- shift + left click on a non-empty hotbar slot stores that stack into
  writable linked storage
- right click on a hotbar slot tries fluid-store from the held container

Pin row:
- pins are client-side quick references to linked items
- pin count overlays use the server-sent total item counts cache
  so search/filter no longer blanks the count display

5.4 Recent entries
Recent panel shows the latest:
- placed items
- used items
- crafted items
- placed fluids
- used fluids
- crafted fluids (protocol supported even if not commonly produced)

Recent entries are tracked server-side and sent with each storage page.
Entries are merged by `(id, item-vs-fluid)` and moved to the front.
This means repeated actions on the same item/fluid accumulate one running
amount instead of creating separate placed/used/crafted rows.
The client recent panel and recent-click selection both use this same
unified recent list instead of a crafted-only subset.
Recent entries are also serialized into the player RTS session NBT so they
persist after reconnect.

Current server-side record points include:
- placement from linked storage
- tool-slot placement/use
- linked-item interaction/use
- craft-to-linked success
- crafting result import paths
- craft terminal result pickup tracking
- fluid store from container/tool slot
- fluid placement

5.5 Link mode UX
Builder screen, LINK mode:
- left click block: toggle/update bidirectional link
- right click block: toggle/update extract-only link

Summary string marks extract-only links in aggregate and for single-link
cases.

5.6 Rotate mode
Builder screen, ROTATE mode:
- right mouse press on a target block sends explicit rotate-block packet
- this bypasses the previous interaction path
- this also bypasses the old press/release drag split for this mode
- goal: rotation always wins over normal block interaction

5.6.2 GUI binding craft dock
Builder screen:
- GUI bindings now live in the craft dock near the sort/resize controls
- slot count is 8 instead of the older 3-slot sidebar layout
- each slot is server-backed persistent state, not client-only transient UI
- bind validation now accepts block entities even when they do not expose a
  direct `MenuProvider`, so AE-style right-click terminals can still be saved
- bound slots can render the saved target block/item icon instead of only a
  numeric index; the icon id is persisted with the binding state
- GUI bind packets also persist the clicked face so multipart / face-mounted
  targets can be reopened against the same side later
- left click on a bound slot opens the saved GUI target directly
- left click on an empty slot arms it for the next world GUI bind
- right click toggles armed rebind mode for a slot
- shift + right click clears the slot
- while a bind is armed, the world cursor shows a dedicated `+` marker
- open path is server authoritative and uses the saved world target

5.6.1 Remote menu-open handoff
- RTS right-click paths that may open a container/menu now start a short
  client-side grace window before `BuilderScreen` is allowed to restore itself
- if `player.containerMenu` changes to a non-player menu while RTS is active,
  `ClientRtsController.tick()` clears `BuilderScreen` and lets the incoming
  menu screen take foreground ownership
- this specifically protects first-open / slow-loading GUI cases where the
  client can spend a few frames with `screen == null` before the real
  container screen is constructed
- server-side interaction itself already uses a temporary remote-POV context;
  this pass stabilizes the client menu takeover side of the flow
- saved-bind opens keep only the short client grace window; RTS no longer uses
  the older client/server validation-position spoof after the `1.0.6` rollback
- generic remote menus still get relaxed container/container-access
  validation when possible
- Sophisticated Storage menus additionally keep their original menu type and
  use a tracked mixin-based `stillValid(...)` bypass because the menu performs
  its own direct player-to-block distance check outside
  `ContainerLevelAccess`

5.7 Crafting support
Crafting-related paths currently in project:
- craft terminal GUI (`RtsCraftTerminalMenu` + `RtsCraftTerminalScreen`)
- overlay import/refill support on crafting screens
- JEI recipe transfer bridge
- craftables panel on RTS UI

Important behavior:
- craftables panel is search-driven and server-backed
- craftables paging now supports append/hasMore flow
- craftables are grouped by result item in the panel, but each entry now
  carries multiple selectable recipe options for the pre-craft dialog
- right click craft opens a high-layer dialog that lets the user choose
  recipe first, then choose craft count
- craft terminal result slot keeps refill behavior
- imported crafting results can be stored into linked storage
- craft output can feed recent history
- successful craft feedback now includes actual consumed item counts and is
  rendered as a high-layer popup so it is not hidden by other UI

================================================================
6. Networking Map
================================================================
Main packet groups:
- camera:
  - `C2SRtsToggleCameraPayload`
  - `C2SRtsCameraMovePayload`
  - `S2CRtsCameraStatePayload`
- storage:
  - `C2SRtsLinkStoragePayload`
  - `C2SRtsRequestStoragePagePayload`
  - `S2CRtsStoragePagePayload`
    - now also carries quick pin item ids, GUI binding labels, and GUI
      binding icon item ids
  - `S2CRtsCraftablesPayload`
    - now includes per-entry recipe-option lists for the pre-craft selector
  - `S2CRtsCraftFeedbackPayload`
    - now includes crafted result plus actual consumed material counts
- interaction/placement:
  - `C2SRtsPlacePayload`
  - `C2SRtsPlaceFluidPayload`
  - `C2SRtsInteractPayload`
  - `C2SRtsRotateBlockPayload`
- craft/import:
  - `C2SRtsOpenCraftTerminalPayload`
  - `C2SRtsImportMenuSlotPayload`
  - `C2SRtsCraftRefillPayload`
  - `C2SRtsCraftRecipePayload`
  - `C2SRtsRequestCraftablesPayload`
  - `S2CRtsCraftablesPayload`
  - `S2CRtsCraftFeedbackPayload`
- storage transfer helpers:
  - `C2SRtsLinkedPickupPayload`
  - `C2SRtsReturnCarriedPayload`
  - `C2SRtsQuickDropPayload`
  - `C2SRtsFillInventoryPayload`
  - `C2SRtsStoreFluidPayload`
  - `C2SRtsStoreHotbarSlotPayload`
  - `C2SRtsSetQuickSlotPayload`
  - `C2SRtsSetGuiBindingPayload`
  - `C2SRtsOpenGuiBindingPayload`
- mining:
  - `C2SRtsMinePayload`
  - `S2CRtsMineProgressPayload`
- utility:
  - `C2SRtsQuestDetectPayload`
  - `S2CRtsRemoteMenuHintPayload`
  - funnel/mode/autostore payloads

================================================================
7. FTB Quest Detect Status
================================================================
Compat is reflection-based on purpose because the project does not hard
depend on FTB jars in `build.gradle`.

Current compat strategy:
- detect FTB Teams API and team manager
- resolve player team lookup with fallback names:
  - `getTeamForPlayerID(UUID)`
  - `getTeamForPlayer(UUID)`
- expose an FTB-team key for optional shared survival progression even when
  FTB Quests itself is not present
- resolve quest tasks via:
  - `collect(ItemTask.class)` if available
  - fallback `submitTasks` field
- skip consume-only and only-from-crafting item tasks
- compute progress from:
  - full player inventory scan
  - linked storage scan

Important bug fixed in current rewrite:
- linked item counting no longer double-counts player main inventory

================================================================
8. Build / Validation
================================================================
Known working validation command:
- `powershell.exe -Command .\\gradlew.bat build`

Current status at rewrite time:
- build succeeds
- there are existing deprecation warnings around `EventBusSubscriber.Bus`
- no test sources currently exist

================================================================
9. Change Notes From This Rewrite
================================================================
This rewrite reflects the repo state after the following functional pass:
- rescan project structure
- refreshed `detailed_project.txt`
- added recent item/fluid panel data flow
- added extract-only linked storage mode
- made rotate mode use explicit block rotation on right click
- made manual quest detect path more robust
- decoupled pin count display from current search/filter results
- added shift-store from RTS hotbar into linked storage
- aligned craft panel visible rows with storage rows

Post-2026-03-28 15:30 follow-up reflected in the current repo state:
- added optional AE2 network item-handler compat for linked storage browse /
  extract / reported-count display paths
- added client layout persistence through `RtsClientLayoutStore`
- added `C2SRtsFillInventoryPayload` and linked fill-inventory behavior
- replaced the old left GUI sidebar with a compact craft/GUI dock and expanded
  GUI bindings from 3 to 8 slots
- changed the main builder storage panel to a bottom-anchored full-width dock
  with height-step resizing instead of free dragging
- made the container/crafting overlay draggable through persisted normalized
  X/Y layout state
- changed recent-entry aggregation to merge repeated actions per item/fluid
- GUI bindings now persist icon item ids so the craft dock can render target
  previews after reconnect / reload
- removed the reopened-menu position spoof again and kept only the no-teleport
  remote-menu grace/validation path
- hardened AE2 write-back so RTS store/craft refund paths can insert into
  slot-agnostic AE2 network views without false `storage is full` failures
- added generic linked-fluid drain fallback for handlers that only support
  amount-based draining
- extended selected RTS fluids so they can use the existing shape placement
  pipeline
- GUI bind packets now carry clicked face + icon hint, and the saved binding
  state persists both for AE-style multipart terminals
- AE2 GUI binding icons now fall back to menu/part-derived item ids when the
  direct clone-stack/block-item path returns no preview
- added dedicated Sophisticated Storage remote-menu compat because the official
  `1.21.x` storage menu bypasses `ContainerLevelAccess` and performs its own
  direct distance `stillValid(...)` check
- replaced the initial Sophisticated Storage wrapper approach with a
  mixin-backed still-valid bypass that preserves the original menu type for
  typed storage screens
- started a deshitfication pass on the client UI layer by extracting shared
  UI primitives and craftables dialog flow out of `BuilderScreen` /
  `RtsClientInputGate` and reusing them in the craft-related screens
- extracted client remote-menu compatibility and reflection-based validation
  patching from `ClientRtsController` into `RtsClientRemoteMenuCompat`, and
  removed dead client spoof-state leftovers from the old path
- extracted client packet construction / dispatch from `ClientRtsController`
  into `RtsClientPacketGateway` so the controller now focuses more on state
  orchestration than payload wiring

================================================================
10. Forge 1.20.1 Sister Project Status
================================================================
Sister workspace:
- `sister-projects/rtsbuilding-forge-1.20.1`

State as of 2026-04-05:
- the sister project now contains a full first-pass port of the main RTS code
  into `Minecraft 1.20.1 + Forge 47.4.16`
- `compileJava` succeeds
- `build` succeeds and produces:
  - `sister-projects/rtsbuilding-forge-1.20.1/build/libs/rtsbuilding-0.1.0-1.20.1-snapshot.jar`

Important porting choices made there:
- kept the copied `com.rtsbuilding.rtsbuilding` package structure aligned with
  mainline so future cherry-pick / manual diff work stays sane
- rewrote the NeoForge payload registrar to a Forge `SimpleChannel` backend
  while preserving most packet payload classes behind local compatibility shims
- migrated major client event hooks from NeoForge events to Forge 1.20.1
  client events
- migrated `SavedData` to the 1.20.1 save/load signatures
- migrated storage / fluid access to Forge item + fluid capability paths
- replaced `1.21.x` crafting input usage with 1.20.1 transient crafting
  containers
- adjusted JEI transfer handling to the 1.20.1 JEI crafting recipe type
- replaced the AE2 network host lookup path with a reflection-based
  `GridHelper.getNodeHost(...)` path instead of NeoForge block capabilities

Known remaining quality items in the sister project:
- there are still a couple of deprecation warnings around `ResourceLocation`
  constructors in the Forge sister branch
- AE2 compat still compiles via reflection and should be runtime-tested in a
  real AE2 1.20.1 Forge environment
- no automated tests exist yet; validation so far is compile/build level only

2026-04-05 runtime follow-up:
- the first generated Forge sister jar originally still contained temporary
  compatibility shim classes under `net.minecraft.network.*`
- that caused a Java module split-package failure at runtime:
  - `Modules rtsbuilding and minecraft export package net.minecraft.network ...`
- fix applied:
  - moved all temporary shim classes out of `net.minecraft.*` /
    `net.neoforged.*` and into:
    - `com.rtsbuilding.rtsbuilding.forgecompat.network.*`
    - `com.rtsbuilding.rtsbuilding.forgecompat.fml.*`
  - updated all packet payload / compat imports to the new shim namespace
- post-fix validation:
  - `sister-projects/rtsbuilding-forge-1.20.1`: `compileJava` SUCCESS
  - `sister-projects/rtsbuilding-forge-1.20.1`: `build` SUCCESS

2026-04-05 JEI compatibility follow-up:
- runtime pack reported `NoSuchMethodError` for
  `IIngredientManager.createClickableIngredient(...)` while loading
  `jei-1.20.1-forge-15.8.0.11`
- root cause:
  - the sister project had been compiled against a newer JEI 15.x API surface
  - RTS JEI handlers were directly calling the newer
    `createClickableIngredient(stack, area, normalize)` path
- fix applied:
  - added `compat/jei/RtsJeiIngredientCompat.java`
  - JEI clickable-ingredient lookup now uses reflection to probe supported
    `createClickableIngredient(...)` signatures at runtime and gracefully
    falls back across JEI 1.20.1 15.x variants
  - lowered sister-project development dependency baseline to:
    - `jei_version=15.8.0.11`
- post-fix validation:
  - `sister-projects/rtsbuilding-forge-1.20.1`: `build` SUCCESS

End of file.

================================================================
11. 2026-04-20 RTS Builder UI Overhaul Pass
================================================================

This pass replaced a large part of the old text-heavy builder top bar and
shape-selection UI with icon-first controls and a persistent quick-build
panel.

Top bar changes:
- the old right-click interaction labels (`Interact`, `Link`, `Funnel`,
  `Rotate`) are now rendered as compact icon buttons instead of large text
  buttons
- the old `FTB check` text button was replaced by a dedicated checkmark icon
  button that still triggers quest detection through
  `ClientRtsController#detectQuestsNow()`
- added a new `Quick Build` button that opens a right-side build-shape panel
- added a new chunk-visibility toggle button in the top bar
- moved system-style utility controls behind a far-right gear button

Quick Build panel changes:
- the old shape context panel flow in `BuilderScreen` was superseded by a
  dedicated right-side quick-build panel
- shape selection is now a 2x3 icon grid using custom pixel-art textures for:
  - `block`
  - `line`
  - `square`
  - `wall`
  - `circle`
  - `box`
- fill mode remains text/radio based (`fill`, `hollow`, `skeleton`) per
  user preference
- rotation was simplified to text + degree readout instead of the earlier
  icon-driven proposal
- all quick-build choices now persist between sessions

Persistent client UI state:
- added `client/RtsClientUiStateStore.java`
- stores the following in `config/rtsbuilding-client-ui.json`:
  - selected build shape
  - fill mode
  - rotation degrees
  - quick-build panel open/closed state
  - chunk curtain visibility
- `BuilderScreen` now loads the persisted state in `init()` and writes it
  back on relevant UI changes and on close

Chunk boundary visualization rework:
- added `chunkCurtainVisible` state to `ClientRtsController`
- replaced the earlier chunk boundary display path in
  `RtsBoundaryRenderer` with translucent blue vertical chunk "curtains"
  closer to the original desired visual language
- the chunk visualization is now explicitly toggleable from the top bar
- 2026-05-22 follow-up: the visible chunk guide was changed from full-height
  blue curtain walls to a tactical horizontal chunk grid at the current player
  operation height:
  - exact 16-block chunk boundaries are drawn from chunk coordinates
  - the current anchor chunk is highlighted in warm amber
  - the outer accessible chunk range is emphasized in brighter blue
  - interior chunk lines are lower-alpha so the guide does not obscure world
    building targets

Asset pipeline / resources:
- generated and staged multiple icon concepts during iteration under
  `staging/`
- `tools/generate_rts_mode_icons.py` keeps preview sheets for the top-bar
  mode icon language under:
  - `staging/ui-icon-concepts/`
  - `staging/ui-icon-final/`
  - `staging/ui-icon-states/`
- `tools/generate_gemini_topbar_icon_concepts.py` can request a fresh
  Nano Banana / Gemini image-model concept sheet for the same four top-bar
  modes, reading the API key from `GEMINI_API_KEY` and writing output under:
  - `staging/gemini-topbar-icons/`
- `tools/chop_gemini_topbar_icons.py` crops the accepted 4x4 Gemini top-bar
  state sheet into 24x24 runtime textures:
  - `src/main/resources/assets/rtsbuilding/textures/gui/topbar/`
  - `staging/gemini-topbar-icons/chopped/`
- accepted quick-build shape icon set was copied into:
  - `src/main/resources/assets/rtsbuilding/textures/gui/quickbuild/`
- generated a current UI concept preview image at:
  - `staging/ui-concepts/current_ui_concept.png`

2026-05-22 top-bar mode icon redraw:
- kept the compact pixel-art / beveled-button design from the 2026-04-20 UI
  pass
- redrew the four primary RTS mode icons in `BuilderScreen`:
  - Interact now reads as a pointer/hand interaction mark instead of a rough
    filled cursor block
  - Link now uses two chain-link loops instead of a four-node topology diagram
  - Funnel now uses a tapered hopper/funnel silhouette instead of stacked bars
  - Rotate now uses an orbiting rotate-arrow around a block center instead of
    disconnected arc fragments
- regenerated the matching staging preview PNGs with
  `tools/generate_rts_mode_icons.py`
- final runtime top-bar mode buttons now use the accepted Gemini/Nano Banana
  chopped PNG assets for inactive / hover / active / pressed states, instead
  of the interim procedural Java symbols

Main code touched for this pass:
- `src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/client/ClientRtsController.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/client/RtsBoundaryRenderer.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/client/RtsClientUiStateStore.java`

Validation:
- ran `.\gradlew.bat compileJava`
- result: SUCCESS
- ran `.\gradlew.bat build`
- result: SUCCESS

Versioning:
- bumped `gradle.properties` `mod_version` from `1.0.8-alpha` to
  `1.0.9-alpha`

Count overlay formatting follow-up:
- reduced slot count overlay text to a smaller scaled render size so stack
  counts no longer dominate 18x18 slot visuals
- unified the overlay drawing path across:
  - `BuilderScreen`
  - `RtsClientInputGate`
  - `RtsCraftTerminalScreen`
  via `client/RtsClientUiUtil.java`
- count display rule is now:
  - below `1000`: show the full integer
  - `1000+`: abbreviate using `K`, `M`, and `B`
  - target roughly `3` significant digits so the text stays short enough for
    narrow item slots
- representative examples:
  - `999 -> 999`
  - `1200 -> 1.2K`
  - `9876 -> 9.88K`
  - `12500 -> 12.5K`
  - `256000 -> 256K`
  - `1542000 -> 1.54M`
- overlay alignment is now right-anchored in the slot footer to reduce
  overflow risk at the display ceiling

================================================================
12. 2026-04-24 Quick Build Cost Display and Forge 1.19.2 Sister Port
================================================================

Quick-build material-cost display:
- updated the main 1.21.1 UI in:
  - `src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java`
- the quick-build panel now displays the current material requirement as:
  - `Materials: <count> blocks`
- the count uses `currentShapeCostText()`, which:
  - returns `1` for single-block mode
  - resolves the current shape input from the active block hit
  - builds the selected shape using the current fill mode
  - filters out occupied/non-ready placement targets before reporting the
    final block count
- this makes the right-side quick-build panel show the consumption estimate
  before confirming shape placement.

UI icon concept pass:
- generated a review-only image-model concept for the top-bar
  `Interact`, `Link`, `Funnel`, and `Rotate` controls
- target style was Minecraft-vanilla-like 16x16 pixel UI icons arranged in a
  clean sheet format for later chopping
- no chopped icon assets were committed from this step yet; it was produced
  for visual approval first.

Forge 1.19.2 sister project:
- created a new isolated sister project at:
  - `sister-projects/rtsbuilding-forge-1.19.2/`
- all 1.19.2 down-port edits for this pass were kept inside that new folder
  except for this documentation entry
- baseline came from the existing Forge 1.20.1 sister project, then was
  retargeted to:
  - Minecraft `1.19.2`
  - Forge `43.4.0`
  - Java runtime/toolchain path compatible with MC 1.19.2 execution
  - JEI `11.6.0.1019`
  - resource `pack_format=9`
  - mod version `0.1.0-1.19.2-snapshot`
- project metadata and docs were updated for the 1.19.2 target:
  - `settings.gradle`
  - `gradle.properties`
  - `README.md`
  - changelog/porting notes
  - `META-INF/mods.toml`
- added a project-local `.gitignore` so generated build/run artifacts stay
  outside the tracked source surface:
  - `build/`
  - `.gradle/`
  - `run/`
  - `run-data/`
  - upstream Forge MDK zip

1.19.2 API compatibility work:
- moved compatibility shims into mod-owned packages to avoid Java module
  split-package conflicts:
  - `com.rtsbuilding.rtsbuilding.forgecompat.client.GuiGraphics`
  - `com.rtsbuilding.rtsbuilding.forgecompat.registry.BuiltInRegistries`
  - `com.rtsbuilding.rtsbuilding.forgecompat.registry.Registries`
- removed earlier `net.minecraft.*` shim classes after runtime testing showed
  they made module resolution fail with exported-package conflicts
- replaced 1.20.x-only imports in the sister project with the local
  compatibility wrappers
- replaced `TransientCraftingContainer` usage with the 1.19.2
  `CraftingContainer` API
- adapted creative-tab access to the older 1.19.2 eager creative tab model
  using `CreativeModeTab.fillItemList(...)`
- changed the mod constructor back to the Forge 1.19.2 JavaFML pattern:
  - `public RtsbuildingMod()`
  - `FMLJavaModLoadingContext.get().getModEventBus()`
- this fixed the client-side mod loading crash:
  - `NoSuchMethodException: com.rtsbuilding.rtsbuilding.RtsbuildingMod.<init>()`

Validation performed for the 1.19.2 sister project:
- static source scan:
  - no remaining `1.20.1`, `47.4.16`, `pack_format 15`, `NeoForge`, or
    `net.neoforged` references in the new sister project source/config
    surface after excluding generated build/cache output
- clean build:
  - command: `.\gradlew.bat --no-daemon --no-watch-fs --console plain clean build`
  - result: SUCCESS
  - output jar:
    `sister-projects/rtsbuilding-forge-1.19.2/build/libs/rtsbuilding-0.1.0-1.19.2-snapshot.jar`
- jar inspection:
  - `mods.toml` advertises Forge loader `[43,)`
  - Minecraft range is `[1.19.2,1.20)`
  - resource pack format is `9`
  - no project-owned classes are packaged under `net/minecraft/*` or
    `net/neoforged/*`
- server launch check:
  - command: `.\gradlew.bat --no-daemon --no-watch-fs --console plain runServer`
  - Forge 43.4.0, Minecraft 1.19.2, RTSBuilding, and JEI were detected
  - launch reached the expected dedicated-server EULA stop
  - EULA was not accepted or modified automatically
- client launch check:
  - first `runClient` found the missing no-arg mod constructor and produced
    an FML crash report
  - after fixing the constructor, `runClient` reached client setup,
    initialized JEI, entered a singleplayer world, and then saved/stopped
    cleanly
  - latest client log contained `HELLO FROM CLIENT SETUP`, JEI runtime
    startup, integrated-server save messages, and normal shutdown messages
  - post-run process check found and stopped leftover Java processes from the
    interrupted Gradle/client command; a follow-up process check showed no
    remaining Java processes

Known caveats after this pass:
- compile output still reports existing deprecation warnings and unchecked
  AE2 compatibility warnings
- `runServer` remains intentionally stopped by the vanilla EULA gate until a
  user explicitly accepts the EULA in the generated run directory
- the generated icon concept for `Interact`, `Link`, `Funnel`, and `Rotate`
  has not yet been chopped into production assets

2026-04-24 - 1.20.1 Forge sister crash investigation and fix:

Crash report summary:
- target environment:
  - Minecraft `1.20.1`
  - Forge `47.4.0`
  - modpack launch name `Craft to Exile 2`
  - RTSBuilding jar `rtsbuilding-0.1.0-1.20.1-snapshot.jar`
  - Sophisticated Storage `1.20.1-1.3.33.1128`
- the Embeddium/Oculus header was not the actionable root cause for this
  crash
- the first actionable failure was RTSBuilding's Sophisticated Storage
  compatibility mixin:
  - `rtsbuilding.mixins.json:compat.sophisticatedstorage.StorageContainerMenuMixin`
  - `@Inject` handler `rtsbuilding$forceRemoteStillValid`
  - target method `stillValid`
  - target class
    `net.p3pp3rf1y.sophisticatedstorage.common.gui.StorageContainerMenu`
- Forge/Mixin failed during game initialization because the target method
  could not be found:
  - `InvalidInjectionException: could not find any targets matching 'stillValid'`
  - `No refMap loaded`
- the later registry errors were treated as likely fallout from this failed
  mixin application:
  - `Registry Object not present: sophisticatedstorage:barrel`
  - Simple Magnets block entity registration NPE
  - Waystones null block registration NPE
  - Nether's Delight creative-tab registry NPE

Fix applied in `sister-projects/rtsbuilding-forge-1.20.1`:
- updated
  `src/main/java/com/rtsbuilding/rtsbuilding/mixin/compat/sophisticatedstorage/StorageContainerMenuMixin.java`
- the mixin now targets both relevant Sophisticated menu types:
  - `net.p3pp3rf1y.sophisticatedstorage.common.gui.StorageContainerMenu`
  - `net.p3pp3rf1y.sophisticatedcore.common.gui.StorageContainerMenuBase`
- the injection now accepts both the development and production method names:
  - `stillValid`
  - `m_6875_`
- the injection is now non-fatal:
  - `require = 0`
- updated
  `src/main/resources/rtsbuilding.mixins.json`
- the mixin config is now tolerant of optional compatibility mismatch:
  - `required: false`
  - `injectors.defaultRequire: 0`

Validation performed:
- build command:
  - `.\gradlew.bat build`
- result:
  - SUCCESS
- generated jar:
  - `sister-projects/rtsbuilding-forge-1.20.1/build/libs/rtsbuilding-0.1.0-1.20.1-snapshot.jar`
- jar inspection confirmed the output jar contains:
  - updated `rtsbuilding.mixins.json`
  - `StorageContainerMenuMixin.class`
- compile output still reports existing `ResourceLocation(String,String)`
  deprecation warnings, but no compile or packaging failure

Expected behavior after the fix:
- if the Sophisticated Storage runtime exposes either supported method name on
  either supported menu class, RTSBuilding keeps the remote menu still-valid
  bypass active
- if a future Sophisticated Storage/SophisticatedCore build changes the method
  or class shape again, this optional compatibility mixin should fail closed
  instead of crashing the entire modpack during initialization

2026-05-22 - RTS settings modal and runtime survival-balance config:

Scope:
- replace the rough top-right gear dropdown with a centered settings modal
- expose survival-balance enable/disable from inside the RTS panel
- expose an in-game material-cost editor for progression/skill-book nodes
- prepare a dedicated Gemini/Nano Banana prompt script for redesigning the
  settings panel visual language

Implementation:
- `src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java`
  - gear/settings UI now renders as a centered modal
  - retained sensitivity, auto-store, and guide controls
  - added a Survival Balance on/off toggle
  - added a compact Skill Cost Editor with previous/next node selection,
    editable `minecraft:item:count` cost text, save, reset, and close buttons
  - routes keyboard and character input into the modal editor field
- `src/main/java/com/rtsbuilding/rtsbuilding/Config.java`
  - added `progressionCostOverrides`
  - added runtime setters for survival progression and skill cost overrides
- `progression/RtsProgressionNodes.java`
  - added config-aware cost lookup and cost-text parse/format helpers
- `progression/RtsProgressionManager.java`
  - unlocks now consume the config-aware cost list
- `client/RtsProgressionScreen.java`
  - progression screen cost display now uses the same config-aware costs
- network:
  - added C2S payloads for survival progression toggling and skill cost edits
  - registered handlers and broadcasts updated progression state to online
    players after an operator edits config
  - progression state now also syncs cost overrides to clients so dedicated
    server UI displays the current server-side material costs
- tools:
  - added `tools/generate_gemini_settings_panel_concepts.py`
  - generalized the Gemini image helper so settings concepts write to
    `staging/gemini-settings-panel/settings_panel_concepts.png`

Validation:
- Ran `.\gradlew.bat --offline --no-daemon compileJava`
- Result: SUCCESS

2026-05-23 - Storage counts, action range, F3-style chunks, bottom guide text tabs, and Box height UX:

Server / storage counts:
- `RtsStorageManager`
  - player inventory counts used by RTS storage pages now include slots 0-35,
    including the player hotbar 1-9
  - quick-slot and recent-item displayed amounts now line up with items carried
    in the hotbar instead of showing zero when the item only exists in hotbar

Server / range model:
- `RtsCameraManager`
  - added `isWithinActionRange(...)`
  - action range is now interpreted as a square half-extent instead of a
    circular radius
  - effective half-extent is progression radius + 8 blocks, matching complete
    chunk coverage for the advertised chunk tier
  - camera panning clamp now uses the same square half-extent
- `RtsProgressionManager`
  - home-radius access uses the same square range rule
  - highest 48 block progression tier covers the intended full 7x7 chunk area
    rather than cutting off edge/corner positions

Client / chunk overlay:
- `RtsBoundaryRenderer`
  - Chunk View was changed from a ground-level grid to an F3-style full-height
    chunk-boundary overlay
  - chunk boundary planes render from world min build height to max build height
    so the player can see chunk borders underground
  - center chunk and outer range still use distinct color emphasis

Client / bottom guide:
- `BuilderScreen`
  - bottom tutorial no longer uses abstract icon-only topic tabs
  - bottom guide tabs are text labels:
    - Sort & Size
    - Craft & Machine Slots
    - Main Storage
    - Recent & Quick Slots
    - Craft Panel

Client / Quick Build Box:
- `BuilderScreen`
  - Box no longer needs a third point click for height
  - after selecting A and B points, the shape enters confirmation preview
  - moving the mouse up/down changes Box height
  - right-click confirms the currently previewed height
- Localization updated for English and Chinese shape status text.

Validation:
- Ran `.\gradlew.bat --offline --no-daemon compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon build`
- Result: SUCCESS

2026-05-23 - Screenless remote-menu recovery for Create/JEI handoffs:

Client screen state:
- `client/ClientRtsController.java`
  - Added a recovery path for a stuck RTS screen state where:
    - RTS mode is still enabled
    - the client player still has a non-zero `containerMenu`
    - `minecraft.screen` is `null`
    - the state is not inside the explicit remote-menu-open grace window
  - This state can happen when external screens such as Create Ponder / JEI
    recipe views interrupt the normal screen handoff; the visible result is
    only vanilla HUD remnants such as the experience bar while RTS does not
    restore until toggled off/on.
  - After 10 consecutive ticks in that screenless menu state:
    - the stale client container is closed with `closeContainer()`
    - remote-menu validation state is cleared
    - normal RTS `BuilderScreen` restoration is allowed to run again
  - Normal RTS remote GUI opening remains protected by the existing 80-tick
    remote menu grace period.

Validation:
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
- Result: SUCCESS

2026-05-23 - Chunk x-ray grid render-stage correction:

Client rendering:
- `client/RtsBoundaryRenderer.java`
  - Root cause for the broken chunk guide was the render stage, not only the
    line geometry:
    - the guide had been moved to `RenderLevelStageEvent.Stage.AFTER_LEVEL`
    - that stage runs after `LevelRenderer.renderLevel` has finished and the
      world model-view stack has already been restored
    - drawing world-space chunk lines there can project them into screen-like
      horizontal/diagonal streaks instead of a stable ground grid
  - Chunk View is now rendered in `AFTER_TRANSLUCENT_BLOCKS`, the same stage
    as the normal red RTS boundary and linked-storage outlines.
  - Chunk View still uses a separate no-depth line pass:
    - custom `POSITION_COLOR_NORMAL` / `RENDERTYPE_LINES_SHADER` RenderType
    - no `VIEW_OFFSET_Z_LAYERING`, so the renderer does not push the grid in
      camera space
    - explicit `RenderSystem.disableDepthTest()` and `depthMask(false)` around
      the draw call
  - Geometry remains the simple flat cyan X/Z chunk-boundary grid at RTS
    anchor Y, built with `LevelRenderer.renderLineBox` so it matches the
    regular RTS outline family.

Validation:
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
- Result: SUCCESS

2026-05-23 - Xray chunk grid and remote GUI flash-close hardening:

Client rendering:
- `client/RtsBoundaryRenderer.java`
  - chunk grid rendering is now separated from normal RTS world overlays:
    - chunk guides render at `RenderLevelStageEvent.Stage.AFTER_LEVEL`
    - linked storage boxes, hover outlines, shape previews, and the red RTS
      radius boundary continue rendering at `AFTER_TRANSLUCENT_BLOCKS`
  - chunk guide geometry now uses thin cyan `POSITION_COLOR` debug lines:
    - full anchor-plane grid across the active RTS chunk area
    - full-height vertical lines at chunk intersections from min build height
      to max build height
    - no thick box/pillar geometry
  - the custom chunk RenderType writes to `MAIN_TARGET` instead of the
    item/entity target so it stays in the main world overlay path.
  - the draw call explicitly disables depth testing and depth writes before
    uploading the chunk guide mesh, then restores vanilla depth state after
    drawing.
  - Reason: in the 1.21.1 RenderStateShard implementation,
    `NO_DEPTH_TEST` with depth func `GL_ALWAYS` does not itself call
    `RenderSystem.disableDepthTest()`. If the previous world pass left depth
    testing enabled, a RenderType-only `NO_DEPTH_TEST` state can still behave
    as if world blocks are occluding the overlay. The RTS chunk guide therefore
    owns this state explicitly.

Remote GUI handoff:
- `client/RtsClientRemoteMenuCompat.java`
  - remote menu screen/menu validation now fails open for vanilla and
    non-Sophisticated screens.
  - Sophisticated Storage safety checks are only attempted when the active
    screen class name belongs to Sophisticated.
  - optional Sophisticated client class probing now catches both
    `ClassNotFoundException` and `LinkageError`.
  - Reason: the dev/remapped runtime can fail to resolve
    `net.p3pp3rf1y.sophisticatedcore.client.gui.StorageScreenBase` because
    that optional class references a client inventory screen name that differs
    from the active runtime mapping. The previous guard interpreted that probe
    failure as a dangerous menu and closed ordinary `ChestMenu` /
    `CraftingMenu`, causing the RTS right-click GUI to flash once and close.

Validation:
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
- Result: SUCCESS

2026-05-23 - Corrected Craft target and chunk grid drawing method:

Correction:
- The previous "floating Craft panel" change targeted the wrong UI surface.
- The RTS Builder bottom bar must keep its embedded Craft panel behavior.
- The collapsible Craft requirement applies to the RTS overlay shown on top of
  ordinary container GUIs such as linked chests/machines.

Builder UI rollback:
- `client/BuilderScreen.java`
  - removed the floating Craft panel introduced in the previous pass
  - removed persisted Craft panel size/open fields from BuilderScreen usage
  - restored bottom bar Craft rendering and click/scroll routing
- `client/RtsClientUiStateStore.java`
  - removed the BuilderScreen floating Craft persisted fields

Container overlay Craft collapse:
- `client/RtsClientInputGate.java`
  - overlay Craft now has a real collapsed width:
    - expanded width: `CRAFT_PANEL_W`
    - collapsed width: `CRAFT_PANEL_COLLAPSED_W`
  - overlay total width is computed dynamically from the Craft state
  - when Craft is collapsed, the linked storage panel shifts left and the
    overall overlay footprint shrinks instead of leaving empty Craft space
  - mouse hit testing, dragging clamp width, scroll routing, and storage panel
    coordinates now use the dynamic overlay width

Chunk grid:
- `client/RtsBoundaryRenderer.java`
  - chunk guide no longer uses raw `DEBUG_LINES`
  - chunk guide now uses a no-depth custom line RenderType compatible with
    `LevelRenderer.renderLineBox`
  - each chunk boundary is drawn as a very thin cyan flat line box at the RTS
    anchor plane, mirroring the existing red boundary-line drawing style
  - explicit no-depth draw wrapper remains, so the flat grid should be visible
    through terrain without becoming full-height/diagonal screen streaks

Validation:
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
- Result: SUCCESS

2026-05-23 - Chunk grid restored to flat xray plane and Craft panel detached:

Client rendering:
- `client/RtsBoundaryRenderer.java`
  - chunk guide keeps the explicit no-depth draw path from the prior fix:
    - `RenderSystem.disableDepthTest()`
    - `RenderSystem.depthMask(false)`
    - restore depth state after upload
  - removed all full-height chunk intersection lines.
  - chunk visualization is now only a flat X/Z plane grid at the RTS anchor
    height, matching the earlier visually correct ground-grid design.
  - Resulting design goal:
    - no tall cyan pillars
    - no diagonal perspective streaks
    - still visible through terrain/blocks because the draw call owns depth
      state explicitly

Builder UI:
- `client/BuilderScreen.java`
  - Craftables panel is no longer embedded inside the bottom storage bar.
  - Bottom storage bar now uses the reclaimed width for storage/recent grids.
  - Bottom header exposes a compact `Craft` toggle; closing Craft removes the
    entire craftables panel instead of leaving an empty frame in the bottom
    layout.
  - Craftables panel now defaults to a floating left-side panel above the
    bottom bar.
  - Floating Craft panel:
    - has a close button in the header
    - has a header resize handle
    - can be dragged via that handle to adjust width/height
    - persists open state and panel size through `RtsClientUiStateStore`
    - consumes mouse/scroll input so world placement/mining does not pass
      through the floating UI
- `client/RtsClientUiStateStore.java`
  - persisted new Craft panel UI fields:
    - `craftPanelOpen`
    - `craftPanelWidth`
    - `craftPanelHeight`

Validation:
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
- Result: SUCCESS

2026-05-23 - Chunk guide returned to flat x-ray grid:

Client rendering:
- `RtsBoundaryRenderer`
  - removed the full-height chunk pillar/curtain presentation
  - chunk guide now renders as a thin flat grid at the RTS anchor Y level
  - current anchor chunk boundaries are highlighted in warm yellow
  - surrounding chunk boundaries remain cyan/blue
  - chunk grid is drawn through terrain by rendering it in a separate line
    buffer with depth test temporarily disabled
  - other overlays such as linked storage boxes, interaction target outlines,
    build range, and shape previews still render with normal depth behavior

Reason:
- The full-height pillar version was too visually heavy and read as random
  poles, especially underground.
- The flat grid is closer to the earlier preferred style while keeping the
  important improvement that blocks no longer hide the chunk boundary lines.

Validation:
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
- Result: SUCCESS

2026-05-23 - Chunk x-ray render state and remote GUI handoff fix:

Chunk View:
- `RtsBoundaryRenderer`
  - replaced the attempted external `RenderSystem.disableDepthTest()` wrapper
    with a dedicated `CHUNK_XRAY_LINES` RenderType
  - the custom RenderType uses `NO_DEPTH_TEST`, `COLOR_WRITE`, translucent
    line rendering, and view-offset layering
  - all chunk grid lines now use cyan coloring; the previous yellow center
    chunk highlight was removed for consistency
  - grid remains flat at RTS anchor Y level and should be visible through
    terrain because depth testing is disabled inside the RenderType itself

Remote GUI opening:
- Root cause found:
  - integrated-server/client timing can expose a non-zero `containerMenu`
    before the vanilla container screen is installed
  - previous logic responded by calling `minecraft.setScreen(null)` from the
    RTS tick, which could close or interrupt the menu handoff and cause the
    observed "flash then close" behavior
  - validation bypass was also too Sophisticated-Storage-specific for normal
    vanilla/container menus opened remotely
- `ClientRtsController`
  - remote GUI handoff now has a `beginRemoteMenuOpenGrace(true)` path used by
    actual GUI-style interactions
  - BuilderScreen is closed before sending those interactions so vanilla
    screen creation can take ownership cleanly
  - the tick loop no longer force-clears BuilderScreen after a remote container
    menu appears
- `RtsSophisticatedStorageCompat`
  - now tracks generic remote menu IDs as well as Sophisticated Storage IDs
  - client and server can force still-valid behavior for any marked remote
    container menu
- Added `RemoteContainerMenuMixin`
  - injects into `AbstractContainerMenu.stillValid(ContainerLevelAccess, Player, Block)`
  - returns true while the player's current container menu is an RTS-marked
    remote menu
- `RtsStorageManager`
  - marks every opened remote menu generically, not only Sophisticated Storage
  - keeps remote validation state while the same container menu remains open

Validation:
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
- Result: SUCCESS
- Ran a short foreground `runClient`; it reached resource loading with no mixin
  apply crash. The command was stopped by tool timeout as expected.

2026-05-23 - Remote GUI restored to 1.0.8/1.0.9 baseline:

Reason:
- The previous attempt added a generic `AbstractContainerMenu` still-valid
  mixin and closed `BuilderScreen` before sending GUI-style interaction
  packets.
- Journal review showed the mature 1.0.8/1.0.9 behavior was different:
  - start a grace window before possible menu opens
  - let the server interaction open the real menu
  - once a non-player `containerMenu` exists, clear BuilderScreen only if it is
    still the current screen
  - use reflection-based validation relaxation for ordinary menus
  - use only the Sophisticated Storage tracked-id mixin for its direct-distance
    `stillValid(...)` implementation

Changes:
- Removed the new generic remote-container mixin and its registration.
- Reverted `RtsSophisticatedStorageCompat` to Sophisticated-only tracked menu
  ids.
- Reverted client remote menu tracking to the old Sophisticated-only marking
  path.
- Reverted GUI interactions to `beginRemoteMenuOpenGrace()` without pre-closing
  BuilderScreen.
- Restored the mature tick-time handoff that clears BuilderScreen only after a
  real remote container menu exists.
- Restored server remote-validation cleanup to the 1.0.8/1.0.9 pattern:
  generic menus get reflection relaxation, Sophisticated menus additionally get
  tracked mixin validation, and non-Sophisticated menus clear the dedicated
  Sophisticated tracking state.

Validation:
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
- Result: SUCCESS

2026-05-23 - Development runtime test pack:

Goal:
- Make regular `runClient` launches include common technology/storage mods so
  RTS interactions are tested against realistic modded blocks and inventories.

Gradle:
- Added Maven repositories:
  - `https://maven.createmod.net`
  - `https://maven.ithundxr.dev/snapshots`
  - `https://api.modrinth.com/maven`
- Added runtime-only local test dependencies:
  - Create `6.0.10-280`
  - Ponder `1.0.82+mc1.21.1`
  - Flywheel `1.0.6`
  - Registrate `MC1.21-1.3.0+67`
  - Sophisticated Core `1.21.1-1.4.35.1823`
  - Sophisticated Storage `1.21.1-1.5.35.1599`
  - Pipez `neoforge-1.21.1-1.2.19`
- `localRuntime` is now also extended into `additionalRuntimeClasspath`.
  This is required for ModDevGradle / Minecraft 1.21.1 runs so external mod
  jars appear in the generated client launch classpath.
- These dependencies are for development runs only and are not production
  dependencies of the published RTS building jar.

Validation:
- Ran `.\gradlew.bat --no-daemon --no-configuration-cache compileJava`
- Result: SUCCESS after downloading dependencies into
  `E:\RTSbuilding\.gradle-user-home`
- Ran `.\gradlew.bat --no-daemon --no-configuration-cache writeClientLegacyClasspath --rerun-tasks`
- Result: SUCCESS
- Confirmed `build/moddev/clientLegacyClasspath.txt` contains Create,
  Flywheel, Ponder, Registrate, Sophisticated Core, Sophisticated Storage,
  Pipez, and JEI.

2026-05-23 - Chunk pillars, linked tool refresh, selected durability, and storage overlay polish:

Client / Chunk View:
- `RtsBoundaryRenderer`
  - Chunk View now adds F3-like yellow vertical pillars at every chunk corner
  - pillars run from world min build height to max build height, improving
    underground visibility
  - center/outer boundaries keep stronger emphasis while intermediate chunk
    corners remain visible

Server / linked mining tools:
- `RtsStorageManager`
  - after a linked-storage mining tool is returned from normal Mine or Ultimine,
    storage page data is immediately resent
  - this prevents the selected tool from disappearing from the Linked Storage UI
    until the user searches again

Client / selected item status:
- `BuilderScreen`
  - selected damageable items now show durability in the top status line,
    e.g. `Netherite Axe 2030/2031`
- `ClientRtsController`
  - when storage data refreshes, the selected item preview is refreshed from the
    current storage entry so durability changes are reflected

Client / remote menu and overlay:
- `ClientRtsController`
  - remote menu handoff grace increased from 20 ticks to 80 ticks to reduce
    BuilderScreen restore races while vanilla/container screens are opening
- `RtsClientInputGate`
  - storage overlay was made smaller:
    - storage panel 212 -> 196 px
    - craft panel 126 -> 116 px
    - slot size 18 -> 16 px
  - removed the redundant `RTS` text from the overlay header
  - craft overlay can be collapsed/expanded by clicking its `Craft +/-` header

Validation:
- Ran `.\gradlew.bat --offline --no-daemon build`
- Result: SUCCESS

2026-05-23 - RTS selected storage tools for mining and Ultimine:

Network:
- `C2SRtsMinePayload`
  - now carries `toolItemId`
- `C2SRtsUltiminePayload`
  - now carries `toolItemId`
- `RtsClientPacketGateway`
  - Mine / Ultimine start packets include the current RTS-selected non-block
    item id when present

Client:
- `ClientRtsController`
  - mining tool resolution now distinguishes between:
    - player hotbar tool slot
    - RTS-selected storage/quick-slot non-block item
  - block items selected for placement are not sent as mining tools

Server:
- `RtsStorageManager`
  - normal remote mining and Ultimine can now use an RTS-selected tool from
    linked storage
  - on mining start, the server extracts one matching non-block tool from
    linked storage
  - destroy progress calculation uses that extracted tool in the temporary
    main-hand context, so axe/pick/shovel speed and harvest rules apply
  - actual block destruction also uses the same extracted tool
  - Ultimine reuses the same extracted tool across all chained block breaks
  - when mining finishes or is aborted, the damaged/remaining tool is returned
    to linked storage, then player inventory, then dropped if no storage space
    is available

Validation:
- Ran `.\gradlew.bat --offline --no-daemon compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon build`
- Result: SUCCESS

2026-05-23 - Reference-style Gemini topbar icons:

Asset correction:
- The first v3 safe-area redraw was rejected because it did not match the
  established Interact / Link / Funnel / Rotate style.
- Built a style reference atlas from the existing topbar mode textures:
  - `staging/gemini-topbar-icons-v4-style-reference.png`
- `tools/generate_gemini_topbar_icon_concepts.py`
  - now supports `RTS_GEMINI_REFERENCE_IMAGE`
  - the helper can send an existing PNG reference image to Gemini alongside
    the prompt
- Generated a new Gemini sheet that follows the established button spec:
  - dark beveled full button background
  - mostly white/off-white icon subjects
  - blue hover row
  - green active row
  - brown pressed row
  - source: `staging/gemini-topbar-icons-v4/quick_ultimine_chunk_reference_style_sheet.png`
- The runtime Quick Build, Ultimine, and Chunk View textures now use direct
  full-cell chops from that Gemini sheet:
  - source columns used: Quick Build column 1, Ultimine column 3, Chunk View
    column 4
  - resized each full button cell to native 24x24
  - preview: `staging/gemini-topbar-icons-v4/icon_preview_chopped_2x.png`

Validation:
- Ran `.\gradlew.bat --offline --no-daemon compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon build`
- Result: SUCCESS

2026-05-23 - Safe 24x24 topbar icons, bottom tutorial rewrite, and RTS craft hotkey:

Gemini / asset pipeline:
- `tools/generate_gemini_topbar_icon_concepts.py`
  - added `RTS_GEMINI_MODELS_OVERRIDE` so a single requested image model can be
    run instead of the whole fallback list
  - added `RTS_GEMINI_IMAGE_SIZE` so Gemini Pro image models can use `1K`
    while Flash-style models can still use `512`
- Generated two new concept sheets from a stricter pixel-art prompt:
  - Pro reference:
    - `staging/gemini-topbar-icons-v3-pro/quick_ultimine_chunk_sheet.png`
    - model: `gemini-3-pro-image-preview`
  - Flash / Nano Banana 2 style reference:
    - `staging/gemini-topbar-icons-v3-flash/quick_ultimine_chunk_sheet.png`
    - model: `gemini-3.1-flash-image-preview`
- Final runtime textures were rebuilt as native 24x24 pixel buttons rather than
  direct crops from the generated atlas. This avoids the previous issue where
  model padding and non-integer atlas cells caused Quick Build, Ultimine, and
  Chunk View icons to crop incorrectly.
- Runtime textures replaced:
  - `quick_build_{inactive,hover,active,pressed}.png`
  - `ultimine_{inactive,hover,active,pressed}.png`
  - `chunk_view_{inactive,hover,active,pressed}.png`
- Preview sheet:
  - `staging/gemini-topbar-icons-v3-flash/icon_preview_manual_2x.png`

Client UI:
- `BuilderScreen`
  - pressing `C` now opens RTS Crafting directly when the Craft Terminal
    progression node is unlocked and no search field is focused
  - bottom guide topics were restructured around actual left-to-right UI
    reading order:
    - Sort & Size: Qty / Mod / Name, S cycling, A/D direction, +/- panel height
    - Craft & Machine Slots: C button, keyboard C shortcut, remote GUI + slots
    - Main Storage: search, storage grid, player hotbar 1-9, fluid strip, fluid
      tank binding
    - Recent & Quick Slots: recent items and RTS quick slots 1-27
    - Craft Panel: craftable search and recipe paging
  - added a small SORT guide glyph for the bottom tutorial topic selector

Localization:
- `en_us.json` and `zh_cn.json`
  - added new bottom-guide copy for the restructured tutorial topics
  - included explicit Chinese explanations for Qty / Mod / Name, S, A/D, +/-,
    C, remote GUI binding, main storage, fluid tanks, recent items, quick slots,
    and craft panel behavior

Validation:
- Ran `.\gradlew.bat --offline --no-daemon compileJava`
- Result: SUCCESS
- Gemini settings script was attempted, but the current shell has no
  `GEMINI_API_KEY` environment variable; the script is ready and does not
  store the key in the repo.

2026-05-22 - Gemini settings concept applied:

Generated:
- `staging/gemini-settings-panel/settings_panel_concepts.png`

Applied UI direction:
- `client/BuilderScreen.java`
  - centered modal now follows the concept image more closely
  - title bar reads `RTSBUILDING CONFIGURATION`
  - close button moved into the title bar
  - Controls section uses a framed sensitivity track and auto-store switch
  - Survival Balance section uses a larger enabled/disabled switch
  - Skill Cost Editor section now has a left progression-node list and a
    right selected-node material cost editor
  - bottom actions are SAVE / RESET / CLOSE

Validation:
- Ran `.\gradlew.bat --offline --no-daemon compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon build`
- Result: SUCCESS

2026-05-22 - Config ownership correction and progression cost icons:

Reason:
- The RTS top-bar gear menu should remain a local controls/settings popup.
- Survival Balance and progression material costs are gameplay/server rules,
  so they belong in the mod config screen and should be treated as
  server-authoritative in multiplayer.
- The RTS Progression screen's text-only costs were hard to read because the
  blurred/covered background made materials non-intuitive.

Implementation:
- Generated a Gemini/Nano Banana top-bar settings gear concept and chopped it
  into 24x24 topbar texture resources:
  - `settings_gear_inactive.png`
  - `settings_gear_hover.png`
  - `settings_gear_active.png`
  - `settings_gear_pressed.png`
- `client/BuilderScreen.java`
  - gear button now uses these texture assets
  - gear popup now only contains local controls:
    - sensitivity
    - auto-store
  - removed Survival Balance and Skill Cost Editor from the RTS overlay
- `client/RtsModConfigScreen.java`
  - new NeoForge mod config screen for RTSBuilding
  - exposes Survival Balance, shared-team progression, and editable
    progression material cost overrides
- `RtsbuildingMod.java`
  - registers the client config screen with `IConfigScreenFactory`
- `client/RtsProgressionScreen.java`
  - no longer draws the default blurred background first
  - uses a stronger opaque panel
  - renders actual item icons with `xN` counts for material costs

Validation:
- Ran `.\gradlew.bat --offline --no-daemon compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon build`
- Result: SUCCESS

2026-05-22 - Rename display title and produce Built From Above artifact:

Naming:
- Internal mod id remains `rtsbuilding`.
- English display name:
  - `RTS building Build From Above`
- Simplified Chinese display name:
  - `俯视角建造`

Implementation:
- `gradle.properties`
  - updated `mod_name`
  - bumped `mod_version` to `1.0.10-alpha`
  - added `mod_archive_name=rtsbuilding-built-from-above`
- `build.gradle`
  - archive base name now uses `mod_archive_name` when present
- `assets/rtsbuilding/lang/en_us.json`
  - updated visible English creative-tab/key-category strings
- `assets/rtsbuilding/lang/zh_cn.json`
  - added Chinese localization strings

Validation:
- Ran `.\gradlew.bat --offline --no-daemon clean build`
- Result: SUCCESS
- Produced jar:
  - `build/libs/rtsbuilding-built-from-above-1.0.10-alpha.jar`
- Verified generated mod metadata:
  - `modId="rtsbuilding"`
  - `displayName="RTS building Build From Above"`
  - `version="1.0.10-alpha"`

2026-05-22 - RTS Progression skill tree and first i18n pass:

Reason:
- The previous RTS Progression screen was still behaving like a list.
- Custom text/icons were visually washed out behind screen blur while vanilla
  unlock buttons remained crisp.
- Unlock buttons were enabled when prerequisites were met even if the player
  did not have enough materials.
- The project is moving toward formal English/Chinese support.

Gemini reference:
- Added `tools/generate_gemini_progression_tree_concept.py`.
- Generated:
  - `staging/gemini-progression-tree/progression_tree_concept.png`
- Used the result as a design guide for a compact technology-tree UI:
  - opaque centered panel
  - dependency lines
  - green unlocked nodes
  - amber unlockable nodes
  - muted locked nodes
  - material item icons with `xN` counts

Implementation:
- `network/S2CRtsProgressionStatePayload.java`
  - added `unlockableNodes`
- `server/RtsProgressionManager.java`
  - computes unlockable nodes on the server from dependencies plus
    material availability
- `client/ClientRtsController.java`
  - tracks the server-synced unlockable node set
- `client/RtsProgressionScreen.java`
  - rewritten as a custom skill tree screen
  - avoids the default blurred background path
  - draws node cards, dependency lines, cost item icons, and status badges
  - unlock clicks are accepted only for server-marked unlockable nodes
- `assets/rtsbuilding/lang/en_us.json`
  - added progression screen strings and node names/descriptions
- `assets/rtsbuilding/lang/zh_cn.json`
  - added Chinese progression screen strings and node names/descriptions

Validation:
- Ran `.\gradlew.bat --offline --no-daemon compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon build`
- Result: SUCCESS

2026-05-22 - RTS Progression state clarity and zoom controls:

Bug fix:
- `server/RtsProgressionManager.syncToPlayer(...)` now ensures the starter
  `camera_core` node is present and saved before computing synced unlocked and
  unlockable node sets.
- This fixes the confusing state where `Camera Core` could show as
  free/unlockable, but clicking it returned "Already unlocked."

UI changes:
- `client/RtsProgressionScreen.java`
  - added four explicit node states:
    - unlocked
    - unlockable
    - missing materials
    - prerequisite locked
  - each state has its own color language:
    - unlocked: green
    - unlockable: amber
    - missing materials: warm brown/orange
    - prerequisite locked: muted blue/gray
  - removed the ambiguous check-box-like glyph from node cards
  - node status is now displayed with text plus a colored side accent bar
  - dependency lines are thicker and higher contrast
  - default node spacing is larger
  - added a bottom zoom bar
  - mouse wheel also changes skill-tree zoom

Localization:
- `en_us.json` and `zh_cn.json`
  - added labels for zoom, missing items, and all four node states

Validation:
- Ran `.\gradlew.bat --offline --no-daemon compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon build`
- Result: SUCCESS

2026-05-22 - RTS Home relocation gate, hidden locked panels, and clipped skill-tree viewport:

Architecture:
- Survival progression remains server authoritative.
- BuilderScreen now treats progression nodes as feature gates for both buttons
  and secondary panels:
  - `remote_place` controls quick-build/shape panel access.
  - `ultimine` controls the ultimine panel.
  - `craft_terminal` controls the craft terminal dock button.
  - `remote_gui` controls remote GUI binding slots.
  - locked features are hidden instead of shown as inactive clutter.
- RTS Home relocation is now a progression-controlled capability:
  - first home set is allowed when no home exists
  - changing an existing home requires `rtsbuilding:home_relocation`
  - this replaces the previous cooldown-style change model

Progression graph:
- Added `rtsbuilding:home_relocation`
  - no dependencies
  - cost: `minecraft:netherite_block` x10
  - used directly by `RtsProgressionManager.canChangeHome(...)`
- The node intentionally has no effect flag because the home manager checks the
  unlocked node set directly.

Client UI:
- `RtsProgressionScreen`
  - skill tree renders inside a larger centered panel
  - inner tree viewport is clipped with `GuiGraphics.enableScissor(...)`
  - zoom range is 45% to 235%
  - card dimensions are damped against zoom so zoom mostly changes spacing and
    readability, not just node size
  - dependency lines remain thick and visible while clipped to the viewport
- `RtsHomeScreen`
  - shows current home, dimension, radius, relocation state, and a warning that
    the starter home should be chosen carefully
  - change-home button is disabled until Home Relocation is unlocked
- `BuilderScreen`
  - top info button opens mode/build-flow guidance
  - bottom info button opens storage/crafting/pin guidance
  - gear modal click region now matches the visual centered modal

Localization:
- English and Simplified Chinese translations were added for:
  - Home Relocation
  - RTS Home warning/state strings
  - top-bar guide
  - bottom-storage guide

Validation:
- Ran `.\gradlew.bat --offline --no-daemon compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon build`
- Result: SUCCESS
- Produced/updated jar:
  - `build/libs/rtsbuilding-built-from-above-1.0.10-alpha.jar`

2026-05-23 - BuilderScreen tutorial/localization pass and fixed GUI-scale layer:

Architecture:
- `BuilderScreen` now owns a small virtual GUI-scale layer for RTS mode:
  - original target RTS UI scale was 3; current default is configurable 2x as
    of `1.0.11-alpha`
  - render pass temporarily converts `Screen.width` / `Screen.height` to the
    scale-3 virtual coordinate space and applies a matching pose scale
  - mouse click, drag, release, and scroll events are mapped into the same
    virtual coordinate space
  - the goal is stable RTS UI sizing regardless of the player's global
    Minecraft GUI scale

Client controls:
- `ClientRtsController`
  - exposes input sensitivity preset index/count
  - exposes `setInputSensitivityByFraction(...)`
  - the settings UI can now behave as a real slider while still snapping to
    stable preset values

BuilderScreen UI:
- Gear/settings modal:
  - widened and made taller
  - added a settings-specific `i` help button
  - Sensitivity is click/drag driven
  - Auto-store row/switch are both clickable
  - labels and ON/OFF states are localized
- Help/tutorial overlay:
  - supports guide contexts:
    - top bar
    - bottom bar
    - settings
  - supports paging with Prev/Next controls
  - panel is smaller and raised slightly so it is less visually tangled with
    the bottom storage panel
- Visible localized BuilderScreen text now includes:
  - top status line
  - home selection overlay
  - quick-build panel labels
  - ultimine panel labels
  - storage panel title/category
  - common hover tooltips

Localization:
- `en_us.json` and `zh_cn.json`
  - added broader status strings
  - added shape/fill labels
  - expanded top tutorial to 18 entries
  - expanded bottom tutorial to 14 entries
  - added settings tutorial to explain sensitivity and auto-store
  - documented Storage Link left-click binding and right-click suppression
  - documented normal right-click behavior for mobs/machines/blocks

Validation:
- Ran `.\gradlew.bat --offline --no-daemon compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon build`
- Result: SUCCESS

2026-05-23 - RTS Progression tree pan interaction:

Client UI:
- `RtsProgressionScreen`
  - tree viewport now supports left-click drag panning
  - virtual canvas offset is stored as `panX` / `panY`
  - node layout applies pan after centering and zoom spacing
  - unlock badge clicks still run before pan begins
  - pan bounds are computed from:
    - current zoom
    - card dimensions
    - virtual tree content size
    - inner viewport size
  - bounded overscroll margins allow comfortable focus on edge nodes while
    preventing the tree from being dragged entirely off-screen
  - zoom and drag both reclamp pan to keep the focus valid

Validation:
- Ran `.\gradlew.bat --offline --no-daemon compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon build`
- Result: SUCCESS

2026-05-23 - Compact topic-based BuilderScreen guide:

Architecture:
- BuilderScreen guide content now uses topic records instead of page slices:
  - `GuideTopic`
  - `GuideIcon`
  - `GuidePanelRect`
- Guide contexts remain:
  - top bar
  - bottom bar
  - settings
- Each context now exposes a small icon directory on the left side of the panel.
  Clicking an icon switches the visible topic.

Client UI:
- `BuilderScreen`
  - guide panel is attached to its source:
    - top guide appears below the top bar
    - bottom guide appears above the bottom storage panel
    - settings guide appears beside the settings modal when space allows
  - guide panel no longer applies a full-screen dark overlay
  - Quick Build and Ultimine top-bar controls are compact icon buttons
  - fallback pixel icons were added for Quick Build and Ultimine
  - FTB quest detect button now appears only when FTB-related mods are loaded:
    - `ftbquests`
    - `ftb_quests`
    - `ftblibrary`

Tutorial content:
- Gemini 2.5 Flash was used to review the tutorial structure and suggested:
  - topic-based guide groups
  - short practical copy per icon
  - avoiding large modal tutorial panels
  - keeping guide placement consistent with the source UI bar
- Implemented concise English and Chinese topic copy for:
  - top bar modes/tools
  - bottom storage functions
  - settings controls

Validation:
- Ran `.\gradlew.bat --offline --no-daemon compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon build`
- Result: SUCCESS

2026-05-23 - Topbar icon refresh and bottom panel collision fix:

Assets:
- Added Gemini-generated topbar icon state textures for:
  - Quick Build
  - Ultimine
  - Chunk View
- Source concept sheet:
  - `staging/gemini-topbar-icons-v2/quick_ultimine_chunk_sheet.png`
- Runtime textures:
  - `assets/rtsbuilding/textures/gui/topbar/quick_build_inactive.png`
  - `assets/rtsbuilding/textures/gui/topbar/quick_build_hover.png`
  - `assets/rtsbuilding/textures/gui/topbar/quick_build_active.png`
  - `assets/rtsbuilding/textures/gui/topbar/quick_build_pressed.png`
  - `assets/rtsbuilding/textures/gui/topbar/ultimine_inactive.png`
  - `assets/rtsbuilding/textures/gui/topbar/ultimine_hover.png`
  - `assets/rtsbuilding/textures/gui/topbar/ultimine_active.png`
  - `assets/rtsbuilding/textures/gui/topbar/ultimine_pressed.png`
  - `assets/rtsbuilding/textures/gui/topbar/chunk_view_inactive.png`
  - `assets/rtsbuilding/textures/gui/topbar/chunk_view_hover.png`
  - `assets/rtsbuilding/textures/gui/topbar/chunk_view_active.png`
  - `assets/rtsbuilding/textures/gui/topbar/chunk_view_pressed.png`

Client UI:
- `BuilderScreen`
  - `topbarModeTexture(...)` now resolves Quick Build, Ultimine, and Chunk View
    texture state sets
  - guide topic icon directory now draws current topbar textures for the top
    bar topics instead of older fallback glyphs
  - Storage Link guide copy now matches actual implementation:
    - left click = bidirectional storage + extraction
    - right click = extract-only
- Bottom panel layout:
  - craft panel width increased to 126 px
  - craft apply/toggle controls widened
  - storage search area is now constrained against the main storage region so
    the search clear button and pager do not overlap the craft panel
- `RtsClientInputGate` and JEI overlay extra-area dimensions were updated to
  match the wider craft panel.

External review:
- Gemini was given relevant Storage Link code snippets and confirmed concise
  tutorial wording for Interact, Storage Link, Quick Build, Ultimine, and Chunk
  View.

Validation:
- Ran `.\gradlew.bat --offline --no-daemon compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon build`
- Result: SUCCESS

2026-05-23 - Recipe-viewer key handoff and screenless menu recovery:

Client input / screen compatibility:
- `client/ClientRtsController.java`
  - Added the screenless remote-menu recovery described above for Create /
    JEI-style handoffs:
    - non-zero client `containerMenu`
    - `minecraft.screen == null`
    - no active RTS remote-menu opening grace
    - recover by closing the stale client container after 10 ticks and letting
      `BuilderScreen` restore normally
- `client/BuilderScreen.java`
  - Recipe viewers now keep ownership of `R` when loaded:
    - `jei`
    - `emi`
    - `roughlyenoughitems`
  - RTS no longer consumes `R` for shape rotation in those environments, so
    Create Ponder -> recipe lookup flows can reach the recipe viewer instead
    of being intercepted by RTS.

Validation:
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
- Result: SUCCESS

2026-05-23 - 1.0.11-alpha UI scale release preparation:

Versioning:
- `gradle.properties`
  - bumped `mod_version` from `1.0.10-alpha` to `1.0.11-alpha`
  - archive name remains `rtsbuilding-built-from-above`

Client UI scale:
- `client/BuilderScreen.java`
  - fixed RTS overlay rendering now targets GUI scale `2x` by default instead
    of the previous hardcoded `3x` baseline
  - the fixed-scale wrapper still transforms render and mouse input together,
    so clicking, dragging, scrolling, and hover coordinates remain aligned with
    the visually scaled RTS panel
  - added a client settings row in the gear menu for `RTS UI scale`
    - range: `1x` to `4x`
    - step: `0.5x`
    - default: `2x`
  - settings guide placement now follows the taller gear menu
- `client/RtsClientUiStateStore.java`
  - added persisted `rtsGuiScale`
  - sanitizes saved values to the same `1x` to `4x` / `0.5x` step grid

Localization:
- `assets/rtsbuilding/lang/en_us.json`
  - added settings label and guide copy for RTS UI scale
- `assets/rtsbuilding/lang/zh_cn.json`
  - added Chinese settings label and guide copy for RTS UI scale

Validation so far:
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
- Result: SUCCESS
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
- Result: SUCCESS
- Produced jar:
  - `build/libs/rtsbuilding-built-from-above-1.0.11-alpha.jar`

2026-05-24 - Field Deployment replaces Home Relocation:

Survival progression:
- `progression/RtsProgressionNodes.java`
  - removed the separate `home_relocation` node from the active progression
    tree
  - changed `field_deployment` cost from:
    - `minecraft:ender_eye` x4
    - `minecraft:gold_block` x1
  - to:
    - `minecraft:dragon_head` x1
  - `field_deployment` remains dependent on `radius_3`

RTS Home rules:
- `server/RtsProgressionManager.java`
  - changing an existing RTS Home now requires `field_deployment`
  - the old `home_relocation` unlock is no longer checked
- `server/RtsCameraManager.java`
  - the server denial message now names `Field Deployment` instead of the old
    day/cooldown wording
- `client/RtsHomeScreen.java`
  - the Home panel's relocation gate now checks `field_deployment`

Localization:
- `assets/rtsbuilding/lang/en_us.json`
  - Field Deployment description now says it bypasses home radius and allows
    moving RTS Home
  - removed active Home Relocation progression copy
- `assets/rtsbuilding/lang/zh_cn.json`
  - same progression/home copy change in Chinese

Chunk loading policy:
- linked-storage chunk loading was intentionally not added
- current behavior remains:
  - linked storage must be in a loaded chunk
  - modpack chunk loaders such as FTB chunk loading can handle persistent
    loading

Validation:
- Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
- Result: SUCCESS

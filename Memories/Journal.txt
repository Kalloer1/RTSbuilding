2026-03-04 - RTS Fluid Normalization Refactor (NeoForge 1.21.x)

[背景目標]
- 移除 bucket 專例交互補丁，改為統一流體管線�?- 支持�?RTS storage / tool bar / pin 區右鍵把容器流體抽�?RTS 流體池�?- 支持�?RTS 流體池選取流體，對世界進行流體放置或機器灌注�?- UI 需要在 storage grid 左側增加 2 格一行（橙色）流體排，與原有搜尋共用�?- 內置容量目標：每種流�?100 桶；外部已綁定流體罐作共享容�?訪問來源�?
[步驟 1 - 網路協議擴展]
- 修改 `S2CRtsStoragePagePayload`�?  - 新增 `fluidIds / fluidAmounts / fluidCapacities` 三組字段�?  - 編解碼器(StreamCodec)同步寫入/讀取流體數據�?- 新增 `C2SRtsStoreFluidPayload`�?  - sourceType:
    - 0 = storage item
    - 1 = tool slot
    - 2 = pin item
  - toolSlot + itemId 作為 source 參數�?- 新增 `C2SRtsPlaceFluidPayload`�?  - BlockHit 位置 + face + hitVec + forcePlace + fluidId�?- 更新 `RtsPayloadRegistrar`�?  - 註冊 `C2SRtsStoreFluidPayload`�?  - 註冊 `C2SRtsPlaceFluidPayload`�?- 更新 `RtsNetworkHandlers`�?  - 新增 `handleStoreFluid` -> `RtsStorageManager.storeFluidFromContainer(...)`�?  - 新增 `handlePlaceFluid` -> `RtsStorageManager.placeFluid(...)`�?
[步驟 2 - Server 端流體核心]
- �?`RtsStorageManager` 引入 fluid stack:
  - `FluidStack` / `FluidUtil` / `IFluidHandler` / `IFluidHandlerItem` / `FluidType`�?- 新增常量�?  - `FLUID_TRANSFER_MB = 1000`�? 桶）�?  - `INTERNAL_FLUID_CAPACITY_MB = 100000`�?00 桶）�?- `Session` 新增�?  - `internalFluidMb: Map<String, Long>`（玩�?RTS 內部流體池）�?- `linkStorage` 修改�?  - 以前只接�?`IItemHandler`�?  - 現在可接�?`IItemHandler` �?`IFluidHandler` 任一能力�?- `requestPage` 修改�?  - item 聚合保留�?  - fluid 聚合新增�?    - 聚合 session 內部流體池�?    - 聚合已綁定外�?`IFluidHandler` tank 內容�?    - capacity 由「外�?tank 容量 + 內置 100 桶基礎容量」合成�?  - 搜索/category/sort 同時作用�?item + fluid�?  - S2C payload 帶回 fluid 列表�?- 新增 `storeFluidFromContainer(...)`�?  - 支持來源：storage item / tool slot / pin item�?  - 對容器做 1 �?drain（fluid capability），再寫�?fluid network�?- 新增 `placeFluid(...)`�?  - 先檢查流體可用量�?  - 優先嘗試向目�?block �?fluid handler 灌注（機器兼容路徑）�?  - 若失敗，�?fallback �?bucket-like 使用流程放置流體�?  - 根據 remainder 推導實際消耗量，再�?fluid network 扣除�?- 新增 helper�?  - `findFluidHandler`
  - `resolveLinkedFluidHandlers`
  - `drainContainer`
  - `insertFluidIntoNetwork`
  - `extractFluidFromNetwork`
  - `fillFluidHandlerAtTarget`
  - `resolveConsumedFluidAmount`

[步驟 3 - 移除 bucket 特判]
- `placeSelected` 中移除：
  - bucket 專屬 `useOn` 臨時位置分支�?- 刪除不再使用的方法：
  - `resolveUseOnInteractionPosition(...)`
  - `requiresRemoteUseOnContext(...)`
- 結果�?  - �?item 放置路徑回歸統一邏輯�?  - 流體行為改由顯式 fluid 系統承擔�?
[步驟 4 - Client Controller]
- `ClientRtsController` 新增 state�?  - `fluidEntries`
  - `selectedFluidId / selectedFluidLabel / selectedFluidPreview`
- `applyStoragePage(...)` 新增 fluid 解析�?  - �?`BuiltInRegistries.FLUID` 還原流體�?  - 生成 bucket preview 用於 UI 顯示�?- 新增方法�?  - `selectFluidEntry`
  - `placeSelectedFluid`
  - `storeFluidFromStorageItem`
  - `storeFluidFromPinnedItem`
  - `storeFluidFromToolSlot`
- 選擇互斥策略�?  - �?item 時清 fluid selection�?  - �?fluid 時清 item selection�?
[步驟 5 - BuilderScreen UI/互動]
- 新增 `hoveredFluidEntry`�?- 下方 panel 佈局改造：
  - 左側新增橙色 fluid strip�? 格一行）�?  - 右側保持 item storage grid�?- 新增 `drawFluidGrid(...)`�?  - 顯示 fluid preview + 量�?overlay（桶數）�?  - 支援 hover tooltip（amount/capacity）�?- 點擊行為�?  - 左鍵 fluid �?=> `selectFluidEntry`�?  - 右鍵 storage/tool/pin => 觸發 `storeFluidFrom*`�?- 世界右鍵行為�?  - 若選�?fluid => �?`placeSelectedFluid(...)`�?  - 否則保留�?item/工具交互流�?- 刪除 client bucket 專用 raycast 判斷�?  - 移除 `shouldUseFluidSourceRaycast()`�?
[步驟 6 - 編譯驗證]
- 執行�?  - `.\gradlew.bat compileJava -x test`
- 結果�?  - 編譯成功（僅 deprecation 提示，無編譯錯誤）�?
[本次修改檔案]
- `src/main/java/com/rtsbuilding/rtsbuilding/network/S2CRtsStoragePagePayload.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/network/C2SRtsStoreFluidPayload.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/network/C2SRtsPlaceFluidPayload.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/network/RtsPayloadRegistrar.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/network/RtsNetworkHandlers.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/client/ClientRtsController.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java`
- `architecture.txt`
- `archetecture.txt`
- `Journal.txt`

2026-03-04 - Follow-up: UI label removal + direct fluid-block placement

[需求]
- 刪除 UI �?`Tools 1-9` / `RTS` / `Fluid` 三個文字標籤�?- 修正 fluid 右鍵地面無法放置：改為“在射線位置直接放流體方塊”，而非水桶右鍵路徑�?
[步驟 A - UI 文本刪除]
- 文件：`src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java`
- 刪除以下 drawString�?  - 工具列標籤：`Tools 1-9`
  - Pin 標籤：`RTS Pin ...`
  - 流體列標籤：`Fluid`
- 保留原有功能�?  - 工具欄切換、Pin 分頁、流體格點擊與右鍵提取均不變�?
[步驟 B - Fluid 放置核心邏輯重寫]
- 文件：`src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java`
- 入口：`placeFluid(...)`
- 行為改為�?  1. 先嘗試目標方�?fluid handler 灌注（保留機器兼容）�?  2. 若灌注失敗，改走“直接放流體方塊”：
     - 解析放置位：�?`clickedPos`，不可放則試 `clickedPos.relative(face)`�?     - 檢查可放置性（replaceable / non-solid / liquid container / canBePlacedInLevel）�?     - 直接生成與設�?fluid block state（不�?bucket useOn/useItem）�?  3. 成功後扣�?1 桶（1000 mB）流體�?
[新增方法]
- `resolveFluidPlacementPos(...)`
- `canPlaceFluidAt(...)`
- `placeFluidBlock(...)`

[移除舊路徑]
- `placeFluid` �?bucket-like fallback（`useItemOnWithMainHand` / `useItemWithMainHand`）不再參與流體方塊放置�?- 與之相關的“由剩餘桶內容推導消耗量”輔助邏輯不再用於此路徑�?
[結果]
- 點擊 Fluid 區的水後，右鍵世界可按射線命中邏輯直接放水方塊�?- 點擊機器時，仍保留先嘗試流體能力灌注�?- UI 三個文字標籤已移除�?
[驗證]
- `.\gradlew.bat compileJava -x test`
- 結果：BUILD SUCCESSFUL�?
2026-03-04 - Persist RTS link/fluid state into player NBT

[需求]
- 把「綁定存儲位置」與「RTS 模式內部流體池」存成玩�?NBT 一部分�?- 目標：玩家退出重進後，綁定箱子與已存流體保持一致�?
[修改檔案]
- `src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java`

[步驟 1 - Session 生命週期調整]
- 新增 `getOrCreateSession(ServerPlayer)`�?  - 先查記憶�?`SESSIONS`�?  - 若不存在，建立新 session 並從玩家 NBT 載入�?  - 放回 `SESSIONS`�?- 把原先多�?`computeIfAbsent` 改走 `getOrCreateSession`，確�?session 可從 NBT 回填�?
[步驟 2 - NBT Schema 設計]
- 玩家 PersistentData 根節點：
  - `rtsbuilding_storage_session`
- 保存欄位�?  - `linked_positions`：`long[]`（`BlockPos#asLong`�?  - `linked_dimension`：`ResourceLocation` string
  - `internal_fluids`：`List<CompoundTag>`
    - `id`：fluid registry id
    - `amount`：mB

[步驟 3 - 寫入/讀取函式]
- 新增 `loadSessionFromPlayerNbt(...)`�?  - 讀�?linked positions / dimension / internal fluid map�?  - �?fluid id �?registry 驗證，過濾非法資料�?- 新增 `saveSessionToPlayerNbt(...)`�?  - �?linked positions �?internal fluid map 反序列化寫回玩家 NBT�?
[步驟 4 - 保存觸發點]
- 登出�?  - `onPlayerLogout` 先保�?session，再從記憶體 map 移除�?- 綁定變更�?  - `linkStorage` link/unlink 後立即保存�?- 流體變更�?  - `storeFluidFromContainer` 成功後保存�?  - `placeFluid` 成功扣流體後保存�?- 維度清理�?  - `sanitizeSessionDimension` 清掉失效綁定時保存清理後狀態�?
[結果]
- 玩家斷線重連後�?  - 之前綁定過的 storage 位置會恢復�?  - RTS 內部流體池（每種流體 amount）會恢復�?
[驗證]
- `.\gradlew.bat compileJava -x test`
- 結果：BUILD SUCCESSFUL（有 deprecation 提示，無錯誤）�?
2026-03-04 - Ray payload sync for Item#use (fish bucket right-click fix)

[問題]
- RTS client 的右鍵命中是自定射線，但 server 只有 clickedPos/face/hitVec�?- 遇到 `Item#use()` 內部再次 raytrace（魚�?桶類）時，server 視角�?client RTS 游標不一致，導致交互失效�?
[修改目標]
- C2S 補齊 ray 資訊（`rayOrigin + rayDir`）�?- server �?`useItem` 路徑用該 ray 重建臨時交互上下文�?- 保持 `useItemOn` 主路徑不被破壞，降低對剪刀等交互回歸風險�?
[修改檔案]
- `src/main/java/com/rtsbuilding/rtsbuilding/network/C2SRtsPlacePayload.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/network/C2SRtsInteractPayload.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/network/C2SRtsPlaceFluidPayload.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/network/RtsNetworkHandlers.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/client/ClientRtsController.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java`

[步驟 1 - Payload 擴展]
- `C2SRtsPlacePayload` 新增�?  - `rayOriginX/Y/Z`
  - `rayDirX/Y/Z`
- `C2SRtsInteractPayload` 新增同樣 6 個欄位�?- `C2SRtsPlaceFluidPayload` 也補同樣 6 個欄位，保持協議一致性�?
[步驟 2 - Client 發送側]
- `BuilderScreen` �?`InteractionTarget` record 新增�?  - `rayOrigin`
  - `rayDir`
- `pickInteractionTarget` 構建 target 時寫入當�?RTS 游標 ray�?- 普通右鍵路徑與 wheel 路徑，所�?place/interact 呼叫都改�?ray�?- `ClientRtsController` 對應方法簽名改為接收 ray 並寫�?payload�?  - `placeSelected`
  - `placeSelectedFluid`
  - `interactEmpty`
  - `interactBlockWithToolSlot`
  - `interactBlockWithPinnedItem`
  - `interactEntityWithToolSlot`
  - `interactEntityWithPinnedItem`

[步驟 3 - Server 接收/執行側]
- `RtsNetworkHandlers` �?ray 欄位傳給�?  - `RtsStorageManager.placeSelected(...)`
  - `RtsStorageManager.interactTarget(...)`
  - `RtsStorageManager.placeFluid(...)`
- `RtsStorageManager` 新增�?  - `parseRayContext(...)`
  - `withTemporaryUseItemContext(...)`
  - `withTemporaryInteractionRay(...)`
- 執行策略�?  - `useItemOn(...)` 維持既有目標近距上下文（保護原有 on-block/on-entity 交互）�?  - `useItem(...)` fallback 改為使用 ray-aligned 上下文（修正魚桶類內�?raytrace）�?
[兼容性預期]
- 改善魚桶/桶類右鍵交互一致性�?- 盡量不影響剪刀等依�?`useOn` 的交互�?
[驗證]
- `.\gradlew.bat compileJava -x test`
- 結果：BUILD SUCCESSFUL�?
2026-03-04 - Rollback: Revert ray-context payload patch (restore previous RTS fluid behavior)

[reason]
- Regression after ray-context sync patch:
  - RTS fluid placement failed (cannot pour water).
  - Previously working fluid ingest/place behavior became unstable.

[rollback scope]
- Fully reverted ray-context payload/context chain added in fish-bucket fix attempt.
- Restored previous stable interaction contract based on clickedPos/face/hitVec + temporary interaction position.

[network rollback]
- Reverted payload schema:
  - C2SRtsPlacePayload: removed rayOriginX/Y/Z + rayDirX/Y/Z.
  - C2SRtsInteractPayload: removed rayOriginX/Y/Z + rayDirX/Y/Z.
  - C2SRtsPlaceFluidPayload: removed rayOriginX/Y/Z + rayDirX/Y/Z.
- Updated RtsNetworkHandlers to stop forwarding ray fields.

[client rollback]
- Restored ClientRtsController method signatures (removed ray args):
  - placeSelected
  - placeSelectedFluid
  - interactEmpty
  - interactBlockWithToolSlot
  - interactBlockWithPinnedItem
  - interactEntityWithToolSlot
  - interactEntityWithPinnedItem
- Restored BuilderScreen interaction target model:
  - removed rayOrigin/rayDir from InteractionTarget.
  - removed all ray forwarding in normal RMB path and interaction wheel path.

[server rollback]
- Restored RtsStorageManager entrypoint signatures:
  - placeSelected(..., itemId)
  - placeFluid(..., fluidId)
  - interactTarget(..., itemId)
- Removed ray-specific runtime helpers:
  - parseRayContext
  - withTemporaryUseItemContext
  - withTemporaryInteractionRay
  - RayContext record
- Restored fallback useItem context to withTemporaryInteractionPosition only.

[changed files]
- src/main/java/com/rtsbuilding/rtsbuilding/network/C2SRtsPlacePayload.java
- src/main/java/com/rtsbuilding/rtsbuilding/network/C2SRtsInteractPayload.java
- src/main/java/com/rtsbuilding/rtsbuilding/network/C2SRtsPlaceFluidPayload.java
- src/main/java/com/rtsbuilding/rtsbuilding/network/RtsNetworkHandlers.java
- src/main/java/com/rtsbuilding/rtsbuilding/client/ClientRtsController.java
- src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java
- archetecture.txt
- Journal.txt

[validation]
- Command: .\gradlew.bat compileJava -x test
- Result: BUILD SUCCESSFUL

2026-03-04 - Remote POV Context v2（真玩家視角上下文重建）

[目標]
- 不使�?fake player�?- �?server �?`Item#use()/Item#useOn` �?POV �?RTS 自定游標 ray 一致�?- 保持 NeoForge 互動鏈：`useItemOn -> consumesAction 判斷 -> useItem`�?
[核心方案]
- C2S payload 重新補齊：`rayOrigin + rayDir`�?- server 端以 hitVec + rayDir 重建「虛擬眼睛位置」：
  - `virtualEye = hitVec - normalize(rayDir) * (reach - epsilon)`
  - `virtualFeet = virtualEye - eyeHeight`
  - 暫時把真玩家移到 virtualFeet 並朝�?hitVec 後執行原版互動�?- 完成互動後恢復玩家原始位置與朝向�?
[涉及檔案]
- `src/main/java/com/rtsbuilding/rtsbuilding/network/C2SRtsPlacePayload.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/network/C2SRtsInteractPayload.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/network/C2SRtsPlaceFluidPayload.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/network/RtsNetworkHandlers.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/client/ClientRtsController.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java`
- `src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java`

[步驟 1 - 協議恢復 ray]
- 三�?payload 重新加入欄位�?  - `rayOriginX/Y/Z`
  - `rayDirX/Y/Z`
- StreamCodec 讀寫同步補齊�?- `RtsNetworkHandlers` 轉發 ray 參數�?`RtsStorageManager`�?
[步驟 2 - Client 傳遞鏈]
- `BuilderScreen#pickInteractionTarget` 攜帶 `camPos + rayDir`�?- 正常右鍵、互動輪、fluid 放置全路徑傳�?ray�?- `ClientRtsController` 互動方法簽名恢復 ray 參數版本�?
[步驟 3 - Server Remote POV Context]
- `RtsStorageManager` 新增�?  - `parseRayContext(...)`
  - `withTemporaryUseItemContext(...)`
- 實作策略�?  - �?ray 無效 -> 回退舊邏輯（`withTemporaryInteractionPosition`）�?  - �?ray 有效 -> �?virtualEye 公式重建 POV，再執行互動�?
[步驟 4 - 套用範圍]
- `placeSelected`�?  - 主手分支 `useItemOn` �?fallback `useItem` 都放�?Remote POV Context�?  - linked-item 分支同樣套用�?- `interactTarget`�?  - tool-slot �?pin-item �?block 互動鏈都改為同一 Remote POV Context�?  - 仍保�?`useItemOn -> useItem` 並用 `consumesAction()` 控制 fallback�?
[兼容性預期]
- 對桶/魚桶/內部�?raytrace 的道具：server POV �?RTS 游標一致性更高�?- 對一�?useOn 道具：維持原順序與真玩家語義，不引入 fake-player 兼容問題�?
[驗證]
- `./gradlew.bat compileJava -x test`
- 結果：`BUILD SUCCESSFUL`

2026-03-04 - Yellow frame can lock fluid sources + top toggle

[request]
- Yellow frame lock method could not lock fluids.
- Add top toggle to enable/disable fluid target lock.

[implemented]
1) Controller state
- Added in `ClientRtsController`:
  - `lockFluidTarget`
  - `isFluidTargetLockEnabled()`
  - `toggleFluidTargetLock()`

2) Top UI toggle
- Added top-bar button in `BuilderScreen`:
  - `LOCK FLUID: ON/OFF`
- Added click handling in `handleTopBarClick(...)`.
- Added status-row text showing current fluid-lock state.

3) Yellow frame targeting
- Updated `RtsBoundaryRenderer` block raycast:
  - supports `includeFluidSource` argument.
  - uses `ClipContext.Fluid.SOURCE_ONLY` when fluid-lock is enabled.
- Result: yellow frame can now lock and outline fluid source targets.

4) Interaction target consistency
- Updated `BuilderScreen#pickInteractionTarget(...)` clip-mode decision:
  - fluid source enabled when `includeFluidSource || controller.isFluidTargetLockEnabled()`.
- Ensures interaction target picking and yellow frame lock follow same toggle policy.

[files]
- src/main/java/com/rtsbuilding/rtsbuilding/client/ClientRtsController.java
- src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- src/main/java/com/rtsbuilding/rtsbuilding/client/RtsBoundaryRenderer.java
- architecture.txt
- archetecture.txt
- Journal.txt

[validation]
- .\gradlew.bat compileJava -x test
- BUILD SUCCESSFUL (warnings only)

2026-03-04 - Extend toggle: lock air block above solid block (for pouring-style use)

[issue]
- Pour-water style interaction needs air target.
- Current lock hit remained on solid block and could miss expected behavior.

[change]
1) Toggle meaning expanded
- Existing top toggle is now `FLUID/AIR LOCK: ON/OFF`.
- Same toggle controls:
  - fluid source lock
  - air-above-solid lock

2) Interaction target remap (BuilderScreen)
- In lock mode ON, when hit is solid block and block above is air:
  - remap target to the above air block.
  - synthetic hit uses `Direction.UP` + center of above block.

3) Yellow frame remap (RtsBoundaryRenderer)
- Same remap rule applied before outline render.
- If remapped target is air, render full yellow cube outline at that air cell.

[effect]
- User can lock the air cell above a solid block and perform pouring/air-use style actions more reliably.
- Visual lock and actual interaction targeting remain aligned.

[files]
- src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- src/main/java/com/rtsbuilding/rtsbuilding/client/RtsBoundaryRenderer.java
- architecture.txt
- archetecture.txt
- Journal.txt

[validation]
- .\gradlew.bat compileJava -x test
- BUILD SUCCESSFUL

2026-03-04 - Fluid placement distance/offset fix (server path)

[issue]
- Fluid placement looked distance-constrained and mostly worked near feet.
- In non-air lock, aiming highlighted cell could place into side-neighbor cells.
- In air lock mode, placement frequently failed.

[server adjustments]
1) Access check split for fluid placement
- `placeFluid(...)` no longer uses generic `canAccessWorldTarget(...)`.
- switched to new `canAccessFluidPlacementTarget(...)`:
  - active RTS + chunk loaded
  - pass if `level.mayInteract(player, pos)`
  - or pass if `RtsCameraManager.isWithinActionRadius(player, pos)`
  - if target cell is air, fallback-check `pos.below()` using same rule set

2) Placement-position candidate reordering
- `resolveFluidPlacementPos(...)` now:
  - first tries clicked cell
  - if side-face hit, prefers `clicked.above()`
  - then tries face-adjacent cell
  - then final fallback to `clicked.above()`
- purpose: reduce lateral offset from side-face hits in RTS camera perspective

[result]
- Improved remote fluid target acceptance within RTS radius.
- Improved consistency against front/back/left/right misplacement in non-air mode.
- Better compatibility for air-target interactions through below-cell support check.

[files]
- src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java
- architecture.txt
- archetecture.txt
- Journal.txt

[validation]
- .\gradlew.bat compileJava -x test
- BUILD SUCCESSFUL

2026-03-04 - 修正虛擬玩家朝向：由眼睛位置計算，並嚴格�?rayDir

[定位]
- 你指出「虛擬玩家看向方向可能不對」是關鍵，這次直接修那層�?- 核心問題：臨時互動朝向之前是用腳底位置計�?yaw/pitch，會導致俯仰與落點偏移�?
[修改]
1) `withTemporaryInteractionPosition(...)`
- 朝向計算從：`yawPitchTo(feetPos, lookAt)`
- 改為：`yawPitchTo(eyePos, lookAt)`
- 其中 `eyePos = feetPos + eyeHeight`

2) `withTemporaryUseItemContext(...)`
- 保留 `virtualEye = hit - dir * (reach - eps)`
- �?`lookAt` 改為 `virtualEye + normalize(rayDir) * reach`
- 不再直接�?`hitVec` 當視線終點，避免 hit 點誤差帶來偏�?
[預期改善]
- 減少「只能腳底附近成功」的表現�?- 減少「對著黃框卻落在鄰格」的偏移�?- `useItem/useItemOn` 的內�?raytrace �?RTS cursor 更一致�?
[檔案]
- src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java
- architecture.txt
- archetecture.txt
- Journal.txt

[驗證]
- .\gradlew.bat compileJava -x test
- BUILD SUCCESSFUL

2026-03-04 - 挖掘實裝：以鼠標綁定工具為唯一計算來源 + 取消速度懲罰

[需求]
- 挖掘時必須按照目前綁定在鼠標的工具計算挖掘等級與速度�?- 取消挖掘速度懲罰�?
[核心修改]
1) 挖掘速度計算改為只看綁定工具�?- 檔案：`RtsStorageManager#computeRemoteDestroyStep(...)`
- 移除「carried 游標物品覆蓋工具」邏輯�?- 改為固定�?`toolSlot`，透過 `withTemporarySelectedSlot(...)` 計算 `state.getDestroyProgress(...)`�?
2) 真正破壞方塊也只看綁定工具槽
- 檔案：`RtsStorageManager#destroyMinedBlock(...)`
- 移除 `destroyBlockWithCarriedTool(...)` 分支�?- 破壞行為改為�?`withTemporarySelectedSlot(...)` 內呼�?`gameMode.destroyBlock(pos)`�?
3) 速度懲罰取消
- 新增 `removeMiningSpeedPenalty(...)`�?  - 在「眼睛在水中且無水下呼吸附魔」時，補償水下速度懲罰（乘�?5x）�?- 保留原本 `withTemporaryOnGround(player, true, ...)`�?  - 持續取消空中挖掘懲罰�?
[結果]
- 挖掘等級/速度�?RTS 綁定工具槽唯一決定�?- 游標攜帶物品不再干擾挖掘速度與等級�?- 環境速度懲罰（至少空�?水下）被取消�?
[修改檔案]
- src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java
- architecture.txt
- archetecture.txt
- Journal.txt

[驗證狀態]
- 目前 shell �?Java 執行環境出現內存/啟動不穩（OutOfMemory / 啟動失敗），
  本次改動後無法完�?compile 驗證�?- �?Java 環境恢復後請重跑：`.\gradlew.bat compileJava -x test`

2026-03-04 - Fix compile break + 1.21.1-correct underwater mining compensation

[issue]
- compileJava failed: EnchantmentHelper.hasAquaAffinity(ServerPlayer) not found.
- Root cause: API drift in MC/NeoForge 1.21.1 (enchantment effect flow moved to attributes).

[what I checked]
- Read local neoform EnchantmentHelper.java and Player.java from Gradle cache sources jar.
- Confirmed hasAquaAffinity helper is absent.
- Confirmed underwater dig speed uses Attributes.SUBMERGED_MINING_SPEED.

[code changes]
1) RtsStorageManager imports
- remove EnchantmentHelper import
- add Attributes import

2) removeMiningSpeedPenalty(...)
- before: eye in water + !hasAquaAffinity => step * 5
- after: eye in water + submergedMiningSpeed in (0,1) => step * (1/submergedMiningSpeed)

[why this is safer]
- No blacklist/whitelist and no enchantment hardcoding path.
- Works with vanilla Aqua Affinity and modded attribute modifiers.
- Uses runtime attribute state as single source of truth.

[build]
- .\gradlew.bat --no-daemon "-Dorg.gradle.jvmargs=-Xmx768m" compileJava -x test
- BUILD SUCCESSFUL

2026-03-04 - UI調整：分類樹、搜索清除按鈕、Tool可Pin、底部兩行保�?
[你提出的問題]
1) 底部UI在小尺度下常只有1行物品格，體感上接近�?行但被卡住�?2) Category字體/行感偏擠，且hover有一條不自然的線�?3) 希望分類先按模組，再可展開看子分類（創造模式分類）�?4) 需要「取消搜�?x」按鈕，快速解除搜索焦點並繼續選物品�?5) Tool欄物品也要能被pin，並避免Tool與Storage同時被視覺上“同時選中”�?
[實作摘要]
A. Category模型升級（client/server一致）
- 類別token改為�?  - all
  - mod|<namespace>
  - tab|<namespace>|<creative_tab_key>
- 保留向後兼容：舊的純namespace會自動轉成mod|namespace�?
B. Server端分�?過濾
- requestPage中加入Creative tab內容重建�?  `CreativeModeTabs.tryRebuildTabContents(...)`
- 新增 item -> creative tab cache，使�?`CreativeModeTab.contains(new ItemStack(item))` 判定�?- 分類輸出改為樹狀token序列（mod後接tab子項）�?- 過濾邏輯�?  - mod選中：過濾命名空間（物品+流體�?  - tab選中：過濾該mod下屬於該創造tab的物品；流體在tab模式不顯�?
C. Client端Category UI
- Category面板改為可展�?收合的樹狀列表�?- 每個mod行右側增加展開鍵�?- 移除原本那條hover水平線�?- 調整行內文字位置與密度，避免字體顯得“壓UI”�?
D. 搜索清除按鈕
- 搜索欄右側新�?`x` 按鈕�?- 點擊後：清空搜索、同步controller搜索、取消焦點�?
E. Tool/Pin與選取視�?- 新增從Tool直接pin能力�?  - hover pin + A，若沒有選中的Storage/Pin物品，會改用當前/hovered工具槽物品pin到該pin位�?- 當已選中Storage物品或Fluid時，Tool選中高亮會隱去，降低“同時選取”混淆�?
F. 底部面板高度保底
- 新增“可行時至少2行物品格”的最小高度計算，避免在邊界尺寸下被壓�?行�?
G. 網路payload容量調整
- category/categoriess[] 的UTF長度限制�?4提升�?28，避免樹狀token溢出�?
[修改檔案]
- src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- src/main/java/com/rtsbuilding/rtsbuilding/client/ClientRtsController.java
- src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java
- src/main/java/com/rtsbuilding/rtsbuilding/network/C2SRtsRequestStoragePagePayload.java
- src/main/java/com/rtsbuilding/rtsbuilding/network/S2CRtsStoragePagePayload.java
- architecture.txt
- archetecture.txt
- Journal.txt

[編譯驗證]
- .\gradlew.bat --no-daemon "-Dorg.gradle.jvmargs=-Xmx768m" compileJava -x test
- BUILD SUCCESSFUL

2026-03-04 - 需求收尾：Category字更�?+ 刪除FLUID/AIR LOCK + 退出重進持久化保證

[本次目標]
1) Category 字體再小一點�?2) 刪除 FLUID/AIR LOCK（不再需要）�?3) 確保 linked 箱子�?RTS fluid 退出遊戲後再進仍保持一致�?
[實作細節]
A. Category 字體縮小
- BuilderScreen 的分類行文字改用縮放繪製：`drawScaledText(...)`�?- 當前 scale = 0.80�?- 同步修正分類文字截斷寬度計算（依縮放後可用寬度截斷）�?
B. 移除 FLUID/AIR LOCK 功能路徑
1) UI �?- 移除頂欄按鈕：`FLUID/AIR LOCK: ON/OFF`�?- 移除狀態列�?lock 開關文字�?- 移除頂欄點擊切換 lock 的邏輯�?
2) 互動/高亮�?- BuilderScreen 不再使用 lock 狀態去改寫 interaction target�?- 刪除 BuilderScreen �?`maybeLockAirAboveSolidBlock(...)`�?- RtsBoundaryRenderer 的黃框高亮不再做 lock 驅動�?air-above-solid 重映射�?
3) 控制器層
- 刪除 ClientRtsController �?lock 相關 state/API�?  - `lockFluidTarget`
  - `isFluidTargetLockEnabled()`
  - `toggleFluidTargetLock()`

C. 持久化加固（linked + internal fluid�?- 原有 NBT 序列化仍保留�?  - root: `rtsbuilding_storage_session`
  - linked: `linked_positions`, `linked_dimension`
  - fluid: `internal_fluids`
- 本次新增兩個保存時機：
  1) `onRtsDisabled(...)` 立即 `saveSessionToPlayerNbt(...)`�?  2) `placeFluid(...)` 在「向方塊流體容器成功注入並消耗流體」分支也立即保存�?
[對你需求的直接結果]
- Category 字體更小、可讀性更符合面板密度�?- FLUID/AIR LOCK 已從 UI 與主互動路徑移除�?- linked 的箱子綁定與 RTS fluid 內部存量，在正常退出重進後可保持一致；
  同時保存時機比之前更積極，降低中途狀態丟失風險�?
[修改檔案]
- src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- src/main/java/com/rtsbuilding/rtsbuilding/client/RtsBoundaryRenderer.java
- src/main/java/com/rtsbuilding/rtsbuilding/client/ClientRtsController.java
- src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java
- architecture.txt
- archetecture.txt
- Journal.txt

[驗證]
- .\gradlew.bat --no-daemon "-Dorg.gradle.jvmargs=-Xmx768m" compileJava -x test
- BUILD SUCCESSFUL

2026-03-04 - 微調：Category 文字置中 + 字體略放�?
[你要求]
- Category 字沒有居中�?- 希望稍微大一點，但仍比之前小�?
[實作]
- BuilderScreen:
  1) 新增 `CATEGORY_TEXT_SCALE = 0.88F`（由 0.80 提升）�?  2) Category 標題改成面板水平置中�?  3) Category 每行文字改為�?     - 在可用區域內水平置中（考慮縮排與展開按鈕占位）�?     - 按縮放後字高做垂直居中�?     - 截斷寬度按縮放比例換算，避免過早省略�?
[結果]
- Category 標題與行內文字視覺居中�?- 字體比上一版略大、但仍小於原始大小�?
[驗證]
- .\gradlew.bat --no-daemon "-Dorg.gradle.jvmargs=-Xmx768m" compileJava -x test
- BUILD SUCCESSFUL

2026-03-04 - Build SHAPE selector + mouse-open shape wheel + first-pass multi-place shapes (1.21.1)

Scope
- Client build UX for RTS placement: top bar shape selector, radial shape wheel (mouse-open, no Alt required), and shape-based multi-place dispatch.
- Client state model: persistent per-session shape state in RTS controller.

Architecture changes
1) Build shape state channel (controller)
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/ClientRtsController.java
- Added enum: `BuildShape { BLOCK, LINE, SQUARE }`.
- Added state field: `buildShape` (default `BLOCK`).
- Added APIs:
  - `getBuildShape()`
  - `setBuildShape(BuildShape)`
  - `cycleBuildShape(int step)`
- Added reset on RTS mode transitions:
  - when RTS camera/session enables
  - when RTS camera/session disables
- Result: shape selection has a single authoritative client state and always resets to safe default when leaving/re-entering RTS.

2) Top bar SHAPE selector + no-key wheel entry
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Added top button width constant: `SHAPE_BUTTON_W`.
- Added top bar button: `SHAPE: <BLOCK|LINE|SQUARE>`.
- Added click routing in `handleTopBarClick(...)` to open shape wheel at cursor position.
- This gives a non-keyboard entry point (no Alt hold needed) to pick build mode quickly.

3) Shape wheel interaction model
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Added wheel state:
  - `shapeWheelOpen`
  - `shapeWheelCenterX`, `shapeWheelCenterY`
- Added wheel layout constants:
  - `SHAPE_WHEEL_RADIUS`
  - `SHAPE_WHEEL_SLOT`
- Added methods:
  - `openShapeWheel(...)`, `closeShapeWheel()`
  - `collectShapeWheelSlots()`
  - `resolveShapeWheelOption(...)`
  - `renderShapeWheel(...)`
  - label helpers: `shapeLabel(...)`, `shapeShortLabel(...)`
- Input routing while wheel open:
  - LMB: select hovered shape and close
  - RMB/MMB: cancel and close
  - Mouse wheel: cycle shapes
  - Keyboard `1/2/3`: pick BLOCK/LINE/SQUARE
  - `Esc`: close
- Mutual exclusion:
  - shape wheel suppresses normal world placement and interaction wheel handling while open.

4) Placement dispatch upgraded to shape-aware multi-place
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Added `placeSelectedWithShape(...)` for selected item placement path.
- Added `buildShapePlacementHits(...)` to expand one base hit into multiple target hits.
- First-pass shapes:
  - `BLOCK`: single target (old behavior)
  - `LINE`: +/- 2 blocks from center (5 total)
  - `SQUARE`: 3x3 on face plane (9 total)
- Axis resolution:
  - `resolveLineAxis(face, rayDir)` chooses a stable axis on target face plane using ray direction magnitudes.
  - `resolveSquareAxes(face)` returns two orthogonal face-plane axes.
- Multi-place dispatch uses existing `controller.placeSelected(...)` for each generated target, preserving server-side interaction rules and mod compatibility path.

5) UX updates around wheel/shape state
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Cursor preview suppressed while shape wheel is open.
- Guide text updated to include SHAPE wheel usage.
- Top status line now includes current shape for visibility.

Tech-stack / compatibility impact
- No protocol payload changes required for this feature.
- No server placement pipeline changes required.
- Implemented as a pure client orchestration layer over existing RTS place payload path, reducing regression risk in multi-mod environments.

Files changed
- src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- src/main/java/com/rtsbuilding/rtsbuilding/client/ClientRtsController.java
- architecture.txt
- archetecture.txt
- Journal.txt

Validation
- Command: .\gradlew.bat --no-daemon "-Dorg.gradle.jvmargs=-Xmx768m" compileJava -x test
- Result: BUILD SUCCESSFUL (2026-03-04)
2026-03-04 - SHAPE v2: two-point parameterized builders (max 32) for LINE/SQUARE/CIRCLE/WALL/BOX

Scope
- Upgraded RTS shape placement from fixed templates to two-step point-A/point-B parameterized generation.
- Expanded shape set and added per-parameter clamping rules.
- Kept server/protocol untouched (client expands to multiple existing place payloads).

1) Shape model expanded
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/ClientRtsController.java
- `BuildShape` enum extended:
  - BLOCK
  - LINE
  - SQUARE
  - CIRCLE
  - WALL
  - BOX
- Existing shape selection APIs (`get/set/cycle`) continue to work with larger enum set.

2) Two-click head/tail placement flow
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Added pending anchor state:
  - `pendingShapeAnchor` (`ShapeAnchor {pos, face, shape}`)
- Behavior for selected storage item placement:
  - BLOCK: immediate single placement (old path)
  - Non-BLOCK:
    - first RMB sets point A only
    - second RMB sets point B and executes batch placement
- Pending anchor is cleared when:
  - shape changes (wheel pick / wheel scroll / 1..6 key)
  - mode switches
  - fluid/tool interaction path is used
  - screen closes

3) Parameterized shape builders (all client-side)
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Replaced fixed-radius generator with shape-specific builders:
  - `addLineTargets(...)`
    - 3D endpoint line from A->B
    - max line length parameter capped by endpoint scaling to 32 blocks (offset <= 31)
  - `addSquareTargets(...)`
    - plane rectangle on hit-face plane using two orthogonal axes
    - length/width each capped to 32
  - `addCircleTargets(...)`
    - filled circle (disk) on hit-face plane
    - radius from A->B projected distance, capped to 32
  - `addWallTargets(...)`
    - vertical/horizontal wall rectangle (length + height/width)
    - dimensions capped to 32
  - `addBoxTargets(...)`
    - filled cuboid A->B in XYZ
    - each axis dimension capped to 32
- New shared helpers:
  - `clampShapeOffset(...)`, `dotDelta(...)`, `offsetPos(...)`
  - `createShapePlacementHit(...)`

4) UI + interaction updates
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Top status row now includes pending shape anchor state text:
  - `A: unset (RMB sets point A)` / `A: x,y,z (RMB sets point B)`
- Shape wheel now includes 6 shapes:
  - BLOCK / LINE / SQUARE / CIRCLE / WALL / BOX
- Shape wheel hotkeys extended:
  - keys `1..6`
- Guide text updated to describe two-click build flow and max-32 cap.

5) Tech-stack / architecture impact
- No new network packets and no server-side shape protocol.
- Client remains orchestration layer that emits multiple existing `placeSelected` payloads.
- Multi-mod interaction path is preserved because each generated target still uses existing server placement logic.

Files changed
- src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- src/main/java/com/rtsbuilding/rtsbuilding/client/ClientRtsController.java
- architecture.txt
- archetecture.txt
- Journal.txt

Validation
- Command: .\gradlew.bat --no-daemon "-Dorg.gradle.jvmargs=-Xmx768m" compileJava -x test
- Result: BUILD SUCCESSFUL (2026-03-04)
2026-03-04 - SHAPE v3: multi-stage point workflow + confirm, translucent ghost preview, fill/rotate controls

Scope
- Upgraded non-BLOCK shape flow from immediate/2-step placement to staged parameter capture with explicit final confirm.
- Added in-world translucent ghost blocks for realtime preview during stage selection.
- Added top-bar shape controls: Fill toggle and Rotate step button.

1) Build workflow protocol (client-side state machine)
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Replaced previous single anchor model with staged session model:
  - `ShapeBuildPhase`: `NEED_SECOND_POINT`, `NEED_THIRD_POINT`, `READY_CONFIRM`
  - `ShapeBuildSession`: stores shape, face, pointA, pointB, boxHeight and mouse baseline.
- Placement flow now:
  - `BLOCK`: single right-click immediate place (unchanged)
  - 2D shapes (`LINE`, `SQUARE`, `CIRCLE`, `WALL`):
    1. RMB set point A
    2. RMB set point B
    3. RMB confirm batch placement
  - 3D shape (`BOX`):
    1. RMB set point A
    2. RMB set base point B (length/width)
    3. Move mouse up/down for live height preview, RMB lock height
    4. RMB confirm batch placement
- Added early confirm path in right-click flow:
  - `tryConfirmPendingShapeBuild(...)` runs before target interaction resolution.

2) Shape parameterization and limits
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Kept max limit policy: dimension/radius max 32 (`SHAPE_MAX_DIMENSION`, `SHAPE_MAX_RADIUS`).
- New shape input records:
  - `ShapeBuildInput` (resolved current preview/confirm parameters)
  - `RotatedOffset` (2D rotated integer offsets)
- Updated target generators:
  - `LINE`: clamped endpoint line interpolation
  - `SQUARE`/`WALL`: plane rectangle generation with optional rotation + hollow/filled modes
  - `CIRCLE`: radius-based disk/ring generation
  - `BOX`: 3D cuboid with rotated base footprint and height stage

3) BOX height realtime behavior (mouse vertical control)
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Added `BOX_HEIGHT_MOUSE_SCALE` and `resolveBoxHeightFromMouse(...)`.
- Height uses screen-mouse Y delta relative to point-B click baseline.
- During `NEED_THIRD_POINT` phase, preview updates continuously with mouse movement.

4) Top bar controls near SHAPE
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Added buttons adjacent to SHAPE:
  - `FILL: ON/OFF` (`shapeFillEnabled`)
  - `ROT: <deg>` (`shapeRotateDegrees`, +15deg per click)
- Added status text exposure in row2:
  - current fill state
  - rotation angle
  - staged prompt (`pendingShapeStatusText()`) showing current step / confirm readiness.

5) In-world translucent ghost preview rendering
- Files:
  - src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
  - src/main/java/com/rtsbuilding/rtsbuilding/client/RtsBoundaryRenderer.java
- Added public preview snapshot API from BuilderScreen:
  - `ShapeGhostPreview getShapeGhostPreview()`
- Renderer integration:
  - `RtsBoundaryRenderer` now reads preview snapshot when BuilderScreen is open.
  - Renders translucent ghost cubes via `RenderType.debugFilledBox` + `LevelRenderer.addChainedFilledBoxVertices(...)`.
  - Renders outline on each ghost block via existing line buffer.
  - Color changes when phase is `READY_CONFIRM` (clear visual ready state).

6) UX text updates
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Guide panel updated to reflect:
  - Fill/Rotate controls
  - 2D/3D staged point capture
  - explicit RMB confirm step
  - BOX mouse-up/down height stage

7) Stability note
- During editing, `BuilderScreen.java` was accidentally re-encoded with UTF-8 BOM by shell replacement.
- Fixed by rewriting with UTF-8 (no BOM) before final compile.

Files changed
- src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- src/main/java/com/rtsbuilding/rtsbuilding/client/RtsBoundaryRenderer.java
- architecture.txt
- archetecture.txt
- Journal.txt

Validation
- Command: .\gradlew.bat --no-daemon "-Dorg.gradle.jvmargs=-Xmx768m" compileJava -x test
- Result: BUILD SUCCESSFUL (2026-03-04)
2026-03-04 - Effortless-style SHAPE workflow: ALT-hold radial selection, Cube fill attributes, staged commits, Ctrl+Z/Y history

Scope
- Refined non-block build UX to follow Effortless-like interaction phases:
  1) ALT hold -> radial shape tool context
  2) contextual fill attribute setup (Fill/Hollow/Skeleton)
  3) staged point-based anchoring/extrusion with explicit commit
  4) rollback/redo shortcuts via local history stack.

1) Phase 1: ALT tool activation + radial selection (release-to-select)
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Added lifecycle state:
  - `shapeWheelOpenedByAlt`
  - `altShapeMenuHeld`
- Added polling/lifecycle method:
  - `updateAltShapeWheelLifecycle()` (called from `tick()`)
- Behavior:
  - Hold ALT in world area -> opens shape radial immediately
  - Hover sector and release ALT -> current hovered sector is selected and wheel closes
- Input conflict handling:
  - right-drag camera rotation now suppressed while ALT is down
  - interaction wheel modifier changed to ALT+CTRL (avoids ALT-only conflict with shape radial)

2) Phase 2: contextual Cube property panel (Fill Attribute)
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Replaced boolean fill toggle with 3-state enum:
  - `ShapeFillMode { FILL, HOLLOW, SKELETON }`
- Added right-side contextual panel rendering and click handling:
  - `renderShapeContextPanel(...)`
  - `handleShapeContextPanelClick(...)`
- Panel offers direct selection for Fill/Hollow/Skeleton.
- Top bar remains with quick controls:
  - `FILL: <mode>`
  - `ROT: <deg>`

3) Phase 3: staged spatial commits (point -> plane -> volume -> confirm)
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Existing staged session model retained and aligned with requested commit flow:
  - non-block shapes require staged points before commit
  - final right-click `confirm` sends batch placement
- 2D shapes (`LINE/SQUARE/CIRCLE/WALL`):
  - click A -> click B -> right-click confirm
- 3D `BOX/CUBE`:
  - click A -> click B (base) -> mouse up/down live height -> click lock height -> right-click confirm
- `pendingShapeStatusText()` updated to reflect active phase prompts.

4) Fill semantics refined (including skeleton)
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- 2D rectangle-based shapes:
  - `FILL`: full area
  - `HOLLOW` / `SKELETON`: edge-only
- Circle:
  - `FILL`: disk
  - `HOLLOW` / `SKELETON`: ring
- Cube/Box:
  - `FILL`: full volume
  - `HOLLOW`: six-face shell (surface)
  - `SKELETON`: 12-edge frame
- Rotation remains active for generated plane offsets:
  - supports diagonal rectangles/walls/base footprints.

5) Undo/Redo (for safe exploration)
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Added local history stacks:
  - `shapeUndoStack`
  - `shapeRedoStack`
  - capped by `SHAPE_HISTORY_LIMIT`
- Added records:
  - `ShapeHistoryBatch(itemId, face, positions)`
- On batch confirm:
  - recorded via `recordShapeBatchForUndo(...)`
- Shortcuts:
  - `Ctrl+Z` -> `undoLastShapeBatch()` using `controller.breakPlaced(...)` in reverse order
  - `Ctrl+Y` -> `redoLastShapeBatch()` (requires currently selected item id matches recorded batch)

6) Overlay / context presence
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Added left helper panel + right attribute panel while non-block shape mode is active.
- Updated guide text to reflect:
  - ALT-hold radial selection
  - staged commit flow
  - Fill/Hollow/Skeleton
  - Ctrl+Z / Ctrl+Y
  - ALT+CTRL interaction-wheel fallback.

7) Shape ghost preview retained
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/RtsBoundaryRenderer.java
- Existing translucent ghost path remains active (`RenderType.debugFilledBox`) and now reflects new fill mode + staged parameters in real-time.

Files changed
- src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- src/main/java/com/rtsbuilding/rtsbuilding/client/RtsBoundaryRenderer.java
- architecture.txt
- archetecture.txt
- Journal.txt

Validation
- Command: .\gradlew.bat --no-daemon "-Dorg.gradle.jvmargs=-Xmx768m" compileJava -x test
- Result: BUILD SUCCESSFUL (2026-03-04)
2026-03-04 - Crash fix: Render buffer state bug in shape ghost overlay (IllegalStateException: Not building!)

Issue
- Client crashed on render thread with:
  - `java.lang.IllegalStateException: Not building!`
  - stack points to `LevelRenderer.renderLineBox(...)`
  - crash site: `RtsBoundaryRenderer.onRenderLevel(...)`.

Root cause
- In `onRenderLevel`, code requested two VertexConsumers up front:
  - `RenderType.lines()`
  - `RenderType.debugFilledBox()`
- Under BufferSource state switching, obtaining a second render type can end/switch active builder.
- The previously cached `lineBuffer` then became stale/non-building but was still used for line writes,
  producing the `Not building!` exception.

Fix strategy
- Stop holding mixed render-type consumers across state switches.
- Draw per render-type in grouped phases and reacquire consumer after switching type.

Code changes
1) `RtsBoundaryRenderer.onRenderLevel(...)`
- Removed eager `fillBuffer` acquisition.
- Kept initial line rendering with `lineBuffer` only.
- Changed ghost render call to pass `bufferSource` instead of pre-cached consumers.
- End-batch order set explicitly after rendering:
  - `endBatch(RenderType.lines())`
  - `endBatch(RenderType.debugFilledBox())`

2) `renderShapeGhostPreview(...)`
- Signature changed:
  - from `(Minecraft, PoseStack, VertexConsumer lineBuffer, VertexConsumer fillBuffer)`
  - to `(Minecraft, PoseStack, BufferSource bufferSource)`
- Rendering split into two passes:
  - pass A: acquire `debugFilledBox` consumer and render all translucent fills
  - pass B: reacquire `lines` consumer and render all outlines
- Prevents stale-consumer usage across RenderType changes.

Compatibility impact
- No protocol/server changes.
- Pure client render-path correction.
- Preserves existing ghost visual behavior (fill + outline + ready-confirm color scheme).

Files changed
- src/main/java/com/rtsbuilding/rtsbuilding/client/RtsBoundaryRenderer.java
- architecture.txt
- archetecture.txt
- Journal.txt

Validation
- Command: .\gradlew.bat --no-daemon "-Dorg.gradle.jvmargs=-Xmx768m" compileJava -x test
- Result: BUILD SUCCESSFUL (2026-03-04)
2026-03-04 - Shape UX refinement: side-only fill control, PageUp/PageDown height nudge, live x*y*z, center-based rotation

Scope
- Refined BuilderScreen shape UX per new requirement:
  - Fill mode no longer adjusted from top bar.
  - Fill handling moved to side context panel and made shape-dimension aware (1D/2D/3D).
  - 3D cube height supports PageUp/PageDown micro adjustment.
  - Live size text shown as `x*y*z`.
  - Rotation now pivots around build geometry center (not anchor/cursor-biased offset).

1) Top bar fill-control removal
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Removed top fill button rendering and click handler path.
- Top bar keeps SHAPE and ROT controls; fill state still displayed in status text.

2) Side panel as single fill-control source
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- `renderShapeContextPanel(...)` now drives fill-mode selection.
- Added shape-aware mode availability via `availableFillModes(shape)`:
  - LINE (1D): `FILL` only
  - SQUARE/CIRCLE/WALL (2D): `FILL`, `HOLLOW`
  - BOX/CUBE (3D): `FILL`, `HOLLOW`, `SKELETON`
- Added `ensureFillModeForShape(shape)` to auto-normalize mode when shape switches.
- `handleShapeContextPanelClick(...)` now iterates only allowed modes for the active shape.

3) 3D height micro-adjust (PageUp/PageDown)
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Added key handling in `keyPressed(...)`:
  - `PageUp` => +1 height step
  - `PageDown` => -1 height step
  - with Shift => x4 step
- Added `adjustBoxHeightNudge(int delta)` for BOX sessions in:
  - `NEED_THIRD_POINT`
  - `READY_CONFIRM`
- Height calculation now combines mouse extrusion + key nudge:
  - new `resolveCurrentBoxHeightOffset(session, mouseY)`

4) Live `x*y*z` display
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Added `currentShapeSizeText()` to compute current preview bounding size in block units.
- Side context panel now renders:
  - `Size: x*y*z` (real-time with cursor and phase changes).
- Added `shapeDimensionLabel(shape)` for panel context (1D/2D/3D).

5) Rotation pivot correction to geometry center
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Reworked plane rotation helper:
  - from `rotatePlaneOffset(a, b, degrees)`
  - to `rotatePlaneOffset(a, b, centerA, centerB, degrees)`
- Square/Wall rotated rectangle generation now computes center from current extent bounds.
- BOX footprint rotation now computes center from base min/max extents before rotation.
- Circle still rotates around its geometric center (0,0 local), preserving expected behavior.
- Result: rotation aligns with build-center pivot instead of anchor-biased rotation.

6) Prompt/guide text updates
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- Height-step prompt updated to include PageUp/PageDown.
- Guide updated to mention height fine-tuning and center-based rotation behavior.

Files changed
- src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- architecture.txt
- archetecture.txt
- Journal.txt

Validation
- Command: .\gradlew.bat --no-daemon "-Dorg.gradle.jvmargs=-Xmx768m" compileJava -x test
- Result: BUILD SUCCESSFUL (2026-03-04)`r`n`r`n2026-03-04 - Undo/Redo architecture extension: apply Ctrl+Z/Y beyond shape batches

Scope
- Extended placement history so Ctrl+Z/Ctrl+Y also works for non-shape single placement flows.
- Kept existing shape batch undo behavior and merged it into one replay path.

Behavior change
- Before:
  - Undo/redo stack recorded only shape-confirm batches.
  - Single block placement in BLOCK mode and tool-slot direct placement were not recorded.
- After:
  - Single placement in BLOCK mode (pin item source) is recorded.
  - Single placement from tool-slot direct place path is recorded.
  - Existing LINE/SQUARE/CIRCLE/BOX batch placement remains recorded.
  - Ctrl+Z/Ctrl+Y now operate on one unified placement history stack.

Implementation details
- File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
1) Input binding updates
- keyPressed():
  - Ctrl+Z now calls undoLastPlacementBatch().
  - Ctrl+Y now calls redoLastPlacementBatch().

2) New single-placement history capture
- mouseReleased() tool-slot branch:
  - after controller.placeSelected(...), calls recordSinglePlacementForUndo(..., TOOL_SLOT, ..., selectedToolSlot).
- placeSelectedWithShape() BLOCK branch:
  - after controller.placeSelected(...), calls recordSinglePlacementForUndo(..., PIN_ITEM, selectedItemId, -1).

3) Unified batch recording path
- Replaced shape-only recorder with generic recorder:
  - recordPlacementBatchForUndo(replayKind, itemId, toolSlot, face, positions)
  - recordSinglePlacementForUndo(...) delegates to the generic recorder with a 1-block list.
- tryConfirmPendingShapeBuild() now converts shape hits to block-pos list and records via generic path.

4) Replay context typing
- ShapeHistoryBatch now stores replay context fields:
  - replayKind (PIN_ITEM / TOOL_SLOT)
  - itemId
  - toolSlot
  - face
  - positions
- Added enum PlacementReplayKind { PIN_ITEM, TOOL_SLOT }.

5) Redo routing logic
- redoLastPlacementBatch():
  - PIN_ITEM history: requires current selected pinned item ID to match recorded itemId.
  - TOOL_SLOT history: clears pinned/fluid selection, restores recorded hotbar slot, then replays placements.
- Replay still uses controller.placeSelected(...) per block with recorded face.

Architecture impact
- Client-side interaction architecture now treats placement actions as source-typed replay events rather than shape-only history.
- No network payload schema change.
- No server-side behavior change.
- No data/NBT schema change.

Validation
- Command: .\\gradlew.bat --no-daemon "-Dorg.gradle.jvmargs=-Xmx768m" compileJava -x test
- Result: BUILD SUCCESSFUL (2026-03-04)

Files changed
- src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- architecture.txt
- archetecture.txt
- Journal.txt

2026-03-04 - Shape undo parity fix: all shapes Ctrl+Z now remove placed block reliably and refund materials

Problem observed
- Non-cube shape undo could miss real placed blocks in some click contexts.
- Root cause: undo history stored intended target block-pos, but actual placement can resolve to `target.relative(face)` (normal vanilla use-on behavior).
- When undo attempted wrong pos, `PlacedBlockTrackerData` check failed, so no remove and no material refund.

Design change
- Extend break packet/context with ray-facing info and fallback policy:
  - Added `face` and `allowAdjacentFallback` into `C2SRtsBreakPayload`.
- Server break path now supports deterministic 1-step adjacent fallback:
  - Try requested `pos` first.
  - If not tracked and fallback enabled, try `pos.relative(face)`.
  - Only then proceed to remove+refund path.

Client changes
1) `src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java`
- Undo loop now calls:
  - `controller.breakPlaced(pos, batch.face(), true)`
- Effect: shape undo includes directional context for every recorded placement batch.

2) `src/main/java/com/rtsbuilding/rtsbuilding/client/ClientRtsController.java`
- Added overload:
  - `breakPlaced(BlockPos pos, Direction face, boolean allowAdjacentFallback)`
- Existing single-arg method kept for compatibility and delegates with safe defaults.

Network changes
3) `src/main/java/com/rtsbuilding/rtsbuilding/network/C2SRtsBreakPayload.java`
- Record expanded from:
  - `(BlockPos pos)`
- To:
  - `(BlockPos pos, byte face, boolean allowAdjacentFallback)`
- Updated stream codec read/write accordingly.

4) `src/main/java/com/rtsbuilding/rtsbuilding/network/RtsNetworkHandlers.java`
- `handleBreak(...)` now decodes face and passes fallback flag:
  - `RtsStorageManager.breakPlaced(serverPlayer, payload.pos(), face, payload.allowAdjacentFallback())`

Server changes
5) `src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java`
- Signature changed:
  - `breakPlaced(ServerPlayer player, BlockPos pos, Direction face, boolean allowAdjacentFallback)`
- Added fallback resolution logic using tracker:
  - if `pos` not tracked and fallback enabled, attempt `pos.relative(face)`.
- All downstream remove/refund/unlink operations now use resolved `targetPos`.
- Internal mining fast-path updated to new signature with fallback disabled to preserve mining semantics.

Impact
- Ctrl+Z behavior for LINE/SQUARE/CIRCLE/BOX now aligns with cube expectation under mixed placement contexts.
- Material recovery path (`refundFromState` + linked storage insertion) now consistently triggers when undoing these shapes.
- No change to storage/NBT schema.

Validation
- Command: .\\gradlew.bat --no-daemon "-Dorg.gradle.jvmargs=-Xmx768m" compileJava -x test
- Result: BUILD SUCCESSFUL (2026-03-04)

Files changed
- src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- src/main/java/com/rtsbuilding/rtsbuilding/client/ClientRtsController.java
- src/main/java/com/rtsbuilding/rtsbuilding/network/C2SRtsBreakPayload.java
- src/main/java/com/rtsbuilding/rtsbuilding/network/RtsNetworkHandlers.java
- src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java
- architecture.txt
- archetecture.txt
- Journal.txt
2026-03-04 - Added detailed_project.txt (extreme-granularity consultant map)

Goal
- Provide a consultant-facing, function-level map so external code consultants can propose targeted fixes without re-scanning entire codebase.

New artifact
- Added file: detailed_project.txt
- Size: large, full-project inventory (40 Java files).

Content structure inside detailed_project.txt
1) Consultant-oriented architecture summary
- Runtime model (client orchestrator, server authority, packet bridge, persistence boundaries).
- Critical execution chains:
  - placement path
  - shape confirm batch path
  - undo/remove/refund path
  - fluid place path
  - mining path
  - server refresh -> client storage page path
- High-impact files for rapid root-cause analysis.
- Reading guide for consultants.

2) Full function inventory and usage map (high granularity)
- Enumerates method-like declarations across src/main/java.
- For each function:
  - declaration path + line
  - exact signature text
  - override marker
  - usage count in Java token scan
  - usage site list (file:line + snippet)
- Includes callback hint when usage count is zero but function is override-based.

Generation methodology
- Token + signature scanner generated from project source itself.
- Supports multiline method signatures (to reduce misses in large APIs like server manager methods).
- Produces call-like usage map by invocation token scan across all Java files.

Why this helps targeted proposals
- Consultants can jump directly from symptom area (e.g., undo/refund, fluid placement, mining) to exact function chain and all known call neighborhoods.
- Reduces onboarding time for external contributors and increases precision of patch scope estimation.

Notes/limits documented in file
- Usage map is token-based; overloaded/common names may include extra/noisy hits.
- Framework callback entries can legitimately have no direct Java call sites.

Files changed
- detailed_project.txt
- architecture.txt
- archetecture.txt
- Journal.txt
2026-03-04 - Rotation hole fix + shape material cost indicator in side panel

Issue summary
- During non-90-degree shape rotation, generated structures (especially SQUARE/BOX) showed visible holes/gaps.
- Root cause: previous generation used forward mapping (`rotatePlaneOffset` + integer rounding) from source grid to destination grid.
  - Forward rounded mapping causes many-to-one collisions and leaves unassigned destination cells.
  - This is expected aliasing for voxelized rotations when using only forward projection.

Solution strategy
- Switched key shape generation paths to inverse-coverage rasterization for rotated rectangles.
- For 3D BOX, re-derived HOLLOW/SKELETON from actual rotated voxel volume boundary rather than unrotated index-boundary checks.
- Added real-time right-panel material cost estimate (block count) based on current preview geometry.

Detailed code changes
File: src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java

1) UI: right side context panel adds cost line
- `renderShapeContextPanel(...)`
  - panel height increased: `panelH` from 110 -> 122.
  - new line rendered:
    - `Cost: <N> blocks`
  - retained existing size, ALT radial hint, undo/redo hint.
- `handleShapeContextPanelClick(...)`
  - panel hit box height synced to 122 to match visual panel.

2) New preview-cost method
- Added `currentShapeCostText()`:
  - resolves current shape input via `resolveCurrentShapeBuildInput(...)`
  - computes `buildShapePositions(...).size()`
  - returns string count for side-panel display.

3) Rotation hole fix for 2D rotated rectangles (used by SQUARE)
- `addRotatedPlaneRectangleTargets(...)` refactored:
  - old: iterate source rectangle -> forward rotate each cell -> rounded destination.
  - new: build rotated fill cells by inverse mapping, then emit either:
    - all cells (FILL)
    - boundary cells only (HOLLOW).
- Added `buildRotatedRectangleFillCells(minA,maxA,minB,maxB,degrees)`:
  - computes rotated bbox from 4 corners.
  - scans destination integer grid range.
  - accepts cell when inverse-rotated center lies inside source cell-bounds (with ��0.5 voxel margin).
- Added `isInverseRotatedInsideCellBounds(...)` for inverse inclusion test.
- Added `isPlaneBoundaryCell(...)` for hollow outline extraction by 4-neighbor absence.

4) Rotation hole fix for 3D BOX (footprint rotation + shell/skeleton)
- `addBoxTargets(...)` refactored:
  - old: unrotated x/z loops + forward rotated footprint + source-space `isBoxSurface/isBoxEdge` tests.
  - new:
    1. create rotated footprint via `buildRotatedRectangleFillCells(...)`.
    2. FILL mode: extrude full footprint across Y range.
    3. HOLLOW/SKELETON:
       - build full rotated volume set first.
       - classify boundary per voxel via axis-neighbor absence in world-grid:
         - xBoundary: east/west missing
         - yBoundary: above/below missing
         - zBoundary: north/south missing
       - HOLLOW keeps voxels with boundaryAxes >= 1.
       - SKELETON keeps voxels with boundaryAxes >= 2.
  - Effect: rotated cube/wall-like prisms no longer lose random cells from forward-mapping aliasing.

5) Data structure addition
- Added local helper record:
  - `PlaneCell(int a, int b)`
- Used as deduplicated integer-cell key for rotated 2D footprint sets.

6) Cleanup impact
- Previous rectangle/box boundary checks (`isRectangleEdge`, `isBoxSurface`, `isBoxEdge`) are no longer used in generation path after refactor.
- `rotatePlaneOffset(...)` remains used for circle generation path; no behavior change there.

Behavioral result
- Rotated SQUARE/BOX preview and confirm placement now produce dense voxel coverage without random holes from projection rounding.
- Right side panel now shows expected block consumption count for current shape preview state.

Validation
- Command: .\\gradlew.bat --no-daemon "-Dorg.gradle.jvmargs=-Xmx768m" compileJava -x test
- Result: BUILD SUCCESSFUL (2026-03-04)

Files changed
- src/main/java/com/rtsbuilding/rtsbuilding/client/BuilderScreen.java
- architecture.txt
- archetecture.txt
- Journal.txt
[2026-03-05] Funnel phase-1 implementation log
- Re-scoped work to funnel-only per user request; removed station/JEI/FTB in-progress protocol surfaces from active code path.
- Deleted non-funnel payload files introduced earlier:
  - network/C2SRtsLinkStationPayload.java
  - network/C2SRtsOpenStationPayload.java
  - network/C2SRtsJeiTransferPayload.java
  - network/C2SRtsQuestDetectPayload.java
  - network/RtsJeiTransferKind.java
- Restored storage-page contract from accidental station extension, then re-extended only for funnel UI projection.

Network changes
- Added C2S:
  - C2SRtsSetFunnelPayload(enabled)
  - C2SRtsFunnelTargetPayload(target)
- Registered handlers in RtsPayloadRegistrar.
- Added handlers in RtsNetworkHandlers:
  - handleSetFunnel -> RtsStorageManager.setFunnelEnabled
  - handleFunnelTarget -> RtsStorageManager.updateFunnelTarget
- Removed stray handler registrations/methods for station/JEI/quest.

Server runtime changes (RtsStorageManager)
- Added funnel constants:
  - radius, per-tick entity/item budget, buffer stack cap, tick interval.
- Session runtime fields:
  - funnelEnabled
  - funnelTarget
  - funnelTickCooldown
  - funnelBuffer (List<ItemStack>)
- Added mode transition rule:
  - leaving FUNNEL mode disables funnel and drains buffer.
- Added funnel API:
  - setFunnelEnabled(player, enabled)
  - updateFunnelTarget(player, target)
- Tick pipeline:
  - tickMining now also executes tickFunnel for each session.
- Funnel logic:
  - verify mode + linked + target + action radius + chunk access.
  - flush existing buffer to linked first each cycle.
  - absorb item entities around target one item at a time.
  - if linked full, spill to small funnel buffer.
  - if both linked and buffer full, stop absorbing (item stays in world).
- Insert preference improvement for funnel path:
  - insertToHandlerPreferExisting: same-item stacks first, empty slots later.
  - storeToLinkedOnlyPreferExisting uses above per handler.
- Safety drain:
  - on RTS disable/logout, buffer is drained to linked, then inventory/drop fallback to avoid loss.

Storage page projection
- S2CRtsStoragePagePayload now carries:
  - funnelEnabled
  - funnelBufferItemIds
  - funnelBufferCounts
- requestPage/sendEmptyPage updated accordingly.

Client controller changes
- Added funnel state/actions:
  - isFunnelEnabled
  - setFunnelEnabled/toggleFunnelEnabled
  - updateFunnelTarget with cooldown+target-change guard
- Added funnel buffer projection list + FunnelBufferEntry.
- applyStoragePage reads server funnel state and buffer summary.

Builder UI changes (BuilderScreen)
- Added top action: FUNNEL.
- Mode switching rules:
  - entering FUNNEL auto-enables funnel.
  - switching to INTERACT/LINK/ROTATE auto-disables funnel.
- In FUNNEL mode tick:
  - continuously sends picked block position as funnel target (throttled by controller).
- Disabled mining start while in FUNNEL mode.
- Added right-side funnel buffer panel:
  - show/hide toggle button
  - list of buffered item previews + counts
  - hover tooltip support
- On screen close, funnel disabled explicitly.

Compile validation
- Ran: .\gradlew.bat compileJava
- Result: SUCCESS

[2026-03-05] RTS craft terminal overlay fix + JEI fill gate update
- Goal
  - Fix issue where RTS craft terminal still appeared as vanilla crafting menu.
  - Remove JEI restriction that required `RtsCraftTerminalScreen` class, so RTS-linked fill also works on non-RTS crafting terminals while RTS mode is active.

- Root-cause notes
  - Client terminal replacement depended on `pendingCraftTerminalOpen` time window only.
  - If replacement timing missed that window, vanilla screen persisted.
  - JEI transfer handler hard-blocked non-`RtsCraftTerminalScreen` even when RTS was enabled and linked.

- Implemented changes
  - `client/ClientRtsController.java`
    - In `tick()`, changed replacement flow:
      - if current screen is vanilla `CraftingScreen` + `CraftingMenu` and should be RTS terminal, force replace with `RtsCraftTerminalScreen`.
      - fallback timer path remains for pending state countdown.
    - Added `shouldUseRtsCraftTerminalScreen(CraftingScreen)`:
      - returns true when `pendingCraftTerminalOpen` is true, OR
      - crafting screen title string equals `RTS Craft Terminal`.
  - `compat/jei/RtsCraftTerminalJeiTransferHandler.java`
    - Removed strict `instanceof RtsCraftTerminalScreen` requirement.
    - Updated condition to allow transfer when:
      - RTS is enabled
      - linked storage exists
      - any crafting screen is open
    - Updated user tooltip text to `Open any crafting menu first.`

- Validation
  - Ran `./gradlew.bat compileJava`.
  - Build result: SUCCESS.
  - No network payload/codec updates needed in this patch.

[2026-03-05] Follow-up root-cause fix: terminal auto-close + JEI transfer gate
- Symptom recap
  - RTS craft terminal still looked overridden/closed.
  - JEI showed `Open any crafting menu first` during RTS flow.

- Investigation result
  - Server-side terminal menu could be closed by crafting-menu validity checks in non-workbench context.
  - JEI transfer handler had an extra screen-class guard that is unreliable in JEI recipe-view overlay flow.

- Fixes applied
  - `server/RtsStorageManager.java`
    - `openCraftTerminal(...)` now opens an always-valid crafting menu:
      - uses `ContainerLevelAccess.NULL`
      - overrides `stillValid(...)` to return true
    - removed dependency on reflection-based validation relaxation for this RTS terminal open path.
  - `compat/jei/RtsCraftTerminalJeiTransferHandler.java`
    - removed `minecraft.screen instanceof CraftingScreen` gate.
    - transfer now only checks RTS enabled + linked storage, then sends `C2SRtsJeiTransferPayload`.

- Why this should resolve the reported behavior
  - Prevents immediate close of RTS craft menu caused by world-validity checks.
  - Prevents false JEI rejection when current top-level screen type is not plain `CraftingScreen` but transfer container is valid.

- Validation
  - Ran `./gradlew.bat compileJava`.
  - Build result: SUCCESS.
- Additional stabilization
  - `client/ClientRtsController.java`: increased craft-terminal pending replacement timeout from 30 ticks to 120 ticks to tolerate delayed menu-open packets.

[2026-03-05] UX repair pass #1
- Scope completed in this pass
  - removed temporary import-buffer side panel display (client overlay)
  - JEI shift behavior adjusted: Shift + '+' requests one set
  - RTS category text scaled down slightly
  - placed-block insta-break path replaced with silk-simulated normal drop flow

- Client changes
  - `client/RtsClientInputGate.java`
    - removed import buffer panel drawing/preview queue.
    - kept Shift+LClick and Shift+RClick menu-slot import behavior (existing payload path).
  - `compat/jei/RtsCraftTerminalJeiTransferHandler.java`
    - on transfer, if `Screen.hasShiftDown()` then force `maxTransfer=false` in `C2SRtsJeiTransferPayload`.
    - non-shift keeps JEI-provided `maxTransfer`.
  - `client/BuilderScreen.java`
    - `CATEGORY_TEXT_SCALE` changed from `0.88F` to `0.84F`.

- Server break/loot path change
  - `server/RtsStorageManager.java`
    - `breakPlaced(...)` no longer uses synthetic `removeBlock + refund item/container extraction`.
    - now snapshots nearby drop entities, performs break via:
      - temporary main hand `NETHERITE_PICKAXE` + `SILK_TOUCH`
      - `player.gameMode.destroyBlock(targetPos)`
    - collects newly spawned drops and imports them into linked storage with existing fallback route.
    - preserves loot-table/block-entity driven drop semantics (important for machine NBT-sensitive blocks).
    - removed obsolete helpers: `extractContainerContents(...)`, `refundFromState(...)`.

- Validation
  - Ran `./gradlew.bat compileJava`.
  - Build result: SUCCESS.

[2026-03-05] FTB integration pass (Quest Detect + RTS storage-aware ItemTask)
- Implemented
  - Added RTS UI button `QUEST DETECT` (top bar) to trigger manual detect.
  - Added payload `C2SRtsQuestDetectPayload(mode)` and server network handler wiring.
  - Added `ClientRtsController.detectQuestsNow()` facade.

- Server detect path
  - Added `RtsStorageManager.detectQuests(...)` manual entry.
  - Added throttled helper `runQuestDetect(player, session, force)` with `nextQuestDetectTick` in session.
  - Added `countLinkedItemsMatching(...)` for per-slot linked storage scans with predicate.

- Auto detect hooks (throttled)
  - after carried->linked import
  - after menu-slot import to linked
  - after mining auto-absorb changed
  - after funnel changed
  - after JEI transfer success
  - after placed-block break recovery import

- FTB compat implementation
  - Added `compat/ftb/RtsFtbCompat`:
    - checks `ftbquests` + `ftbteams` loaded
    - initializes impl safely; disables on init failure
  - Added `compat/ftb/RtsFtbCompatImpl` (reflection-based):
    - resolve team from FTB Teams API
    - resolve TeamData from `ServerQuestFile.INSTANCE.getOrCreateTeamData(team)`
    - read submit task list (`submitTasks`)
    - process only ItemTask-like objects
    - skip tasks with `consumesResources=true` or `onlyFromCrafting=true`
    - count matching stacks from player inventory + linked handlers using ItemTask `test(stack)`
    - clamp by `getMaxProgress()` and write via `TeamData.setProgress(task, value)`

- Notes on dependency strategy
  - Used reflection in compat impl to avoid hard compile-time coupling for this pass.
  - Core RTS code path remains load-safe when FTB mods are absent.

- Validation
  - Ran `./gradlew.bat compileJava`.
  - Build result: SUCCESS.

[2026-03-05] Craft terminal stabilization + custom RTS craft GUI pass
- Runtime stabilization
  - `ClientRtsController.tick()` now has pending-open fallback based on `player.containerMenu`:
    - if pending and current menu is non-player `CraftingMenu`, force open RTS terminal screen immediately.
    - keeps existing replacement from vanilla `CraftingScreen` as secondary path.

- Dedicated craft GUI rewrite
  - `client/RtsCraftTerminalScreen.java` rewritten to custom `AbstractContainerScreen<CraftingMenu>` implementation.
  - custom panel rendering for terminal look; no vanilla workbench background.
  - `RtsClientInputGate.resolvePanelY()` updated to treat `RtsCraftTerminalScreen` as top-aligned overlay context.

- JEI transfer behavior correction
  - transfer handler now forwards JEI `maxTransfer` directly (shift semantics from JEI preserved).
  - server `applyJeiTransfer(...)` rewritten:
    - combined extraction sources: linked storage first, then player inventory.
    - supports single-pass and max-pass fill behavior.
    - supports topping up already matching ingredient stacks.
    - includes per-pass rollback to avoid half-applied failed pass.

- Craft-result auto-refill in import flow
  - in `importMenuSlotToLinked` result-slot path:
    - snapshot pre-craft 3x3 blueprint,
    - quick-move result and import to linked,
    - refill grid from linked + player inventory using blueprint (fill-all mode for this shift import path).

- Validation
  - Ran `./gradlew.bat compileJava`.
  - Build result: SUCCESS.

[2026-03-05] Craft terminal UI harmony pass (inventory/hotbar visibility repair)
- Symptom recap
  - RTS crafting terminal looked unbalanced/harsh.
  - player inventory + hotbar region not visually clear enough in current custom-painted background.

- External comparison (web lookup)
  - Reviewed ToM's Simple Storage terminal artifacts and class/texture structure:
    - dedicated terminal textures (terminal.png, crafting_terminal.png) and texture-driven layout.
  - Applied same principle in RTS terminal: prioritize stable slot scaffold first, then add styling.

- Changes made
  - client/RtsCraftTerminalScreen.java
    - switched terminal base layer to vanilla crafting texture:
      - textures/gui/container/crafting_table.png
    - changed imageHeight from 196 to 166 (vanilla crafting baseline).
    - adjusted title anchor to align with header zone.
    - added restrained RTS overlay frames:
      - top accent header
      - craft grid frame
      - result slot frame
      - full player inventory + hotbar frame
    - added drawPanelFrame(...) helper for consistent border rendering.

- Result
  - visual readability improved with clear inventory/hotbar boundaries.
  - slot alignment now follows vanilla crafting geometry while keeping RTS identity overlay.

- Validation
  - Ran ./gradlew.bat compileJava.
  - Build result: SUCCESS.
[2026-03-05] Craft terminal main-panel linked grid + shift-right bulk import pass
- User target
  - linked storage grid must live inside RTS craft terminal main panel (not bottom overlay)
  - Shift+RMB result import must be truly bulk, not one-by-one

- Client terminal integration
  - client/RtsCraftTerminalScreen.java
    - expanded terminal width and added right-side linked panel region
    - linked panel now includes:
      - 8x5 item slots
      - sort mode + sort direction mini buttons
      - page prev/next and page index text
      - STORE button for carried->linked import
    - linked slot click behavior:
      - LMB: pickup max stack to carried
      - RMB: pickup one
    - Shift + LMB/RMB on occupied menu slot sends C2SRtsImportMenuSlotPayload directly from terminal screen

- Overlay decoupling
  - client/RtsClientInputGate.java
    - for RtsCraftTerminalScreen, overlay render/input hooks now early-return
    - result: terminal uses in-panel linked UI only; old bottom overlay is not used in this screen

- Server bulk craft import fix
  - server/RtsStorageManager.java
    - in importMenuSlotToLinked(...) result-slot branch:
      - replaced single quickMoveStack path with iterative loop (guarded)
      - each loop captures player inventory delta for crafted result and immediately stores to linked
      - keeps blueprint refill after loop (illAll=true)
    - this resolves "shift-right still importing one by one" symptom

- Build check
  - Ran ./gradlew.bat compileJava
  - Result: SUCCESS

[2026-03-09] Release metadata update pass
- User request
  - change version to `1.0.3-alpha`
  - run build

- Changes made
  - `gradle.properties`
    - updated `mod_version` from `1.0.3` to `1.0.3-alpha`
  - version wiring remains property-driven through Gradle and mod template expansion

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS

[2026-04-04] Forge 1.20.1 sister project scaffold + verified bootstrap
- User request
  - evaluate the practicality of a `1.20.1 Forge` sister project
  - recommend a maintainable folder layout
  - download as much of the required bootstrap material as possible

- Decisions
  - keep the port as a separate in-repo sister project at
    `sister-projects/rtsbuilding-forge-1.20.1/`
  - use the latest official Forge `1.20.1` MDK available on the Forge files
    page at the time of setup: `47.4.16`
  - keep the Java package and mod id aligned with mainline
    (`com.rtsbuilding.rtsbuilding`, `rtsbuilding`) to reduce copy/edit churn

- Changes made
  - downloaded the official Forge MDK zip to
    `sister-projects/rtsbuilding-forge-1.20.1/upstream/forge-1.20.1-47.4.16-mdk.zip`
  - verified the MDK zip SHA1 as
    `7b1b2a6fa948089bd2b4ff6105367af083918a01`
  - expanded the MDK into the sister-project directory
  - replaced the default `examplemod` template with a minimal
    `rtsbuilding` Forge bootstrap:
    - `RtsbuildingMod.java`
    - `Config.java`
    - updated `gradle.properties`
    - updated `settings.gradle`
    - added `maven.blamejared.com` to repositories for future JEI work
  - added local sister-project documentation:
    - `sister-projects/rtsbuilding-forge-1.20.1/README.md`
    - `sister-projects/rtsbuilding-forge-1.20.1/docs/PORTING.md`
  - added empty structure placeholders for:
    - `assets/rtsbuilding/lang`
    - `data/rtsbuilding`
    - `src/generated/resources`
  - updated sister-project `.gitignore` to ignore cached upstream zips

- Environment findings
  - machine default `java` is Java 21
  - local JDK 17 exists at
    `C:\Program Files\Microsoft\jdk-17.0.8.7-hotspot`
  - Gradle wrapper could not fetch `gradle-8.8-bin.zip` reliably through the
    Java downloader due repeated connection resets, so the distribution was
    downloaded manually into the local wrapper cache and unpacked there

- Build check
  - Ran `./gradlew.bat help` with JDK 17
  - Result: SUCCESS
  - Ran `./gradlew.bat compileJava` with JDK 17
  - Result: SUCCESS

[2026-03-31] Deshitfication phase 1 kickoff
- User request
  - identify the growing God Classes in the project
  - write a deshitfication plan to disk
  - start the first low-risk refactor slice without changing existing
    function entry points
  - update `Journal.txt` and `detailed_project.txt`

- Safety / process
  - created a pre-refactor git snapshot commit:
    - `backup: pre-deshitfication snapshot`

- Changes made
  - `deshitfication_plan.txt`
    - added the staged refactor plan with guardrails and execution order
  - `client/RtsClientUiUtil.java`
    - added shared panel-frame drawing, text trimming, compact count, and
      compact fluid amount helpers
  - `client/RtsCraftablesUiHelper.java`
    - added shared craftables dialog open/submit/search-normalization helpers
  - `client/BuilderScreen.java`
    - delegated repeated craft dialog flow and UI primitive formatting into
      shared helpers
  - `client/RtsClientInputGate.java`
    - delegated repeated craft overlay dialog flow and UI primitive
      formatting into shared helpers
  - `client/RtsCraftTerminalScreen.java`
    - switched local count/panel helpers to the shared client UI helper
  - `client/RtsCraftQuantityDialog.java`
    - switched panel frame drawing to the shared client UI helper
  - `client/RtsCraftFeedbackPopup.java`
    - switched panel frame drawing to the shared client UI helper

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS
  - Warnings only: existing `EventBusSubscriber.Bus.GAME` deprecation warnings

[2026-03-31] Deshitfication phase 2 - client remote menu compat extraction
- User request
  - continue with the next refactor slice

- Changes made
  - `client/RtsClientRemoteMenuCompat.java`
    - extracted client remote-menu wrapper install + validation-relax logic
      out of `ClientRtsController`
    - owns the reflection-based menu remap and validity patch helpers
  - `client/ClientRtsController.java`
    - now delegates remote-menu compat work to the new helper
    - removed dead remote-menu spoof state left over from the earlier
      no-teleport rollback path
    - simplified remote-menu grace calls to a no-argument helper because the
      target position is no longer used client-side
  - `deshitfication_plan.txt`
    - updated progress to mark shared client UI extraction complete and
      remote-menu compat extraction started
  - `detailed_project.txt`
    - refreshed the top-level code map and change notes for the new helper

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS
  - Warnings only: existing `EventBusSubscriber.Bus.GAME` deprecation warnings

[2026-03-31] Deshitfication phase 3 - client packet gateway extraction
- User request
  - continue splitting with a `don't break things` bias

- Changes made
  - `client/RtsClientPacketGateway.java`
    - extracted C2S payload construction and `PacketDistributor.sendToServer`
      calls out of `ClientRtsController`
    - centralizes camera move, storage page, craftables, quick slot,
      GUI binding, place/interact, fluid, break, and mine packet sends
  - `client/ClientRtsController.java`
    - now delegates packet emission to the new gateway while keeping local
      state validation and state mutations in place
    - removed direct `sendToServer(...)` usage from the controller
  - `deshitfication_plan.txt`
    - refreshed phase progress for the packet gateway extraction
  - `detailed_project.txt`
    - updated the code map and change notes for the new helper

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS
  - Warnings only: existing `EventBusSubscriber.Bus.GAME` deprecation warnings

[2026-04-02] Sophisticated Storage remote-menu compat follow-up
- Problem addressed
  - the initial Sophisticated Storage remote-menu wrapper fixed the direct
    distance `stillValid(...)` check, but it also changed the runtime menu
    type
  - Sophisticated Storage client screens expect the original
    `StorageContainerMenuBase` hierarchy, so wrapped menus could still produce
    incompatible screen/menu pairs during remote open

- Changes made
  - `compat/sophisticatedstorage/RtsSophisticatedStorageCompat.java`
    - removed the old wrapper-menu approach for Sophisticated Storage remote
      opens
    - added tracked remote-menu ids on both server and client so only RTS-
      opened menus receive the still-valid bypass
    - added helpers that recognize the original Sophisticated Storage menu and
      screen base classes
  - `mixin/compat/sophisticatedstorage/StorageContainerMenuMixin.java`
    - injected `StorageContainerMenu#stillValid(...)`
    - returns `true` only for tracked RTS-opened Sophisticated Storage menus
  - `src/main/resources/rtsbuilding.mixins.json`
    - registered the Sophisticated Storage mixin
  - `src/main/templates/META-INF/neoforge.mods.toml`
    - enabled loading of `${mod_id}.mixins.json`
  - `client/RtsClientRemoteMenuCompat.java`
    - added a screen/menu pair safety guard for Sophisticated Storage screens
    - marks and clears tracked client remote-menu ids instead of relying on a
      wrapped menu instance
  - `client/ClientRtsController.java`
    - remote-menu grace begin now notifies Sophisticated Storage compat about
      the pending open
    - remote-menu install is now guarded so incompatible menu/screen pairs
      fail closed instead of crashing the client
  - `server/RtsStorageManager.java`
    - server-side remote-menu validation now tracks Sophisticated Storage menu
      ids instead of carrying old spoof-position session fields
    - clears the tracked compat state once the player leaves the remote menu

- Effect
  - Sophisticated Storage remote opens stay on the no-teleport path
  - the original storage menu type is preserved for typed client screens
  - the dedicated `stillValid(...)` bypass only applies to RTS-opened menus

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS
  - Warnings only: existing `EventBusSubscriber.Bus.GAME` deprecation warnings

[2026-04-04] AE2 GUI binding icon fallback for terminal/provider targets
- Problem addressed
  - GUI binding slots could still show no icon for AE2 multipart targets even
    after face persistence was added
  - the normal clone-stack / `block.asItem()` preview path can come back blank
    for AE2 terminal/provider parts mounted on a cable bus

- Changes made
  - `compat/ae2/RtsAe2Compat.java`
    - added AE2 GUI binding icon fallback resolution
    - fallback inspects AE menu/provider display names, class names, and
      direction-specific cable-bus parts to recover the original item id when
      the normal preview path is blank
    - includes alias handling for crafting terminal, pattern encoding terminal,
      pattern access / pattern-provider style terminal names
  - `client/BuilderScreen.java`
    - GUI bind item-id hint lookup now falls back to the AE2 compat resolver
      when block clone-stack preview is empty
  - `server/RtsStorageManager.java`
    - server-side GUI binding icon resolution now does the same AE2 fallback
      before saving the binding icon id into player session data
    - request-page refresh now backfills old saved bindings whose icon item id
      was blank, so existing AE targets can recover an icon without rebinding
  - `detailed_project.txt`
    - refreshed the project map to note the AE2 GUI binding icon fallback path

- Effect
  - binding AE2 crafting/pattern/provider-style terminals can now recover a
    visible icon even when the world target itself does not expose a normal
    block item preview
  - once resolved, the icon still persists through the existing saved GUI
    binding item-id path
  - previously saved blank-icon AE GUI binds can self-heal on the next storage
    page refresh if their world target is still available

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS
  - Warnings only: existing `EventBusSubscriber.Bus.GAME` deprecation warnings

[2026-03-05] RTS terminal-only search/category migration pass
- User target
  - add search box + category column directly in RTS craft terminal linked panel
  - terminal context should no longer rely on bottom overlay for search/category

- Screen changes
  - client/RtsCraftTerminalScreen.java
    - added EditBox search control in linked panel
    - search behavior:
      - live sync to ClientRtsController.setStorageSearch(...)
      - clear button
      - Esc clears and exits focus
      - Enter exits focus
    - added category list column (scrollable + clickable)
      - click category -> setStorageCategory(...)
      - selected category auto-scrolls into visible range
    - linked grid layout changed to 5x5 to make room for search + category UI

- Scope boundary
  - terminal overlay suppression remains active (RtsClientInputGate already early-returns on RtsCraftTerminalScreen)
  - other container screens are unchanged and still use overlay behavior

- Build check
  - Ran ./gradlew.bat compileJava
  - Result: SUCCESS
[2026-03-05] Craft-result restoration + JEI R/U lookup integration pass
- Issues addressed
  - RTS terminal could not show crafting result / could not craft
  - non-terminal shift bulk import was refilling from player inventory unexpectedly
  - JEI R/U did not work on RTS-rendered storage item slots

- Craft terminal fix (server)
  - added server/RtsCraftTerminalMenu.java (extends CraftingMenu, stillValid=true)
  - in RtsStorageManager.openCraftTerminal(...):
    - replaced ContainerLevelAccess.NULL with dynamic access bound to player's world/position
    - keeps remote validity relaxed while restoring crafting result recalculation path

- Refill source policy split
  - in importMenuSlotToLinked(...) result-slot path:
    - terminal menu (RtsCraftTerminalMenu) keeps linked+player fallback refill
    - non-terminal menus use linked-only refill
  - efillCraftGridFromBlueprint(...) now accepts includePlayerFallback

- JEI R/U on RTS storage slots
  - added compat/jei/RtsCraftTerminalJeiGuiHandler.java
    - container handler for RtsCraftTerminalScreen
    - provides JEI clickable ingredient under mouse for terminal linked grid
  - added compat/jei/RtsOverlayJeiGlobalGuiHandler.java
    - global handler for RTS overlay on non-terminal container screens
    - provides JEI clickable ingredient under mouse for overlay linked grid
  - updated compat/jei/RtsJeiPlugin.java
    - registered GUI handlers in egisterGuiHandlers(...)

- Build check
  - Ran ./gradlew.bat compileJava
  - Result: SUCCESS
[2026-03-05] Shift-left unified bulk craft-import pass
- User feedback addressed
  - RTS terminal refill still pulled from player inventory
  - shift interaction felt split: first one craft, next click bulk
  - target behavior: Shift + LMB directly bulk crafts with continuous refill

- Server logic update (RtsStorageManager.importMenuSlotToLinked)
  - result-slot bulk loop now refills during the loop, not only at end
  - when result temporarily disappears, performs one immediate refill attempt and retries in same click
  - after each successful craft-transfer step, triggers single-step refill and continues
  - full refill still runs at loop end to leave grid state consistent

- Source policy update
  - result-slot refill now uses linked-only source (includePlayerFallback=false) in this import path
  - terminal no longer consumes player inventory during refill
  - normal crafting menus follow same linked-only refill mode for unified behavior

- Input normalization
  - RtsCraftTerminalScreen: result slot shift-import is now Shift + LMB only
  - RtsClientInputGate: same rule applied for non-terminal container path (menuSlot == 0)

- Build check
  - Ran ./gradlew.bat compileJava
  - Result: SUCCESS
[2026-03-05] RTS terminal category module removal pass
- User request
  - remove category module from RTS terminal linked panel

- Changes made
  - client/RtsCraftTerminalScreen.java
    - removed category column rendering and all category click/scroll logic
    - removed category helper methods and related state fields
    - kept search box + sort + page + linked slot interactions
    - restored linked grid to 8 columns x 5 rows for denser visible storage slots

- Build check
  - Ran ./gradlew.bat compileJava
  - Result: SUCCESS
[2026-03-05] Top-bar compaction pass (Quest Detect overflow)
- User issue
  - Quest Detect button exceeded top-layer bounds
  - requested slightly narrower top UI

- Changes made
  - client/BuilderScreen.java
    - introduced compact top-bar constants:
      - TOP_BUTTON_GAP, TOP_ACTION_BUTTON_W, TOP_SENS_BUTTON_W, TOP_AUTOSTORE_BUTTON_W, TOP_GUIDE_BUTTON_W
    - narrowed widths for shape/rotate/quest buttons too
    - updated both render path and click-hit path to use identical constants

- Result
  - top row footprint reduced; quest button no longer pushes out of UI in narrow layouts

- Build check
  - Ran ./gradlew.bat compileJava
  - Result: SUCCESS

[2026-03-09] RTS craftables lazy-search pagination pass
- User issue
  - RTS storage craftables lookup caused severe lag
  - wanted craft lookup to stay idle when craft search is empty
  - wanted only the first 12 results at first, then scroll-triggered fill
  - wanted duplicate fetches avoided

- Changes made
  - `client/ClientRtsController.java`
    - craftables requests now clear-and-stop on empty search
    - added 12-result batch loading and `requestMoreCraftables()`
    - added pending offset tracking to avoid duplicate in-flight fetches
    - stale craftables payloads are ignored if search/toggle state no longer matches
  - `server/RtsStorageManager.java`
    - craftables request API now accepts `offset` + `limit`
    - empty search returns immediately without recipe scan work
    - server sends only the requested slice plus append/hasMore metadata
    - remembers requested depth so post-craft refresh can refill the already-loaded range
  - `network/C2SRtsRequestCraftablesPayload.java`
    - added `offset` and `limit`
  - `network/S2CRtsCraftablesPayload.java`
    - added `offset`, `append`, and `hasMore`
  - `network/RtsNetworkHandlers.java`
    - forwards paged craftables request parameters
  - `client/BuilderScreen.java`
    - craft panel scroll now requests the next batch only when reaching the current bottom
  - `client/RtsClientInputGate.java`
    - overlay craft panel uses the same bottom-scroll incremental loading rule

- Build check
  - Ran `./gradlew.bat compileJava`
  - Result: SUCCESS

[2026-03-20] RTS remote GUI first-open handoff stabilization
- User issue
  - right-click opening GUI in RTS could intermittently fail/crash on first open
  - after opening the same GUI once outside RTS, the RTS path often became stable
  - suspicion was a loading/timing issue rather than the server-side hit logic itself

- Root-cause notes
  - RTS already performs server-side remote interaction through a temporary remote-POV player context
  - the unstable part was the client handoff after menu open:
    - `ClientRtsController.tick()` restored `BuilderScreen` whenever `minecraft.screen == null`
    - some container screens can spend a short first-open window with `screen == null` while the real GUI is still being constructed
    - during that gap, RTS could reclaim the foreground and interfere with the menu open

- Changes made
  - `client/ClientRtsController.java`
    - added generic `pendingRemoteMenuOpenTicks` grace window for RTS right-click paths that may open menus
    - `placeSelected(...)`, `interactEmpty(...)`, block/entity interact helpers, and `openCraftTerminal()` now start that grace window before sending the packet
    - `tick()` now checks for non-player `containerMenu` ownership before restoring `BuilderScreen`
    - if a real remote menu is already open and `BuilderScreen` is still current, RTS clears it so the incoming container GUI can take foreground ownership cleanly
  - no server/protocol changes were needed in this pass

- Why this approach
  - deeper "virtual player right-click" behavior is already present server-side
  - this patch addresses the client-side menu-loading race directly, which better matches the reported "open once outside RTS, then RTS works" symptom

- Build check
  - Ran `powershell.exe -Command .\\gradlew.bat compileJava`
  - Result: SUCCESS
  - Ran `powershell.exe -Command .\\gradlew.bat build`
  - Result: SUCCESS

[2026-03-20] Craft dialog recipe-selection + consumed-material feedback popup
- User issue
  - right-click craft dialog should let the user choose the recipe before choosing craft count
  - the craft dialog should render on a higher layer so other UI does not cover it
  - after crafting, a high-layer popup should show the consumed materials

- Changes made
  - `server/RtsStorageManager.java`
    - changed craftables grouping from single best-recipe collapse to grouped result entries that retain all matching recipe options
    - each grouped craftable entry now sorts recipe options by craftability / missing count / output count
    - craft execution now aggregates actual consumed items from extracted ingredients
    - craft feedback packet now includes consumed item counts
  - `network/S2CRtsCraftablesPayload.java`
    - extended payload shape to carry flattened per-entry recipe-option lists
  - `network/S2CRtsCraftFeedbackPayload.java`
    - extended payload shape to carry consumed item ids + counts
  - `client/ClientRtsController.java`
    - craftable entries now decode and store recipe-option lists
    - craft feedback now decodes consumed materials and keeps them for popup rendering
    - repeated same-item craft feedback merges both crafted count and consumed counts while the popup is active
  - `client/RtsCraftQuantityDialog.java`
    - redesigned dialog to support recipe selection plus quantity input
    - dialog now keeps selected recipe state, supports recipe scrolling, and confirms against the selected recipe id
    - dialog render uses a high Z layer so it stays above other UI
  - `client/RtsCraftFeedbackPopup.java`
    - added high-layer craft-result popup that shows crafted output plus consumed materials
  - `client/BuilderScreen.java`
    - right-click craft tooltip now reflects recipe/count selection
    - craft dialog open path now passes recipe options instead of a single recipe
    - craft feedback render now uses the new high-layer popup
  - `client/RtsClientInputGate.java`
    - overlay craft dialog now uses the same recipe-selection flow
    - overlay scroll input is routed into the open recipe dialog
    - overlay craft feedback render now uses the same high-layer popup
  - `detailed_project.txt`
    - documented grouped craftables with per-entry recipe selection and consumed-material feedback popup behavior

- Build check
  - Ran `./gradlew.bat compileJava`
  - Result: SUCCESS

[2026-03-20] RTS camera lower-height allowance
- User issue
  - RTS camera could not move down to player height
  - wanted camera to be allowed at player height and up to 5 Y below the player anchor

- Changes made
  - `server/RtsCameraManager.java`
    - lowered the authoritative RTS camera minimum height offset from `+4` to `-5`
    - reapplies the vertical clamp after distance normalization so `MIN_DIST` cannot push camera below the new floor
  - `client/ClientRtsController.java`
    - matched local prediction camera Y clamp to the same `-5 .. +80` range
    - introduced local camera height bound constants to avoid client/server drift in future edits
    - reapplies the same vertical clamp after local distance normalization for prediction parity
  - `detailed_project.txt`
    - documented the current RTS camera vertical bounds and the requirement that client prediction matches server authority

- Build check
  - Ran `./gradlew.bat compileJava`
  - Result: SUCCESS

[2026-03-20] Recent panel unified-list fix
- User issue
  - recent panel only showed crafted entries
  - placed/used recent entries were being recorded server-side but filtered out on the client

- Changes made
  - `client/ClientRtsController.java`
    - removed the extra crafted-only recent cache
    - `getRecentEntries()` now returns the full `recentEntries` list
    - `applyStoragePage(...)` now keeps all decoded recent entries in the same list used by the panel
    - `selectRecentEntry(...)` now resolves from the unified recent list
  - `detailed_project.txt`
    - clarified that the client recent panel uses the unified server-backed recent list rather than a crafted-only subset

- Build check
  - Ran `./gradlew.bat compileJava`
  - Result: SUCCESS

[2026-03-10] Storage split/recent/link-mode/rotate/quest detect refresh
- User issue
  - `detailed_project.txt` was outdated and needed a full rescan rewrite
  - RTS storage needed a recent-history panel without adding extra bottom rows
  - storage search/filter should not blank hotbar/pin information
  - shift + left click on RTS hotbar should store that hotbar stack into linked storage
  - linked storage needed two link types:
    - left click: extract + store
    - right click: extract only
  - rotate mode should rotate the clicked block directly instead of triggering normal interaction
  - quest detect button had stopped updating FTB non-consume inventory tasks
  - craft panel row count should match visible storage rows

- Changes made
  - `server/RtsStorageManager.java`
    - added persistent per-link mode storage in session + player NBT
    - wrapped linked item/fluid handlers so extract-only links still allow reads/drains but reject inserts/fills
    - extended storage page payload build to include:
      - total item counts cache
      - recent item/fluid entries
    - added recent-entry tracking for place/use/craft/fluid actions
    - added explicit block rotation server path
    - added hotbar-to-linked store path
    - removed linked-item quest detect double-counting of player main inventory
  - `network/*`
    - `C2SRtsLinkStoragePayload` now carries link mode
    - added `C2SRtsRotateBlockPayload`
    - added `C2SRtsStoreHotbarSlotPayload`
    - extended `S2CRtsStoragePagePayload` with total counts + recent entries
  - `client/ClientRtsController.java`
    - now caches total item counts and recent entries
    - added explicit rotate-block send helper
    - added hotbar-store send helper
    - added extract-only link send path
  - `client/BuilderScreen.java`
    - split bottom storage area into normal storage + recent panel
    - recent panel supports item/fluid selection
    - pin count overlays now read total count cache instead of current filtered page only
    - shift + left click on RTS hotbar stores that slot to linked storage
    - rotate mode right click now sends direct rotate packet
    - link mode right click now creates extract-only links
    - craft panel visible rows now match storage row count
  - `client/RtsClientInputGate.java`
    - pin count overlay now also uses total count cache
  - `server/RtsCraftTerminalMenu.java`
    - records craft output for recent history
  - `compat/ftb/RtsFtbCompatImpl.java`
    - made reflection more robust for newer FTB naming/layout changes
    - team lookup now supports both `getTeamForPlayerID` and `getTeamForPlayer`
    - task collection now prefers `collect(ItemTask.class)` and falls back to `submitTasks`
  - `detailed_project.txt`
    - fully rewritten from current repo scan

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS

[2026-03-09] Release metadata update pass
- User request
  - change version to `1.0.4-alpha`

- Changes made
  - `gradle.properties`
    - updated `mod_version` from `1.0.3-alpha` to `1.0.4-alpha`
  - version wiring remains property-driven through Gradle and mod template expansion

- Build check
  - not run in this pass

[2026-03-09] Craft search confirm-button pass
- User issue
  - craft panel search was still firing recipe lookup while typing each letter
  - wanted an explicit confirm button before searching

- Changes made
  - `client/BuilderScreen.java`
    - added compact `OK` button beside the craft search box
    - craft search box now edits only local draft text
    - craftables search is submitted only on `Enter` or `OK`
    - `Escape` still clears and applies empty craft search
  - `client/RtsClientInputGate.java`
    - overlay craft panel uses the same `OK` submit behavior
    - removed per-character craftables submit on typing/backspace/delete/paste
    - draft/apply mismatch now highlights the `OK` button

- Build check
  - Ran `./gradlew.bat compileJava`
  - Result: SUCCESS

[2026-03-09] Shape plane consistency pass
- User issue
  - `LINE` should follow the same flattened first-stage plane logic as `SQUARE`
  - `SQUARE`/`BOX` first stage should keep all target blocks on one plane
  - building near block edges could produce a mixed-height footprint with part of the result lifted upward

- Changes made
  - `client/BuilderScreen.java`
    - `LINE`, `SQUARE`, and `BOX` first-stage face resolution now uses horizontal `UP` plane logic
    - `LINE` second-point selection now projects onto the same horizontal build plane instead of using raw hovered block coordinates
    - added uniform plane target resolution for `LINE` / `SQUARE` / `BOX`
    - whole footprint now inherits one shared anchor-based offset decision instead of shifting occupied cells one-by-one
    - kept `SQUARE` first-stage axes horizontal so footprint generation remains same-`Y`

- Build check
  - Ran `./gradlew.bat compileJava`
  - Result: SUCCESS

[2026-03-09] Shape face-splitting pass
- User issue
  - if `pointA` is clicked on a block side face, the whole `LINE` / `SQUARE` / `BOX` result should land on that side’s outer layer
  - it should not be shifted onto the top face
  - requested approach: preserve original clicked face separately from construction-plane face

- Changes made
  - `client/BuilderScreen.java`
    - split shape face state into:
      - `planeFace` for projection / footprint generation
      - `placementFace` for final placement direction
    - `ShapeBuildSession` and `ShapeBuildInput` now carry both faces
    - first click now stores original clicked face through `resolveShapePlacementFace(...)`
    - uniform target resolution for `LINE` / `SQUARE` / `BOX` now follows the anchor’s original clicked face
    - generated placement hits and undo/redo replay now use `placementFace`

- Build check
  - Ran `./gradlew.bat compileJava`
  - Result: SUCCESS

[2026-03-22] Persistent recent/pins + GUI binding sidebar + rotate click priority
- User request
  - make recent items and RTS hotbar/pin records part of save data so they survive exit/rejoin
  - fix rotate mode being overridden by normal right-click behavior
  - add a collapsible sidebar that can bind GUIs and open them directly
  - bump version to `1.0.6-alpha`

- Changes made
  - `server/RtsStorageManager.java`
    - extended persistent RTS session NBT with:
      - recent entries
      - quick pin item ids
      - GUI binding slots
    - added server APIs for:
      - quick pin set/clear
      - GUI bind/clear
      - GUI open by saved binding slot
    - storage page payload build now includes quick pin ids and GUI binding labels
    - GUI bindings store target position, target dimension, and cached display label
  - `network/S2CRtsStoragePagePayload.java`
    - added quick pin item-id list
    - added GUI binding label list
  - `network/C2SRtsSetQuickSlotPayload.java`
    - new C2S payload for persistent quick pin updates
  - `network/C2SRtsSetGuiBindingPayload.java`
    - new C2S payload for bind/clear GUI slot updates
  - `network/C2SRtsOpenGuiBindingPayload.java`
    - new C2S payload for opening a saved GUI binding
  - `network/RtsNetworkHandlers.java`
    - registered handlers for new quick pin / GUI binding payloads
  - `network/RtsPayloadRegistrar.java`
    - registered new payloads in the play channel
  - `client/ClientRtsController.java`
    - quick pins are now locally optimistic but server-backed
    - added GUI binding label cache + open/bind/clear send helpers
    - quick pins and GUI labels are applied from `S2CRtsStoragePagePayload`
  - `client/BuilderScreen.java`
    - added collapsible left GUI sidebar
    - left click on bound slot opens saved GUI
    - right click arms rebind mode for a slot
    - shift + right click clears a slot
    - next left click on a world block while armed saves that GUI target
    - rotate mode right click now executes immediately on mouse press to guarantee rotation wins over generic right-click flow
  - `gradle.properties`
    - updated `mod_version` to `1.0.6-alpha`

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS

[2026-03-26] AE2 network compat + client layout persistence
- Functional pass
  - add optional AE2-linked storage access
  - persist storage panel layout client-side
  - add fill-inventory-from-linked packet path for RTS storage

- Changes made
  - `compat/ae2/RtsAe2Compat.java`
    - added reflection-based AE2 storage-service bridge
    - exposes AE2 network contents through an `IItemHandler`-style view
    - reports full stored counts separately from display-stack counts
  - `server/RtsStorageManager.java`
    - linked item resolution now prefers AE2 network access when available
    - linked item count paths can use reported counts from wrapped handlers
  - `client/RtsClientLayoutStore.java`
    - added local JSON-backed layout persistence under `config/rtsbuilding-client-layout.json`
  - `client/ClientRtsController.java`
    - load/save normalized storage panel layout through `RtsClientLayoutStore`
    - added `fillInventoryFromLinked()` packet helper
  - `network/C2SRtsFillInventoryPayload.java`
    - new C2S payload for linked-storage inventory fill
  - `network/RtsNetworkHandlers.java`
    - added handler that routes fill-inventory requests into `RtsStorageManager.fillPlayerInventoryFromLinked(...)`
  - `network/RtsPayloadRegistrar.java`
    - registered the new fill-inventory payload

- Build check
  - not re-run separately in this historical entry

[2026-03-28] Bottom dock + overlay drag + recent aggregation pass
- Functional pass
  - replace the older floating/bespoke RTS storage presentation with a tighter post-1.0.7 layout
  - expand GUI binding usability
  - make recent history accumulate repeated actions instead of constantly replacing rows

- Changes made
  - `client/BuilderScreen.java`
    - replaced the old floating bottom storage layout with a full-width bottom dock
    - kept height resizing on `+` / `-` controls, but removed free drag positioning
    - replaced the old left GUI sidebar with a compact craft/GUI dock
    - craft dock center `C` button opens the RTS craft terminal
    - GUI binding ring now supports 8 slots around the craft button
    - empty left click arms bind mode, bound left click opens, right click toggles rebind, `Shift+Right` clears
    - armed GUI bind now renders a dedicated `+` cursor in world space
    - recent grid tooltip/overlay now emphasizes accumulated amount instead of action text badges
  - `client/RtsClientInputGate.java`
    - added `MOVE` drag handle for the storage overlay on container/crafting screens
    - overlay position now uses persisted normalized X/Y layout state
    - drag state is canceled cleanly on release, close, and screen swaps
  - `client/ClientRtsController.java`
    - increased GUI binding slot count from 3 to 8
    - remote menu hints can now complete even after RTS handoff has already started
  - `server/RtsStorageManager.java`
    - increased server GUI binding slot count from 3 to 8
    - GUI binding open path now waits for a real menu open before sending the remote-menu hint
    - recent entries now merge by item/fluid identity and accumulate amounts
    - guarded remote-menu validation hookup against null menus
    - linked item wrappers now forward AE2-reported counts consistently

- Build check
  - validated in the 1.0.8-alpha release pass below

[2026-03-28] Release 1.0.8-alpha metadata + docs pass
- User request
  - record post-`1.0.7` changes from `2026-03-26` to `2026-03-28` in git
  - build a new `1.0.8-alpha`
  - update `detailed_project.txt` and `Journal.txt`

- Changes made
  - `gradle.properties`
    - updated `mod_version` from `1.0.7-alpha` to `1.0.8-alpha`
  - `detailed_project.txt`
    - refreshed the project map for:
      - AE2 compat
      - client layout persistence
      - bottom-dock RTS UI
      - craft-dock GUI bindings
      - draggable overlay behavior
      - recent-entry aggregation rules
      - post-1.0.7 networking additions
  - `Journal.txt`
    - appended the 2026-03-26 through 2026-03-28 development/release log

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS
  - Artifact: `build/libs/rtsbuilding-1.0.8-alpha.jar`

[2026-03-28] GUI bind fallback for AE-style terminals
- User issue
  - empty GUI bind could not save AE terminal targets
  - expected any block that opens a GUI on right click to be bindable

- Changes made
  - `server/RtsStorageManager.java`
    - relaxed bind-time validation so GUI binding accepts reachable block
      entities even when they do not expose a direct `MenuProvider`
    - this specifically allows AE-style terminals that open through their own
      right-click path to be stored as GUI binds
    - kept `MenuProvider` display-name lookup when available and fell back to
      the block display name otherwise
    - added an explicit client hint when a bound target consumes the bind but
      still does not open a GUI at use time
  - `detailed_project.txt`
    - updated GUI binding notes to reflect the block-entity fallback path

- Build check
  - validated in the remote-menu rollback pass below

[2026-03-28] Remote GUI validation teleport rollback
- User issue
  - remote GUI open still teleported the player to the target and then back
  - this validation strategy had already been documented as problematic and needed to be rolled back

- Changes made
  - `server/RtsStorageManager.java`
    - disabled the opened-menu tick-time player-position spoof on the server
    - kept remote menu state cleanup when the container closes
  - `client/ClientRtsController.java`
    - disabled the client-side mirror of the remote-menu position spoof
    - kept menu handoff timing and relaxed menu validation paths intact
  - `detailed_project.txt`
    - updated the remote menu handoff note to record that opened-menu validation no longer depends on player teleport spoofing

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS

[2026-03-28] Restore 2026-03-26 15:30 remote GUI baseline + GUI bind icons
- User request
  - restore the right-click GUI logic to the `1.0.7` / `2026-03-26 15:30`
    baseline
  - change craft-dock GUI bindings so they render the bound GUI target icon
    instead of `1/2/3...`
  - keep that icon state persistent across reconnect / reload

- Changes made
  - `client/ClientRtsController.java`
    - restored the client-side pre/post remote-menu validation position spoof
      used by the `1.0.7` baseline
    - restored the `enabled` gate on `applyRemoteMenuHint(...)`
    - now stores GUI binding icon item ids and preview stacks from the storage
      page payload
  - `server/RtsStorageManager.java`
    - restored early `sendRemoteMenuOpenHint(...)` for bound GUI opens
    - restored the server-side tick validation-position spoof used while a
      remote menu remains open
    - GUI binding save/load/page-sync now persists an icon item id alongside
      the binding label and target position
    - kept the newer AE-style bind fallback and explicit "did not open a GUI"
      feedback
  - `network/S2CRtsStoragePagePayload.java`
    - now syncs GUI binding icon item ids in addition to labels
  - `client/BuilderScreen.java`
    - craft-dock GUI binding slots now render the saved preview icon when one
      is available instead of falling back to slot digits
  - `detailed_project.txt`
    - corrected the remote GUI handoff notes to match the restored baseline
    - documented persistent GUI binding icon previews

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS

[2026-03-28] Fluid placement orientation fix + AE2 RTS import/store fix
- User request
  - remove the side-hit "prefer above block" branch from RTS direct fluid
    placement
  - preserve the important hit orientation data during RTS fluid placement
  - fix dragging carried items into RTS storage UI so linked AE2 networks can
    accept the returned items

- Changes made
  - `server/RtsStorageManager.java`
    - removed the side-face upward-bias branch from
      `resolveFluidPlacementPos(...)`
    - fluid placement now only resolves to the clicked block or the clicked
      face's adjacent block
    - added a dedicated fluid-placement hit resolver so replaceability /
      placement context keeps the original click face instead of forcing
      `Direction.UP`
  - `compat/ae2/RtsAe2Compat.java`
    - relaxed AE2 network insertion so RTS write-back does not depend on a
      synthetic slot matching the network snapshot
    - after a successful insert, refreshes the cached snapshot instead of
      manually patching slot state
  - `detailed_project.txt`
    - updated fluid-placement and AE2 write-back notes

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS

[2026-03-29] Remote GUI rollback + AE2 write-back hardening + fluid shape placement
- User request
  - roll remote modded-GUI open back to the `1.0.6` no-teleport solution
  - make RTS store/craft write-back behave correctly while linked AE2 storage
    is in use
  - allow linked-container fluids to place more reliably on right click
  - let selected RTS fluids use the existing shape placement flow

- Changes made
  - `server/RtsStorageManager.java`
    - removed the remote-menu tick-time validation position spoof path and
      kept only relaxed menu validation
    - changed craft-to-linked output handling to use real linked insertion with
      rollback instead of the older slot-snapshot precheck, avoiding false
      `linked storage is full` failures on AE2-backed handlers
    - added a fluid-handler drain fallback that retries via generic
      `drain(amount)` when exact-stack drain paths are not supported
    - linked item insertion helpers now recognize slot-agnostic AE2-backed
      handlers during RTS store/refund/write-back flows
  - `compat/ae2/RtsAe2Compat.java`
    - exposed a slot-agnostic insert path for AE2 network handlers so RTS
      write-back no longer depends on synthetic slot layout
  - `client/ClientRtsController.java`
    - rolled remote GUI handoff back to grace-window behavior without client
      position spoofing
  - `client/BuilderScreen.java`
    - selected fluids now enter the same staged shape workflow as items
    - shape confirm can dispatch repeated RTS fluid placements for
      `LINE/SQUARE/CIRCLE/WALL/BOX`
    - fluid shape mode intentionally skips undo batch recording for now

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS

[2026-03-29] AE2 terminal GUI binding fix
- User request
  - investigate why an AE2 terminal bound into the GUI shortcut bar showed an
    empty icon and would not reopen when clicked
  - allow AE2-specific compatibility if needed

- Root cause
  - the GUI binding packet only stored `BlockPos`, so reopen logic had to guess
    an interaction face later; that is unreliable for face-mounted/multipart
    targets such as AE2 terminals on a cable bus
  - binding icon resolution only looked at the host block item, which can be
    blank or generic for multipart hosts, so the craft-dock slot could render
    as empty even when the binding itself existed

- Changes made
  - `network/C2SRtsSetGuiBindingPayload.java`
    - extended the packet to carry `faceId` and an optional `itemIdHint`
  - `network/RtsNetworkHandlers.java`
    - forwarded the new binding metadata into the server manager
  - `client/ClientRtsController.java`
    - binding requests now send the clicked face and item-id hint
    - clear requests send explicit empty metadata
  - `client/BuilderScreen.java`
    - GUI bind click now captures `BlockHitResult#getDirection()`
    - binding icon hint now prefers the block clone stack instead of only
      `block.asItem()`
  - `server/RtsStorageManager.java`
    - GUI bindings now persist the chosen face in player NBT
    - reopen flow now uses the stored face instead of re-guessing from player
      position
    - synthetic interaction points now honor vertical faces as well
    - binding icon resolution now accepts the hinted item id first, then falls
      back to clone-stack lookup, then finally plain block item fallback

- Notes
  - this fixes the main reopen path for AE2 terminals without hard-linking RTS
    against AE2 classes
  - if a specific AE2 multipart host still reports a generic icon, the next
    step is an AE2 reflection helper that resolves the mounted part item from
    the clicked face

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS

[2026-03-29] Sophisticated Storage remote-menu compat + detailed project refresh
- User request
  - investigate why remote-opening Sophisticated Storage barrels still only
    flashed briefly and returned to the RTS screen
  - search the upstream mod source before deciding on a fix
  - update `detailed_project.txt` from the `2026-03-28 15:30` onward journal
    history

- Upstream finding
  - official `1.21.x` Sophisticated Storage source opens barrels through
    `BarrelBlock#useWithoutItem(...) -> player.openMenu(...)`
  - the resulting `StorageContainerMenu#stillValid(...)` does not rely on
    `ContainerLevelAccess`; it directly checks player distance to the storage
    block each tick
  - that explains why RTS' generic relaxed `ContainerLevelAccess` patch is not
    sufficient and why the issue is concentrated in Sophisticated Storage

- Changes made
  - `compat/sophisticatedstorage/RtsSophisticatedStorageCompat.java`
    - added a dedicated remote-menu wrapper for Sophisticated Storage menus
    - wrapper keeps remote-opened menus valid without reintroducing player
      teleport / position spoof behavior
  - `server/RtsStorageManager.java`
    - remote menu handoff now routes opened menus through the Sophisticated
      Storage compat wrapper before validation relax hooks
  - `client/ClientRtsController.java`
    - client remote-menu handoff now mirrors the same wrapper path
    - current container screen menu reference is remapped to the wrapped menu
      so client-side validity checks do not immediately close it
  - `detailed_project.txt`
    - refreshed the project map to reflect all journaled changes since
      `2026-03-28 15:30`, including:
      - no-teleport remote-menu behavior
      - AE2 write-back hardening
      - fluid shape placement
      - GUI binding face/icon persistence
      - Sophisticated Storage remote-menu compat

- Build check
  - Ran `./gradlew.bat build`
  - Result: SUCCESS

[2026-04-05] Forge 1.20.1 sister project bulk migration to buildable state
- User request
  - stop planning and push the Forge `1.20.1` sister project through a serious
    end-to-end migration pass
  - search/document where necessary, modularize the work, and keep going until
    the port reaches a meaningful checkpoint

- Sister project targeted
  - `sister-projects/rtsbuilding-forge-1.20.1`

- Major migration work completed
  - copied the current mainline RTS source/resources into the Forge sister
    workspace
  - kept the already-created Forge MDK + Gradle + JEI + mixin scaffold and
    continued from that baseline
  - replaced the NeoForge payload registration layer with a Forge `SimpleChannel`
    implementation in `network/RtsPayloadRegistrar.java`
  - added local compatibility shim classes for `CustomPacketPayload`,
    `StreamCodec`, `RegistryFriendlyByteBuf`, `IPayloadContext`,
    `PacketDistributor`, `ModList`, and `FMLPaths` so the wider codebase could
    be ported incrementally instead of rewritten all at once
  - rewrote Forge bootstrap / event registration in `RtsbuildingMod.java`
  - ported client event-driven files such as:
    - `client/ClientInputHandler.java`
    - `client/ClientKeyMappings.java`
    - `client/RtsBoundaryRenderer.java`
    - `client/RtsCameraRenderSync.java`
    - `client/RtsClientInputGate.java`
  - ported `server/data/PlacedBlockTrackerData.java` to Forge/1.20.1 `SavedData`
  - ported crafting preview / craft execution code away from `CraftingInput`
    and onto `TransientCraftingContainer`
  - ported JEI transfer handling to JEI `15.x` `RecipeType<CraftingRecipe>`
  - replaced NeoForge item/fluid capability access in `RtsStorageManager.java`
    with Forge capability lookups / `FluidUtil`
  - updated AE2 compat to resolve node hosts reflectively through
    `appeng.api.networking.GridHelper#getNodeHost(...)` instead of NeoForge
    block-capability APIs
  - fixed a long tail of 1.21.x -> 1.20.1 API differences, including:
    - `ItemStack.isSameItemSameTags(...)`
    - old screen scroll signatures
    - old crafting/result helper signatures
    - old liquid placement signatures
    - Aqua Affinity based underwater mining penalty handling

- Validation
  - Ran `./gradlew.bat compileJava`
  - Result: SUCCESS
  - Ran `./gradlew.bat build`
  - Result: SUCCESS
  - Output jar:
    - `sister-projects/rtsbuilding-forge-1.20.1/build/libs/rtsbuilding-0.1.0-1.20.1-snapshot.jar`

- Remaining known follow-up
  - runtime validation with a real Forge 1.20.1 mod set is still needed,
    especially for AE2 / Sophisticated Storage / FTB integration paths
  - the sister branch still has a couple of `ResourceLocation` constructor
    deprecation warnings that are safe but should be cleaned later

[2026-04-05] Forge sister runtime module-conflict fix
- User report
  - launching the Forge `1.20.1` sister jar in HMCL failed with:
    - `java.lang.module.ResolutionException`
    - `Modules rtsbuilding and minecraft export package net.minecraft.network ...`

- Root cause
  - the port had shipped temporary compatibility shims inside
    `net.minecraft.network.*`
  - Java's module layer rejects split packages between the RTS mod module and
    the core Minecraft module

- Fix made
  - moved shim classes out of official package names and into RTS-owned
    package space:
    - `com.rtsbuilding.rtsbuilding.forgecompat.network.*`
    - `com.rtsbuilding.rtsbuilding.forgecompat.fml.*`
  - updated all packet payload, compat, and helper imports to the new package
    names

- Validation
  - Ran `./gradlew.bat compileJava`
  - Result: SUCCESS
  - Ran `./gradlew.bat build`
  - Result: SUCCESS

[2026-04-05] JEI 1.20.1 rollback + runtime-compatible clickable ingredient path
- User report
  - pressing JEI recipe/focus hotkeys in-game crashed with:
    - `NoSuchMethodError`
    - missing `IIngredientManager.createClickableIngredient(Object, Rect2i, boolean)`
  - runtime JEI version in the pack was `jei-1.20.1-forge-15.8.0.11`

- Root cause
  - the Forge sister project was compiled against a newer JEI `15.x` build
    (`15.20.0.105`)
  - RTS JEI overlay handlers directly invoked a clickable-ingredient helper
    signature that is not stable across JEI `1.20.1` minor versions

- Changes made
  - `compat/jei/RtsJeiIngredientCompat.java`
    - added reflection-based runtime dispatch for
      `createClickableIngredient(...)`
    - supports rollback-friendly probing instead of hard-linking one JEI
      signature
  - `compat/jei/RtsCraftTerminalJeiGuiHandler.java`
    - switched clickable ingredient lookup to the compat helper
  - `compat/jei/RtsOverlayJeiGlobalGuiHandler.java`
    - switched clickable ingredient lookup to the compat helper
  - `gradle.properties`
    - lowered JEI dev dependency baseline to `15.8.0.11`

- Validation
  - Ran `./gradlew.bat build --refresh-dependencies`
  - Result: SUCCESS

[2026-04-20] Builder UI overhaul, quick-build panel, chunk curtain toggle
- Scope
  - replaced the old text-forward builder top bar flow with a more compact
    icon-first layout
  - integrated the approved quick-build shape icons and persistent builder UI
    state
  - reworked chunk boundary display into a dedicated toggleable blue curtain
    overlay

- UI changes made
  - `BuilderScreen`
    - converted the top-row right-click action controls to icon buttons for:
      - `Interact`
      - `Link`
      - `Funnel`
      - `Rotate`
    - replaced the former `FTB check` text button with a checkmark icon button
      bound to `detectQuestsNow()`
    - added a `Quick Build` button that opens a right-side panel
    - added a new chunk-visibility icon button
    - added a far-right gear icon button for system-style controls
    - added custom icon drawing helpers for quest detect, chunk curtain, and
      gear visuals
  - quick-build panel
    - uses a 2x3 shape grid with pixel-art icons for:
      - `block`
      - `line`
      - `square`
      - `wall`
      - `circle`
      - `box`
    - keeps `fill / hollow / skeleton` as text radio options
    - uses text-based rotation control with degree readout
    - persists the user's last selected shape/fill/rotation/open state

- New client persistence
  - added `client/RtsClientUiStateStore.java`
  - persisted data now includes:
    - selected build shape
    - fill mode
    - rotation
    - quick-build panel open state
    - chunk curtain visibility
  - storage file:
    - `config/rtsbuilding-client-ui.json`

- Chunk display rework
  - `ClientRtsController`
    - added `chunkCurtainVisible`
  - `RtsBoundaryRenderer`
    - removed the previous chunk boundary presentation from the active path
    - now renders translucent blue filled vertical chunk planes when enabled
    - keeps the existing red drag limit / linked storage highlight logic

- Assets / generated artifacts
  - copied the accepted quick-build icon set into:
    - `src/main/resources/assets/rtsbuilding/textures/gui/quickbuild/`
  - generated a visual concept snapshot for the current direction:
    - `staging/ui-concepts/current_ui_concept.png`

- Validation
  - Ran `.\gradlew.bat compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat build`
  - Result: SUCCESS

- Version
  - bumped `mod_version` from `1.0.8-alpha` to `1.0.9-alpha`

[2026-04-20] Count overlay formatting follow-up
- Scope
  - tightened the item-count overlay so slot counts stay readable without
    spilling out of compact RTS UI slots

- Changes made
  - `RtsClientUiUtil`
    - added a shared slot count overlay renderer
    - reduced rendered count size via scaled text draw
    - changed count compaction to short-form `K / M / B` output with an
      approximately `3` significant-digit target
  - `BuilderScreen`
    - switched slot count overlays to the shared utility path
  - `RtsClientInputGate`
    - switched overlay quickbar / grid count rendering to the shared utility
      path
  - `RtsCraftTerminalScreen`
    - switched linked-panel count overlays to the shared utility path

- Count display rules
  - `< 1000`
    - show the full integer
  - `>= 1000`
    - use suffixes:
      - `K`
      - `M`
      - `B`
    - keep the text short enough for slot overlays by favoring about `3`
      significant digits

- Examples
  - `999 -> 999`
  - `1200 -> 1.2K`
  - `9876 -> 9.88K`
  - `12500 -> 12.5K`
  - `256000 -> 256K`
  - `1542000 -> 1.54M`

[2026-05-21] Cross-dimension linked storage persistence + camera distance resilience
- Scope
  - prevent RTS storage bindings, link modes, quick references, GUI bindings,
    and related session data from being lost when the player changes dimension
  - reduce RTS camera/session loss when movement reaches distance or chunk
    tracking edges
  - install the missing mod-development dependency cache under the project on
    E: instead of the default user cache path

- Dependency setup
  - used the project Gradle Wrapper with:
    - `GRADLE_USER_HOME=E:\RTSbuilding\.gradle-user-home`
  - downloaded Gradle `9.2.1`, NeoForge `21.1.219`, ModDevGradle runtime,
    JEI compile/runtime artifacts, Minecraft/NeoForm artifacts, and related
    toolchain jars into the E: project cache
  - `.gitignore`
    - added `.gradle-user-home/` so the local E: dependency cache stays out of
      source control

- Storage persistence changes
  - `server/RtsStorageManager.java`
    - replaced the single-session `linkedDimension` model with
      `LinkedStorageRef(dimension, pos)`
    - changed linked names and link modes to key by the full storage ref
    - added persistent `linked_entries` NBT records:
      - dimension
      - pos
      - mode
    - kept legacy `linked_positions`, `linked_modes`, and `linked_dimension`
      read/write support so existing saves migrate cleanly
    - changed dimension sanitizing so it removes invalid in-memory refs only;
      it no longer clears all links just because the player is in another
      dimension
    - resolver paths now activate only refs in the player's current dimension,
      preventing accidental same-coordinate access in the wrong level
    - storage-page linked-position payload now includes only current-dimension
      refs for client highlight rendering while preserving all saved refs
    - breaking a linked storage removes only the current-dimension ref for
      that block, instead of relying on position-only identity

- Camera resilience changes
  - `server/RtsCameraManager.java`
    - stores the last authoritative camera position in the server session
    - searches all server levels when discarding a camera entity
    - recreates the RTS camera entity from session state if the entity is
      missing, instead of immediately stopping RTS mode
    - resyncs `S2CRtsCameraStatePayload` after restoration
  - `RtsbuildingMod.java`
    - increased RTS camera entity client tracking range from `32` to `128`

- Validation
  - Ran `.\gradlew.bat --no-daemon dependencies`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS

[2026-05-23] Hotbar-aware counts, square build range, F3 chunk overlay, text guide tabs, and Box mouse-height
- Scope
  - quick slots / recent slots displayed zero when the item existed only in
    player hotbar slots 1-9
  - bottom guide topic icons were too abstract
  - highest range tier was described as 7x7 chunks but circular checks cut off
    edge/corner building positions
  - chunk overlay was hard to read underground
  - Box quick-build height needed wall-like mouse adjustment instead of a
    separate third point workflow

- Server changes
  - `RtsStorageManager.java`
    - inventory summary now includes player slots 0-35, including hotbar
  - `RtsCameraManager.java`
    - added square action range checks and square camera clamp
    - effective half-extent is radius + 8 blocks for full chunk-tier coverage
  - `RtsProgressionManager.java`
    - home radius uses the same square chunk-range model

- Client changes
  - `RtsBoundaryRenderer.java`
    - Chunk View now renders full-height F3-style chunk boundaries
  - `BuilderScreen.java`
    - bottom guide uses text topic tabs instead of icon-only tabs
    - Box shape enters preview after A/B and height follows mouse up/down
  - `en_us.json`
  - `zh_cn.json`
    - updated Box status text

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS

[2026-05-23] Chunk pillars, mining-tool UI refresh, durability text, and overlay sizing
- Scope
  - Chunk View still did not clearly separate every chunk boundary underground
  - linked storage tools temporarily disappeared after mining until the storage
    search/page refreshed
  - selected damageable tools did not show durability
  - storage overlay header still had redundant `RTS` text and felt too large
  - remote container screens could briefly lose the race to BuilderScreen
    restoration again

- Client
  - `RtsBoundaryRenderer.java`
    - added F3-like yellow full-height pillars at chunk corners
  - `BuilderScreen.java`
    - selected damageable item status includes durability
  - `ClientRtsController.java`
    - refreshes selected item preview from storage page data
    - remote menu open grace increased to 80 ticks
  - `RtsClientInputGate.java`
    - removed overlay `RTS` label
    - reduced storage/craft overlay dimensions and slot size
    - added `Craft +/-` collapse toggle

- Server
  - `RtsStorageManager.java`
    - resends storage page after linked mining tools are refunded

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS

[2026-05-23] Remote mining uses RTS-selected linked tools
- Scope
  - selecting a tool from linked storage or RTS quick slots did not affect
    mining
  - Mine / Ultimine only sent the player hotbar slot, so the server treated
    storage-selected tools as empty hand when the player hotbar slot was empty

- Network
  - extended `C2SRtsMinePayload` and `C2SRtsUltiminePayload` with `toolItemId`
  - client sends the current RTS-selected non-block item id for mining starts

- Client
  - `ClientRtsController`
    - sends selected storage/quick-slot tools for Mine / Ultimine
    - ignores selected block items so building materials are not used as tools

- Server
  - `RtsStorageManager`
    - extracts one matching linked tool on mining start
    - uses that tool for destroy-speed calculation and actual block destruction
    - reuses it through the whole Ultimine chain
    - refunds the damaged/remaining tool on completion or abort

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS
- Remaining warnings are existing NeoForge `EventBusSubscriber.Bus`
  deprecation warnings.

[2026-05-23] RTS Progression tree panning
- Scope
  - skill tree had zoom but no way to shift focus by dragging
  - zoomed content felt awkward because the viewport focus was fixed

- Client UI
  - `client/RtsProgressionScreen.java`
    - added `panX` / `panY` virtual canvas offsets
    - left-click dragging inside the tree viewport now pans the tree
    - unlock badge clicks still take priority over pan start
    - pan is clamped against current zoom, virtual content size, and viewport
      size
    - clamp includes a bounded margin so the user can focus edge nodes without
      dragging the whole tree completely out of view
    - zoom changes reclamp pan so existing focus remains valid

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS

[2026-05-23] Gemini icon redraw, link-mode tutorial correction, and bottom overlap fix
- Scope
  - tutorial directory icons were still using older hand-drawn/fallback glyphs
  - Chunk View / Quick Build / Ultimine topbar icons were not clear enough
  - Storage Link tutorial text did not match actual left/right click behavior
  - bottom panel search/craft areas could overlap

- Gemini icon work
  - requested a new Gemini image concept sheet for:
    - Quick Build
    - Ultimine
    - Chunk View
  - generated:
    - `staging/gemini-topbar-icons-v2/quick_ultimine_chunk_sheet.png`
  - chopped the sheet into 24x24 topbar textures:
    - `quick_build_{inactive,hover,active,pressed}.png`
    - `ultimine_{inactive,hover,active,pressed}.png`
    - `chunk_view_{inactive,hover,active,pressed}.png`

- Client UI
  - `client/BuilderScreen.java`
    - wired the new texture states into `topbarModeTexture(...)`
    - guide topic icons now use the current topbar textures for Interact,
      Link, Funnel, Rotate, Quick Build, Ultimine, Chunk View, and Gear
    - fallback pixel icons remain for non-topbar bottom/settings guide topics

- Storage Link tutorial correction
  - verified actual code behavior:
    - left click in Storage Link mode sends bidirectional link
    - right click in Storage Link mode sends extract-only link
  - updated English and Chinese guide text accordingly
  - asked Gemini to review the actual snippets and confirm concise wording

- Bottom/craft layout
  - widened the craft panel from 106 to 126 px
  - widened craft apply/toggle controls
  - recalculated main search width from the main storage area instead of the
    full storage width so the search clear button and pager no longer collide
    with the craft panel
  - updated the container overlay and JEI extra-area dimensions to match the
    wider craft panel

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS

[2026-05-23] Compact guide directory and top-bar cleanup
- Scope
  - user wanted the FTB button hidden unless the relevant FTB mod exists
  - Quick Build / Ultimine were too much like wide text labels, not real
    buttons
  - guide panels needed to become smaller, attached, icon-topic based tutorials
    rather than large paragraph popups
  - Gemini was consulted for tutorial grouping and practical beginner copy

- Gemini consultation
  - Used Gemini 2.5 Flash because the requested 3.x model ids were not
    available in the current API endpoint
  - Main advice adopted:
    - group top topics by modes, build tools, information/settings
    - group bottom topics by storage browsing and utility functions
    - use short 2-4 line topic copy instead of long pages
    - avoid modal overload and keep the panel attached to the related UI

- Client UI
  - `client/BuilderScreen.java`
    - FTB quest detect button is now shown only if FTB quest/library mods are
      loaded
    - Quick Build and Ultimine are now compact icon buttons
    - added custom pixel icons for Quick Build and Ultimine fallback rendering
    - guide panel no longer darkens the whole screen
    - top guide appears below the top bar
    - bottom guide appears just above the storage panel
    - settings guide appears beside the settings modal when possible
    - guide panel now uses a left-side icon directory; clicking an icon switches
      to that topic
    - topics are concise, practical, beginner-facing explanations

- Localization
  - `en_us.json` and `zh_cn.json`
    - added topic titles and short topic lines for:
      - Interact
      - Storage Link
      - Funnel
      - Rotate
      - Quick Build
      - Ultimine
      - Chunk View
      - Search/Categories
      - Stored Items
      - Recent Items
      - Fluids
      - Pins/Hotbar
      - Craft/Remote GUI
      - Sensitivity
      - Auto-store
      - Server Config

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS

[2026-05-23] Builder tutorial pagination, localized controls, and fixed RTS UI scale
- Scope
  - make the top/bottom `i` help panels work as real beginner tutorials
  - reduce help panel size and add paging so long text does not collide with
    bottom UI
  - localize the most visible BuilderScreen status/settings strings into
    English and Simplified Chinese
  - make settings controls behave like their visuals
  - make the RTS screen draw and receive mouse input as if Minecraft GUI scale
    were 3

- Tutorial UI
  - `client/BuilderScreen.java`
    - replaced one-page top/bottom help with paged guide content
    - added `Prev` / `Next` navigation and page indicator
    - added a third guide context for the gear/settings modal
    - guide panel is smaller and positioned higher to reduce awkward overlap
      with the storage panel
  - `en_us.json` / `zh_cn.json`
    - expanded top-bar tutorial to 18 entries
    - expanded bottom-bar tutorial to 14 entries
    - added settings tutorial entries
    - explicitly documents:
      - normal right-click interaction with mobs/machines/blocks
      - Storage Link left-click binding behavior
      - Storage Link right-click suppression
      - placement fallback behavior
      - ultimine completion behavior

- Settings modal
  - gear panel now has its own `i` button
  - Sensitivity is now a clickable/draggable slider backed by stable presets
  - Auto-store can be toggled by clicking the row or switch
  - spacing/alignment between Sensitivity and Auto-store was increased
  - settings labels and ON/OFF are localized

- Builder localization
  - localized the visible status row:
    - mode label
    - selected item/fluid
    - storage linked/not linked
    - auto-store state
    - funnel/shape/ultimine/fill/rotation/undo state
  - localized home selection overlay, quick-build panel, ultimine panel,
    storage title/category, and common tooltips

- Fixed RTS UI scale
  - `BuilderScreen` now renders through a virtual GUI-scale-3 coordinate layer
  - mouse click/drag/scroll coordinates are mapped into the same virtual layer
  - this keeps RTS UI size stable even when the user's Minecraft GUI scale is
    not 3

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS
  - Remaining warnings are existing NeoForge `EventBusSubscriber.Bus`
    deprecation warnings.

[2026-05-23] Reworked Chunk View into an x-ray flat grid
- Scope
  - the full-height chunk pillar version was too thick and visually noisy
  - underground chunk boundaries needed to stay visible through terrain

- Client rendering
  - `client/RtsBoundaryRenderer.java`
    - removed the vertical pillar/curtain guide style
    - restored the idea of a flat chunk boundary grid
    - the grid is anchored at the RTS anchor Y level
    - center chunk lines are yellow; surrounding chunk lines are cyan/blue
    - chunk guide lines render through blocks by using a separate line buffer
      with depth testing disabled only for the chunk grid pass
    - normal RTS outlines and previews still use ordinary depth behavior

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS
  - Produced `build/libs/rtsbuilding-build-from-above-1.0.1-beta.jar`
  - Verified the jar contains:
    - `assets/rtsbuilding/lang/en_us.json`
    - `assets/rtsbuilding/lang/zh_cn.json`
    - `assets/rtsbuilding/lang/zh_tw.json`
    - `assets/rtsbuilding/lang/zh_hk.json`

  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS
  - Produced `build/libs/rtsbuilding-build-from-above-1.0.1-beta.jar`
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS

[2026-05-24] Added world-save RTS session snapshots
- Request
  - Keep RTS settings, recent entries, bookmarks, and related state in a
    dedicated place inside the world save so a client/game crash is less likely
    to lose the latest RTS UI/session changes.

- Persistence
  - Added `server/data/RtsStorageSessionStore.java`
    - stores compressed NBT at:
      - `<world>/rtsbuilding/storage_sessions.dat`
    - stores sessions under player UUID strings
    - writes through a temp file before replacing the real file
  - `RtsStorageManager`
    - loads this world snapshot first
    - falls back to legacy player persistent NBT when no snapshot exists
    - keeps writing player persistent NBT for compatibility

- Newly persisted RTS state
  - storage page, search, category, sort, order, and auto-store
  - craft search/show-unavailable/requested batch state
  - linked storage refs/modes
  - internal fluids
  - recent entries
  - quick-slot/bookmark ids
  - GUI bindings and icon ids
  - client-local input sensitivity now persists in the existing RTS UI config

- Save trigger changes
  - `requestPage(...)` now snapshots page/search/category/sort state
  - `requestCraftables(...)` snapshots craft-panel state
  - auto-store toggles save immediately
  - main-hand RTS use/placement recent entries save before early return
  - craft-result refill recent entries save when recorded
  - BuilderScreen restores and writes sensitivity along with the existing
    local RTS UI state

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS

[2026-05-24] Reworked Chunk View into edge-cell highlights
- Request
  - Stop fighting the broken flat/long-line chunk overlay.
  - Use block-style highlights instead:
    - highlight each chunk's X/Z perimeter cells
    - do not tint/fill blocks
    - allow air-cell highlights when a whole perimeter column is void
    - keep chunk colors easy to distinguish.

- Implementation
  - `client/RtsBoundaryRenderer.java`
    - removed the previous anchor-height continuous chunk guide lines
    - renders a no-depth outline box for each loaded chunk perimeter cell
    - alternates chunk color between cyan-blue and yellow by chunk coordinate
    - uses `WORLD_SURFACE` heightmap to outline the top visible solid-ish block
      in each perimeter column
    - falls back to an anchor-Y air-cell outline for truly empty/void columns
    - increased the chunk-guide render buffer for many small line boxes

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS
  - Produced `build/libs/rtsbuilding-built-from-above-1.0.11-alpha.jar`
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS
  - Produced `build/libs/rtsbuilding-built-from-above-1.0.11-alpha.jar`
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS

[2026-05-23] Added stale screenless-menu recovery for Create Ponder / JEI sequence
- Problem
  - User reported that in RTS mode, after opening Create's W/Ponder flow and
    then opening R/recipe, the client could end up showing only the bottom
    experience bar until RTS was exited and re-entered.

- Likely root cause
  - External non-container screens can interrupt the RTS screen handoff.
  - The client can be left with a non-zero `player.containerMenu` but
    `minecraft.screen == null`.
  - Existing logic treats a non-zero menu as a real remote menu and therefore
    refuses to restore `BuilderScreen`, leaving only vanilla HUD remnants.

- Fix
  - `ClientRtsController` now counts consecutive ticks where RTS is enabled,
    `minecraft.screen` is null, and a non-zero container menu is still present
    outside the remote-open grace window.
  - After 10 ticks it closes that stale client container, clears remote-menu
    compatibility state, and lets the normal BuilderScreen restoration path
    recover the RTS panel.
  - The existing 80-tick remote-menu grace path still protects legitimate
    chest/machine opening flows.

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS

[2026-05-23] Fixed chunk grid render stage instead of changing geometry again
- Problem
  - User screenshots showed cyan chunk lines turning into screen-like
    horizontal and diagonal streaks.
  - The flat grid geometry was not the main failure at this point; it was being
    drawn from the wrong render stage.

- Root cause
  - `RtsBoundaryRenderer` rendered Chunk View in `AFTER_LEVEL`.
  - NeoForge fires that stage after `LevelRenderer.renderLevel` has finished
    and the world model-view stack has already been popped/restored.
  - World-space coordinates drawn there can be projected with the wrong matrix,
    producing the broken diagonal/horizon-line appearance.

- Fix
  - Moved Chunk View back into `AFTER_TRANSLUCENT_BLOCKS`, matching the working
    red RTS boundary pass.
  - Kept Chunk View in a separate no-depth draw pass so terrain should not hide
    the flat cyan chunk grid.
  - Removed `VIEW_OFFSET_Z_LAYERING` from the custom chunk RenderType to avoid
    camera-space line shifting.
  - Increased the chunk line buffer to 8192 bytes for the visible multi-chunk
    grid.

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS

[2026-05-23] Fixed Chunk View depth mode and remote GUI flash-close path
- Scope
  - Chunk View still failed to show through blocks because the previous
    depth-disable call was outside `RenderType.draw`, where `RenderType.lines`
    re-applied its own depth state
  - right-click GUI opening could flash and close because RTS screen restoration
    competed with vanilla container-screen handoff

- Chunk View
  - `client/RtsBoundaryRenderer.java`
    - added a dedicated `CHUNK_XRAY_LINES` RenderType
    - uses `NO_DEPTH_TEST` inside the RenderType state itself
    - uses cyan for all chunk grid lines and removes the yellow center-chunk
      coloring
    - keeps the flat RTS-anchor-Y grid presentation

- Remote GUI
  - `client/ClientRtsController.java`
    - added an explicit remote GUI handoff variant of `beginRemoteMenuOpenGrace`
    - GUI-style interactions now close BuilderScreen before the C2S interaction
      packet is sent, giving vanilla screen creation a clean path
    - removed the tick-time `minecraft.setScreen(null)` fallback when a remote
      container is detected
  - `compat/sophisticatedstorage/RtsSophisticatedStorageCompat.java`
    - now tracks generic remote menu IDs separately from Sophisticated Storage
      IDs
    - exposes a generic still-valid force check for any RTS-opened remote menu
  - `mixin/RemoteContainerMenuMixin.java`
    - injects into `AbstractContainerMenu.stillValid(ContainerLevelAccess, Player, Block)`
      and returns true for currently marked RTS remote menus
  - `server/RtsStorageManager.java`
    - marks every successfully opened remote menu for generic validation bypass
    - preserves validation state while the marked container menu remains open

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS
  - Short foreground `runClient` reached resource loading without mixin apply
    errors before the tool timeout stopped it.

[2026-05-23] Restored remote GUI handoff to the known 1.0.8/1.0.9 baseline
- Investigation
  - reviewed older journal entries around:
    - 2026-03-20 remote GUI first-open handoff stabilization
    - 2026-03-28 1.0.8-alpha GUI binding / remote-menu work
    - 2026-03-29 Sophisticated Storage remote-menu compat
    - 2026-04-02 Sophisticated Storage follow-up
  - compared the current code against the sister 1.20.1 project, which still
    preserves the mature 1.0.8/1.0.9 remote-menu structure

- Finding
  - the previous fix attempt diverged from the known-good path by:
    - closing BuilderScreen before sending GUI-style interaction packets
    - adding a generic `AbstractContainerMenu` still-valid mixin
    - tracking generic remote menu ids in the Sophisticated Storage compat
      helper
  - the known-good path relies on:
    - a pending remote-menu grace window
    - tick-time detection of an actual non-player `containerMenu`
    - clearing BuilderScreen only after the menu exists
    - reflection-based validation relaxation for ordinary menus
    - Sophisticated Storage-specific tracked-id mixin bypass only for that mod

- Changes
  - deleted `mixin/RemoteContainerMenuMixin.java`
  - removed its registration from `rtsbuilding.mixins.json`
  - reverted `RtsSophisticatedStorageCompat` to Sophisticated-only remote menu
    tracking
  - reverted `RtsClientRemoteMenuCompat.install(...)` to mark client remote
    state only for Sophisticated menus
  - reverted all GUI-style interaction calls to plain
    `beginRemoteMenuOpenGrace()`
  - restored `ClientRtsController.tick()` clearing BuilderScreen only when a
    real remote container menu is already present
  - restored `RtsStorageManager` remote validation cleanup to the 1.0.8/1.0.9
    pattern

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS

[2026-05-23] Added Create/Sophisticated Storage/Pipez to development runClient
- Scope
  - the normal RTS development client should load a realistic tech/storage
    testing set for compatibility work and future showcase recording
  - requested mods:
    - Create
    - Sophisticated Storage
    - Pipez

- Gradle setup
  - `build.gradle`
    - added Create Maven, ithundxr snapshots Maven, and Modrinth Maven
    - added runtime-only test dependencies through `localRuntime`
    - wired `localRuntime` into `additionalRuntimeClasspath` so ModDevGradle
      includes external mod jars in the generated 1.21.1 run classpath
  - `gradle.properties`
    - pinned versions for Create, Ponder, Flywheel, Registrate,
      Sophisticated Core, Sophisticated Storage, and Pipez

- Dependency policy
  - these mods are loaded for dev `runClient`/`runServer` testing only
  - the published RTS building jar does not require Create, Sophisticated
    Storage, or Pipez

- Validation
  - Ran `.\gradlew.bat --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --no-daemon --no-configuration-cache writeClientLegacyClasspath --rerun-tasks`
  - Result: SUCCESS
  - Verified `build/moddev/clientLegacyClasspath.txt` includes the requested
    test mods and their Create dependencies.

[2026-05-23] Replaced failed hand-redraw with Gemini reference-style chops
- Scope
  - v3 hand-rebuilt Quick Build / Ultimine / Chunk View icons did not match the
    established topbar art direction
  - desired style is the existing Interact / Link / Funnel / Rotate spec:
    dark beveled button, white icon subject, blue hover, green active, brown
    pressed

- Asset workflow
  - created `staging/gemini-topbar-icons-v4-style-reference.png` from the
    existing four topbar mode icon sets
  - extended the Gemini helper to accept `RTS_GEMINI_REFERENCE_IMAGE`
  - regenerated a reference-style sheet with Gemini using the existing icons as
    visual style input
  - chopped full button cells directly from the Gemini sheet:
    - Quick Build from column 1
    - Ultimine from column 3
    - Chunk View from column 4
  - replaced all 12 runtime topbar textures for those three buttons

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS

[2026-05-23] Safe icon rebuild, bottom tutorial pass, and RTS craft hotkey
- Scope
  - Quick Build / Ultimine / Chunk View generated atlas crops were visibly wrong
  - bottom guide needed to explain the real left-to-right UI controls:
    Qty/Mod/Name, S, A/D, +/-, C, remote GUI slots, main storage, hotbar,
    fluid strip/tanks, recent items, quick slots 1-27, and craft panel
  - unlocked RTS Crafting needed a direct `C` keyboard shortcut

- Assets
  - improved the Gemini icon helper with:
    - `RTS_GEMINI_MODELS_OVERRIDE`
    - `RTS_GEMINI_IMAGE_SIZE`
  - generated one Pro concept sheet and one Flash/Nano-Banana-2-style concept
    sheet for Quick Build, Ultimine, and Chunk View
  - used the clearer Flash concept as reference, then rebuilt the final
    in-game textures as native 24x24 pixel buttons to avoid crop/padding bugs

- Client UI
  - `BuilderScreen.java`
    - `C` opens RTS Crafting when Craft Terminal is unlocked and search is not
      focused
    - bottom guide topics now follow the actual panel layout and explain the
      controls in practical terms
    - added a SORT guide glyph for the topic selector

- Localization
  - `en_us.json`
  - `zh_cn.json`
  - added English and Chinese copy for the new bottom-guide topics

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Remaining warnings are existing NeoForge `EventBusSubscriber.Bus`
    deprecation warnings.

[2026-05-22] Home relocation gate, hidden locked RTS panels, and skill-tree viewport polish
- Scope
  - make the RTS Progression tree less cramped and prevent zoomed content from
    drawing outside the tree area
  - hide survival-progression features from BuilderScreen panels until their
    corresponding nodes are unlocked
  - replace the 10-day home relocation cooldown idea with an expensive
    progression node
  - add clearer in-game guidance for the top mode bar and bottom storage bar

- Gemini design consultation
  - attempted `gemini-3.1-pro` and `gemini-3.1-flash`; both model ids were not
    available through the current API endpoint
  - used `gemini-2.5-flash` for technical UI advice
  - adopted the suggested viewport approach:
    - draw the skill tree inside a virtual canvas
    - clip nodes and dependency lines with `GuiGraphics.enableScissor(...)`
    - keep the panel frame and zoom bar outside the clipped layer

- Progression / home rules
  - `progression/RtsProgressionNodes.java`
    - added `home_relocation`
    - cost is 10 netherite blocks
    - no dependencies, so the player can pursue it directly if they can pay
  - `server/RtsProgressionManager.java`
    - the first home can still be set freely
    - changing an existing home now requires the `home_relocation` node
    - the previous cooldown path now reports zero remaining ticks
  - `client/RtsHomeScreen.java`
    - explains starter-home importance, radius restriction, and expensive
      relocation requirement
    - disables the change-home button until relocation is unlocked

- Builder UI
  - `client/BuilderScreen.java`
    - top mode buttons now only show unlocked feature groups
    - quick-build and ultimine floating panels no longer render or accept clicks
      before their nodes are unlocked
    - craft terminal button hides until `craft_terminal` is unlocked
    - remote GUI binding buttons hide until `remote_gui` is unlocked
    - top bar gained an info button for mode/build-flow guidance
    - bottom storage bar gained a small info button for storage/crafting/pin
      guidance
    - gear menu click bounds now match the centered modal's visual bounds

- Progression UI
  - `client/RtsProgressionScreen.java`
    - panel is larger
    - tree content is clipped to an inner viewport
    - zoom range widened to 45%-235%
    - node cards are smaller and less dominant relative to dependency lines
    - zoom affects spacing more than card size, making relationships easier to
      inspect

- Localization
  - `en_us.json` and `zh_cn.json`
    - added Home Relocation node strings
    - added RTS Home warning/status strings
    - added English and Chinese guide text for top and bottom RTS UI bars

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS
  - Produced/updated:
    - `build/libs/rtsbuilding-built-from-above-1.0.10-alpha.jar`
  - Remaining warnings are existing NeoForge `EventBusSubscriber.Bus`
    deprecation warnings.

[2026-05-22] Survival progression fluid/shared backend + RTS top-bar mode icon redraw
- Scope
  - finish the rough survival-balance backend pieces around fluid capacity and
    shared/team progression
  - redraw the four compact top-bar mode icons for Interact / Link / Funnel /
    Rotate without changing the surrounding UI layout

- Survival/shared progression changes
  - `Config`
    - added `shareSurvivalProgressionWithTeams`
    - default remains `false`
    - option only matters when survival progression is enabled
  - `compat/ftb/RtsFtbCompat`
    - split FTB Teams lookup from FTB Quests lookup
    - exposes a reflection-based team key without hard depending on FTB Teams
  - `compat/ftb/RtsFtbTeamsCompatImpl`
    - added reflection-only FTB Teams key resolver
    - supports common id/uuid getter names and falls back safely
  - `server/data/RtsSharedProgressionData`
    - added world `SavedData` root `rtsbuilding_shared_progression`
    - stores shared unlocked nodes and optional shared home anchor
  - `RtsProgressionManager`
    - personal progress remains the default
    - when sharing is enabled, FTB Teams is preferred and vanilla scoreboard
      teams are fallback
    - existing personal unlocked nodes merge into the shared record on login
    - existing personal home can initialize an empty shared home record
    - unlock/home changes sync to related online teammates

- Fluid capacity changes
  - `RtsStorageManager`
    - internal fluid capacity now uses
      `RtsProgressionManager.getFluidCapacityBuckets(player)`
    - `fluid_buffer` currently provides the 100 bucket capacity through the
      progression effect path
    - saved internal fluids are no longer clamped away on load
    - new insertion respects the current derived capacity

- Top-bar icon redraw
  - `BuilderScreen`
    - kept the existing compact beveled-button layout and state colors
    - Interact icon redrawn as a clearer pointer/interaction mark
    - Link icon redrawn as paired chain loops instead of four topology dots
    - Funnel icon redrawn as a tapered funnel silhouette
    - Rotate icon redrawn as a rotate-arrow orbiting a center block
  - `tools/generate_rts_mode_icons.py`
    - updated the staging icon generator to match the new top-bar icon
      language
    - regenerated:
      - `staging/ui-icon-concepts/`
      - `staging/ui-icon-final/`
      - `staging/ui-icon-states/`
  - `tools/generate_gemini_topbar_icon_concepts.py`
    - added a reproducible Gemini / Nano Banana concept-sheet generator for
      the same four top-bar icons
    - reads the API key from `GEMINI_API_KEY`
    - writes generated concepts to `staging/gemini-topbar-icons/`
  - `tools/chop_gemini_topbar_icons.py`
    - added a 4x4 sheet chopper for the accepted Gemini concept sheet
    - crops Interact / Link / Funnel / Rotate across inactive / hover /
      active / pressed rows
    - resizes the 96px source cells to 24px with nearest-neighbor sampling so
      the final in-game buttons keep hard pixel edges
    - writes 24x24 runtime textures to
      `src/main/resources/assets/rtsbuilding/textures/gui/topbar/`
    - writes a staging preview to
      `staging/gemini-topbar-icons/chopped/sheet_chopped.png`
  - `src/main/resources/assets/rtsbuilding/textures/gui/topbar/`
    - added the 16 chopped Gemini/Nano Banana top-bar mode textures
  - `BuilderScreen`
    - Interact / Link / Funnel / Rotate icon buttons now choose chopped
      texture assets by current button state
    - the previous procedural icon helpers remain as fallback code paths, but
      the four primary mode buttons no longer depend on them
  - `survival_balancing_plan.md`
    - documented implemented fluid-capacity and optional shared/team
      progression behavior
  - `detailed_project.txt`
    - documented the procedural top-bar icon drawing and regenerated preview
      assets

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build` after adding chopped
    top-bar textures
  - Result: SUCCESS
  - Remaining warnings are existing NeoForge `EventBusSubscriber.Bus`
    deprecation warnings.

[2026-05-22] Chunk grid visibility cleanup
- Scope
  - replace the ugly full-height chunk curtain visualization with a cleaner
    RTS-friendly chunk boundary guide

- Design options considered
  - full F3+G-style vertical chunk columns:
    - accurate but too visually noisy for top-down building
  - pure ground-plane tactical grid:
    - cleanest and least intrusive
  - ground-plane grid plus short posts:
    - more spatial information, but still a bit busy

- Chosen implementation
  - `client/RtsBoundaryRenderer.java`
    - `renderChunkGuides(...)` now draws exact 16-block chunk boundaries as a
      horizontal grid at the current player operation height
    - removed full-world-height blue curtain walls
    - derives visible chunk range from the current RTS max radius in chunks
    - highlights the anchor/current chunk with amber lines
    - highlights the outer visible range with stronger blue lines
    - keeps interior grid lines lower-alpha so they do not hide build targets

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS
  - Remaining warnings are existing NeoForge `EventBusSubscriber.Bus`
    deprecation warnings.

[2026-05-22] Gemini settings concept applied
- Generated
  `staging/gemini-settings-panel/settings_panel_concepts.png` with the
  Gemini settings-panel redraw prompt after allowing the external API request
- Updated `client/BuilderScreen.java` to follow the concept more closely:
  - centered `RTSBUILDING CONFIGURATION` modal
  - title bar close button
  - framed Controls section with sensitivity track and auto-store toggle
  - framed Survival Balance section with a large enabled/disabled switch
  - framed Skill Cost Editor with a left node list and right selected-node
    cost editor
  - bottom SAVE / RESET / CLOSE actions
- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS

[2026-05-22] Config ownership correction and progression cost icon display
- Scope
  - correct the settings ownership model after testing:
    - top-bar gear is for local RTS control settings only
    - survival balance and skill/material costs belong in the mod config
      screen and remain server-authoritative
  - make the RTS Progression screen show actual material icons because the
    blurred/covered world background made text-only costs hard to understand

- Top bar gear / controls
  - generated a Gemini/Nano Banana concept for the top-bar gear icon
  - chopped it into:
    - `settings_gear_inactive.png`
    - `settings_gear_hover.png`
    - `settings_gear_active.png`
    - `settings_gear_pressed.png`
  - `client/BuilderScreen.java`
    - gear button now uses topbar texture assets instead of the old hand-drawn
      square-pixel cog
    - gear popup was slimmed back down to local controls:
      - sensitivity
      - auto-store
    - removed survival balance and skill cost editing from the RTS overlay

- Mod config
  - added `client/RtsModConfigScreen.java`
    - registered via NeoForge `IConfigScreenFactory`
    - exposed survival balance on/off
    - exposed shared-team progression toggle
    - exposed editable material cost overrides for progression nodes
    - edits save to the common config
  - `RtsbuildingMod.java`
    - registers the mod config screen extension on the client

- RTS Progression screen
  - `client/RtsProgressionScreen.java`
    - no longer renders the default blurred background first
    - uses a more opaque panel
    - renders real item icons from the item registry with `xN` counts instead
      of text-only material names

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS

[2026-05-22] Rename display title and produce Built From Above artifact
- Naming decision
  - kept internal mod id as `rtsbuilding` for world/config/resource/network
    compatibility
  - English display name is now `RTS building Build From Above`
  - Simplified Chinese display name is now `俯视角建造`
- Build metadata / resources
  - `gradle.properties`
    - `mod_name=RTS building Build From Above`
    - `mod_version=1.0.10-alpha`
    - `mod_archive_name=rtsbuilding-built-from-above`
  - `build.gradle`
    - jar archive base name now reads `mod_archive_name` when present
  - `assets/rtsbuilding/lang/en_us.json`
    - updated creative tab/key category display strings
  - `assets/rtsbuilding/lang/zh_cn.json`
    - added Chinese display strings
- Validation
  - Ran `.\gradlew.bat --offline --no-daemon clean build`
  - Result: SUCCESS
  - Produced:
    - `build/libs/rtsbuilding-built-from-above-1.0.10-alpha.jar`
  - Verified generated metadata still uses:
    - `modId="rtsbuilding"`
    - `displayName="RTS building Build From Above"`
    - `version="1.0.10-alpha"`

[2026-05-22] RTS Progression skill tree + first i18n pass
- Scope
  - fix the RTS Progression screen where custom text/icons were visually
    washed out behind the screen blur while widget buttons remained readable
  - replace the list layout with a proper skill-tree/technology-tree view
  - make unlock buttons server-authoritative for material availability
  - start formal English/Simplified Chinese localization for new visible text

- Gemini design reference
  - added `tools/generate_gemini_progression_tree_concept.py`
  - generated `staging/gemini-progression-tree/progression_tree_concept.png`
  - used it as a direction reference:
    - opaque dark centered panel
    - connected node cards
    - green unlocked, amber unlockable, muted locked states
    - item icons with x-counts

- Server/network
  - `S2CRtsProgressionStatePayload`
    - now includes `unlockableNodes`
  - `server/RtsProgressionManager.java`
    - computes unlockable nodes using server-side dependencies plus
      `hasCosts(...)`
    - clients no longer enable Unlock just because dependencies are met

- Client
  - `client/ClientRtsController.java`
    - tracks synced unlockable progression nodes
  - `client/RtsProgressionScreen.java`
    - rewritten as a custom-drawn skill tree
    - no longer calls the default blurred background path
    - no longer relies on vanilla widget buttons for node unlocks
    - draws dependency lines, node cards, material item icons, and status
      badges
    - only unlockable nodes accept clicks

- Localization
  - `assets/rtsbuilding/lang/en_us.json`
    - added progression screen strings and node names/descriptions
  - `assets/rtsbuilding/lang/zh_cn.json`
    - added Chinese progression screen strings and node names/descriptions

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS

[2026-05-22] RTS Progression state clarity and zoom controls
- Scope
  - fix a confusing starter-node state where Camera Core could display as
    free/unlockable but the server reported "already unlocked" on click
  - make node states visually distinct
  - remove the unclear check-box-like node glyph
  - make the skill tree larger and add zoom controls

- Server fix
  - `server/RtsProgressionManager.java`
    - `syncToPlayer(...)` now ensures the starter node is present and saved
      before computing `unlockedNodes` / `unlockableNodes`

- Progression UI
  - `client/RtsProgressionScreen.java`
    - added four explicit node states:
      - unlocked
      - unlockable
      - missing materials
      - prerequisite locked
    - each state has separate background, border, accent bar, label, action
      badge, and connection color
    - removed ambiguous check/lock glyph; node state is now shown as text plus
      a colored side accent
    - dependency lines are thicker and higher contrast
    - default node spacing is larger
    - added a bottom zoom bar and mouse-wheel zoom support

- Localization
  - added English and Chinese strings for:
    - zoom
    - need items
    - all four node states

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS

[2026-05-22] Ultimine FTB-style chain mining behavior
- Scope
  - change RTS ultimine from a serial queued miner into the mature
    FTB-style model:
    - collect connected same-block targets around the seed block
    - highlight the collected batch while the player holds mining
    - mine only the seed block's progress bar
    - break the cached batch only when the seed block actually completes
    - abort without drops if the player releases early

- Client behavior
  - `client/BuilderScreen.java`
    - normal remote mining and ultimine both abort on left mouse release
    - while ultimine is being held, the client computes a local connected
      same-block preview using the same 6-neighbor BFS shape as the server
    - the existing shape ghost renderer is reused to show the ultimine batch
      highlight

- Server behavior
  - `server/RtsStorageManager.java`
    - removed the old serial `miningQueue` behavior for ultimine
    - `startUltimine(...)` now collects and caches the full target list, then
      starts normal remote mining on the seed block
    - if mining is aborted before completion, cached targets are cleared and
      no chained blocks are broken
    - when the seed block reaches completion, the server breaks the cached
      target set as a chained operation
    - auto-store mined drops and quest detect still run after successful
      chained mining

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS
  - Remaining warnings are existing NeoForge `EventBusSubscriber.Bus`
    deprecation warnings.

[2026-05-22] Death / respawn RTS panel handoff
- Scope
  - prevent RTS panels from staying open over the vanilla death screen or
    auto-restoring immediately after respawn

- Client behavior
  - `client/ClientRtsController.java`
    - added a death handoff guard in `tick()`
    - when the local player is dead or dying:
      - clears BuilderScreen / RtsHomeScreen / RtsProgressionScreen /
        RtsCraftTerminalScreen if one is open
      - clears pending RTS craft/remote-menu handoff state
      - exits home-selection mode locally
      - clears active mine progress rendering
      - restores the previous camera entity and vanilla camera options
      - locally disables RTS state
      - sends `C2SRtsToggleCameraPayload` so the server RTS session shuts down
    - BuilderScreen auto-restore is gated behind a suppression flag so respawn
      returns to normal non-panel gameplay until the player explicitly enters
      RTS again

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS
  - Remaining warnings are existing NeoForge `EventBusSubscriber.Bus`
    deprecation warnings.

[2026-05-22] Centered settings modal and editable survival balance config
- Scope
  - settings/gear panel was still a rough corner dropdown
  - survival balance needed an in-game on/off toggle
  - progression skill-book/material costs needed an in-game editor
  - Gemini settings-panel redraw needed a proper prompt without hardcoding
    API keys

- Client UI
  - `client/BuilderScreen.java`
    - gear panel now renders as a centered modal
    - added Survival Balance ON/OFF row
    - added Skill Cost Editor:
      - previous/next progression node selector
      - editable cost text field
      - save/reset/close buttons
    - keyboard input and Enter-to-save are routed to the editor field

- Runtime config / progression
  - `Config.java`
    - added `progressionCostOverrides`
    - added runtime setters that save the common config
  - `progression/RtsProgressionNodes.java`
    - added config-aware `costsFor(...)`
    - added cost parse/format helpers
  - `progression/RtsProgressionManager.java`
    - unlock cost checks/consumption now use config-aware costs
  - `client/RtsProgressionScreen.java`
    - displayed material costs now match overrides

- Network
  - added C2S payloads for:
    - survival progression toggle
    - progression node cost override
  - server handlers require permission level 2 and resync progression state
    to online players after edits
  - progression state also carries cost overrides so client UI reflects
    server-side material edits on dedicated servers

- Gemini redraw prompt
  - added `tools/generate_gemini_settings_panel_concepts.py`
  - generalized the existing Gemini helper to accept prompt and output-path
    environment overrides
  - attempted to run the script; it stopped safely because `GEMINI_API_KEY`
    is not present in the current shell environment

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon build`
  - Result: SUCCESS
  - Remaining warnings are existing NeoForge `EventBusSubscriber.Bus`
    deprecation warnings.

[2026-05-23] RTS chunk grid xray and remote GUI flash-close fix
- Scope
  - fix chunk grid still being hidden by world depth even after the earlier
    RenderType change
  - fix RTS right-click remote GUI opening for one frame and then closing
    when Create/Sophisticated/Pipez runtime mods are present

- Chunk grid rendering
  - `client/RtsBoundaryRenderer.java`
    - moved chunk guide rendering to its own `AFTER_LEVEL` branch
    - changed chunk guide output from the item/entity target to the main
      render target
    - converted the guide to all-cyan, thin debug-line geometry:
      - anchor-plane grid across the RTS chunk area
      - full-height vertical lines at chunk intersections
    - added `drawBuiltBufferNoDepth(...)`, which explicitly:
      - disables depth testing
      - disables depth writes
      - draws the guide mesh
      - restores depth writes, depth testing, and `GL_LEQUAL`
    - important note: in this MC/NeoForge runtime, `RenderStateShard.NO_DEPTH_TEST`
      does not actively disable depth testing when the current state already has
      depth test enabled, so the chunk guide must own the RenderSystem state
      around the draw call

- Remote GUI
  - `client/RtsClientRemoteMenuCompat.java`
    - confirmed from `run/logs/latest.log` that vanilla `ChestMenu` and
      `CraftingMenu` were being closed by the Sophisticated compatibility
      screen/menu safety guard
    - root error was optional class probing:
      `NoClassDefFoundError:
      net/minecraft/client/gui/screens/inventory/AbstractContainerScreen`
    - limited Sophisticated menu safety checks to Sophisticated screen class
      names
    - made optional class probing catch `LinkageError` as well as
      `ClassNotFoundException`
    - vanilla and unrelated mod container screens now fail open instead of
      being closed by this compatibility guard

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS

[2026-05-23] Chunk guide reverted to flat xray grid and Craft panel detached
- Scope
  - user confirmed right-click remote GUI now works
  - chunk guide was visually wrong because full-height xray lines produced
    tall/diagonal perspective streaks instead of a readable ground grid
  - bottom Craft panel needed to disappear as a whole when hidden and stop
    consuming bottom storage width

- Chunk guide
  - `client/RtsBoundaryRenderer.java`
    - removed full-height vertical chunk intersection lines
    - kept only the flat X/Z grid at RTS anchor Y
    - kept the explicit no-depth draw wrapper so the flat grid is still visible
      through terrain and underground

- Builder UI
  - `client/BuilderScreen.java`
    - moved craftables from the bottom storage layout into a floating
      left-side panel
    - bottom storage layout now uses the width previously reserved for Craft
    - bottom header has a compact `Craft` toggle for showing/hiding the
      floating panel
    - close button removes the whole Craft panel, not just its contents
    - header resize handle adjusts width and height and persists the result
    - scroll/click handling was rerouted to the floating panel
    - world clicks no longer pass through the floating Craft panel
  - `client/RtsClientUiStateStore.java`
    - added persisted fields for Craft panel open state and size

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS

[2026-05-23] Corrected Craft target and switched chunk grid to no-depth flat boxes
- Scope
  - user clarified the Craft collapse request was for the RTS overlay on
    ordinary container GUIs, not the BuilderScreen bottom bar
  - user clarified the chunk grid should be the original flat cyan ground-grid
    style, made xray, not raw perspective `DEBUG_LINES`

- Rollback / correction
  - `client/BuilderScreen.java`
    - removed the accidental floating Craft panel changes
    - restored the bottom bar embedded Craft panel and its routing
  - `client/RtsClientUiStateStore.java`
    - removed persisted BuilderScreen floating Craft panel fields

- Container GUI overlay
  - `client/RtsClientInputGate.java`
    - added a real collapsed Craft width
    - overlay width now changes when Craft is collapsed
    - linked storage panel shifts left when Craft is collapsed
    - hit testing, dragging bounds, and scroll routing use dynamic overlay
      width/panel width

- Chunk grid
  - `client/RtsBoundaryRenderer.java`
    - replaced raw `DEBUG_LINES` with a custom no-depth line RenderType using
      `POSITION_COLOR_NORMAL` + `RENDERTYPE_LINES_SHADER`
    - chunk boundaries are now drawn using `LevelRenderer.renderLineBox`, the
      same family of rendering used by the red RTS boundary box
    - each boundary is a thin flat cyan box line on the RTS anchor plane
    - explicit depth-test/depth-write disable remains around the chunk guide
      draw call

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS

[2026-05-23] Recipe viewer R-key handoff added for RTS BuilderScreen
- Problem
  - User reported a Create W/Ponder -> R/recipe sequence in RTS mode could
    leave the client in a bad visual state.
  - A second direct conflict was found: `BuilderScreen` consumed `R` for RTS
    shape rotation, while JEI/EMI/REI use `R` as a core recipe lookup key.

- Fix
  - `BuilderScreen` now checks for loaded recipe viewers:
    - `jei`
    - `emi`
    - `roughlyenoughitems`
  - When one is present, pressing `R` is passed through to normal screen/key
    handling instead of rotating the RTS shape.
  - RTS rotation remains available through the top-bar rotate workflow and
    existing rotate controls.

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS

[2026-05-23] Prepared 1.0.11-alpha with configurable RTS UI scale
- Request
  - Build a new `1.0.11-alpha` artifact.
  - Carry forward the requested UI scaling change:
    - render RTS UI as if GUI scale is `2` by default
    - let the gear settings panel adjust RTS UI render scale.

- Versioning
  - `gradle.properties`
    - changed `mod_version=1.0.10-alpha` to `mod_version=1.0.11-alpha`

- Implementation
  - `client/BuilderScreen.java`
    - replaced the hardcoded fixed RTS baseline `3.0D` with persisted
      `fixedRtsGuiScale`
    - default fixed RTS UI scale is now `2.0D`
    - added `RTS UI scale` controls to the gear menu:
      - minus button
      - current scale label
      - plus button
      - range `1x` to `4x`
      - increments of `0.5x`
    - render scaling and input scaling still share the same frame transform,
      so pointer hitboxes match the scaled UI
    - settings guide panel positioning now uses the shared gear menu height
  - `client/RtsClientUiStateStore.java`
    - added `rtsGuiScale`
    - clamps invalid/out-of-range stored values before saving/loading
  - `assets/rtsbuilding/lang/en_us.json`
    - added English UI scale label and settings tutorial topic
  - `assets/rtsbuilding/lang/zh_cn.json`
    - added Chinese UI scale label and settings tutorial topic

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS
  - Produced `build/libs/rtsbuilding-built-from-above-1.0.11-alpha.jar`

[2026-05-24] Merged Home Relocation into Field Deployment
- Request
  - Do not implement automatic linked-storage chunk loading.
  - Treat modpack chunk loaders, such as FTB chunk loading, as the intended
    way to keep linked storage chunks loaded.
  - Make Field Deployment the expensive late-game unlock.
  - Remove the separate 10-netherite-block Home Relocation upgrade.

- Progression
  - `progression/RtsProgressionNodes.java`
    - removed the active `home_relocation` node
    - changed `field_deployment` cost to `minecraft:dragon_head` x1
    - kept dependency on `radius_3`
    - kept `bypassHomeRadius()` as the unlock effect

- RTS Home
  - `server/RtsProgressionManager.java`
    - `canChangeHome(...)` now checks `field_deployment`
  - `server/RtsCameraManager.java`
    - changed the blocked relocation message to name Field Deployment
  - `client/RtsHomeScreen.java`
    - the change-home button now unlocks from Field Deployment

- Localization
  - `en_us.json` and `zh_cn.json`
    - Field Deployment copy now describes both home-radius bypass and RTS Home
      movement
    - Home panel warning now points players at Field Deployment instead of a
      separate Home Relocation skill

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS

[2026-05-27] Localized Scan Storage and cleaned survival-only entry UI
- Request
  - Add Chinese support for the Scan Storage / FTB scan popup.
  - Hide RTS Home and RTS Progress entry buttons when survival balancing is
    not enabled.
  - Make the inventory entry labels Chinese when the game language is Chinese.
  - Fix the RTS Home panel visual problem where the starter-home button and
    warning copy could appear to sit in the same blurred/overlapped layer.
  - Record the long-term UI-language design rule in `AGENT.md`.

- BuilderScreen / Scan Storage
  - Replaced hardcoded English popup title/status strings with
    `screen.rtsbuilding.quest_scan.*` translation keys.
  - Kept scan result wording focused on new tasks executed:
    - `Completed 1 new task`
    - `Completed %s new tasks`
    - `No new tasks completed`
    - `No item tasks found`
  - Added English, Simplified Chinese, Traditional Chinese Taiwan, and
    Traditional Chinese Hong Kong strings.

- Inventory entry buttons
  - `RtsClientInputGate.renderInventoryRtsButtons(...)`
    - now returns immediately when `ClientRtsController.isProgressionEnabled()`
      is false.
  - `RtsClientInputGate.handleInventoryRtsButtonClick(...)`
    - now uses the same guard so hidden buttons have no live hitbox.
  - Button labels now use translatable keys:
    - `screen.rtsbuilding.inventory.progression_button`
    - `screen.rtsbuilding.inventory.home_button`

- RTS Home panel
  - `RtsHomeScreen`
    - screen title changed from literal `RTS Home` to translatable component
    - introduced shared panel width/height/y helpers used by both init and
      render
    - moved the warning copy below the home action button
    - anchored the Back button to the panel bottom instead of screen bottom
    - refreshes the home button label/active state every render so progression
      state sync can update the button without closing the screen

- Localization maintenance
  - Added the new keys to:
    - `en_us.json`
    - `zh_cn.json`
    - `zh_tw.json`
    - `zh_hk.json`
  - Updated stale guide copy that still referenced the removed
    `Home Relocation` skill; it now names `Field Deployment` / `野外部署`.
  - Verified all four language files expose 232 keys.

- Project instructions
  - `AGENT.md`
    - added the standing instruction that any UI element must consider
      English, Simplified Chinese, and Traditional Chinese text/layout.

- Validation
  - Ran language JSON/key parity check across all four runtime language files.
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache build`
  - Result: SUCCESS
  - Produced `build/libs/rtsbuilding-build-from-above-1.0.1-beta.jar`
  - Verified the jar contains:
    - `assets/rtsbuilding/lang/en_us.json`
    - `assets/rtsbuilding/lang/zh_cn.json`
    - `assets/rtsbuilding/lang/zh_tw.json`
    - `assets/rtsbuilding/lang/zh_hk.json`

[2026-05-27] Added Mouse Tweaks style import and compacted the E overlay
- Request
  - Make inventory-to-RTS-storage import less tedious:
    - currently Shift + click imports one stack at a time
    - desired behavior is holding Shift and dragging left mouse over many
      inventory slots, similar to Mouse Tweaks.
  - Make the small E-screen RTS overlay narrower.
  - Reduce each row from the old wide layout to about five or six items.
  - Show only six quickbar slots.
  - Add item information/help so the user can quickly understand what the
    overlay controls do.
  - Keep the Craft overlay control labels in simple English, with a small
    bottom-right `i` help button instead of translated explanatory UI.

- Shift drag import
  - `RtsClientInputGate`
    - added `shiftImportDragging`
    - added `shiftImportDragScreen`
    - added `shiftImportDragSlots`
  - Shift + left mouse on a player inventory slot starts the drag import.
  - Dragging over additional player-inventory slots sends
    `C2SRtsImportMenuSlotPayload` once per slot.
  - The gesture does not start on non-player slots, so machine/chest slots are
    still explicit one-click imports.
  - `MouseDragged.Pre` has no button getter in this NeoForge version, so the
    implementation uses GLFW to confirm the left mouse button is still down.
  - Drag state is cleared on release, screen close, or active overlay screen
    switch.

- Compact overlay
  - Replaced shared `COLS/ROWS` constants with:
    - `STORAGE_COLS=6`
    - `STORAGE_ROWS=4`
    - `QUICKBAR_SLOTS=6`
  - The storage grid is now 6 x 4.
  - The quickbar row now renders and hit-tests only six slots.
  - Reduced `STORAGE_PANEL_W` from 196 to 172.
  - Slightly shifted storage header controls to keep search/page controls
    usable in the narrower panel.

- Help / item information
  - Added a small bottom-right `i` button to the storage side of the E overlay.
  - Clicking it toggles an English help panel for all compact overlay controls.
  - Existing item tooltip behavior now also covers:
    - quick slots
    - Return preview slots
  - Help copy remains English by design because these button labels are simple
    operational terms and the user requested not to localize this part.

- Validation
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS

[2026-05-27] Ported the latest UI polish to Forge 1.20.1
- Request
  - Read the updated detailed project and journal, then carry the recent
    1.21.1 UI changes over to the 1.20.1 sister project.

- Ported files
  - `sister-projects/rtsbuilding-forge-1.20.1/src/main/java/com/rtsbuilding/rtsbuilding/client/RtsClientInputGate.java`

- UI changes now present in Forge 1.20.1
  - Compact E overlay:
    - storage grid is 6 x 4
    - quickbar shows six RTS quick slots
    - storage panel width is reduced
  - Bottom-right `i` help button and English help panel.
  - Hover item info for:
    - storage entries
    - craftable entries
    - quick slots
    - Return preview slots
  - Shift + left-drag import from player-inventory slots, with one
    `C2SRtsImportMenuSlotPayload` per newly crossed slot.

- Compatibility kept for Forge 1.20.1
  - Forge event classes and `EventBusSubscriber.Bus.FORGE`
  - `RenderGuiOverlayEvent.Pre` / `VanillaGuiOverlay`
  - `ScreenEvent.MouseScrolled.Pre#getScrollDelta()`
  - `ItemStack.isSameItemSameTags`
  - Forge packet shim `forgecompat.network.PacketDistributor`

- Validation
  - Ran `powershell -NoProfile -ExecutionPolicy Bypass -File tools\build_all_ports.ps1 -SkipMain -Skip119 -Tasks compileJava`
  - Result: SUCCESS
  - Ran `powershell -NoProfile -ExecutionPolicy Bypass -File tools\build_all_ports.ps1 -SkipMain -Skip119 -Tasks build`
  - Result: SUCCESS
  - Produced `sister-projects/rtsbuilding-forge-1.20.1/build/libs/rtsbuilding-build-from-above-forge-1.20.1-1.0.0-beta-forge.1.20.1.jar`

[2026-05-27] Fixed overlay help i18n and high-GUI-scale sizing
- Problem
  - The bottom-right `i` help panel was hardcoded English even though the
    project now explicitly requires English, Simplified Chinese, and
    Traditional Chinese UI consideration.
  - Keeping labels such as `Craft`, `OK`, `MAKE`, `MOVE`, `Q/M/N`, and `A/D`
    in English is acceptable because the user requested those simple control
    words stay English.
  - However, the longer help copy and the Return/import label should localize.
  - Screenshots at GUI scale 4/5/6 showed the E-screen overlay becoming far
    too large physically and colliding with the open chest UI.

- Localization
  - `RtsClientInputGate`
    - replaced the visible Return label with
      `screen.rtsbuilding.overlay.return_label`
    - replaced help title and help rows with
      `screen.rtsbuilding.overlay.help.*`
  - Added keys to:
    - `en_us.json`
    - `zh_cn.json`
    - `zh_tw.json`
    - `zh_hk.json`
  - The Chinese help explains all compact overlay controls while preserving
    short English button names where requested.

- Vertical paging
  - Replaced the wide horizontal storage pager with a narrow vertical pager:
    - `^`
    - `page/total`
    - `v`
  - Updated click hitboxes to match the new vertical pager.

- High GUI scale adaptation
  - Added an `OverlayProfile` for the container overlay.
  - The profile detects actual Minecraft GUI scale from:
    - physical window width / GUI-scaled width
  - For GUI scale <= 3:
    - render scale remains 1.0
    - storage rows remain 4
    - Craft panel follows user collapse state
  - For GUI scale > 3:
    - render scale becomes approximately `3 / currentGuiScale`
    - mouse hit tests use inverse-transformed overlay coordinates
    - storage rows reduce to 3
  - For GUI scale >= 5.5:
    - storage rows reduce to 2
    - the first pass force-collapsed Craft by profile
    - this was superseded later the same day by the storage-over-Craft stack
  - This keeps the overlay useful at GUI scale 4/5/6 without permanently
    changing the user's normal Craft collapsed preference.

- Layout internals
  - `OverlayLayout` now carries virtual screen size, dynamic panel height,
    dynamic row count, craft collapsed flags, and vertical page positions.
  - Return row and info button positions are derived from dynamic layout
    values instead of static `OVERLAY_H` offsets.

- Validation
  - Checked language JSON key parity:
    - `en_us`
    - `zh_cn`
    - `zh_tw`
    - `zh_hk`
    - all currently expose 245 keys
  - Ran `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`
  - Result: SUCCESS

[2026-05-27] Changed high-GUI-scale overlay to storage-over-craft stack
- User correction
  - The 4/5/6 GUI-scale solution should not remove Craft.
  - GUI scale 1/2/3 should keep the existing side-by-side layout.
  - At high GUI scale, storage should stay on top and Craft should move below
    it, so the MOVE drag handle remains in the storage header.

- Mainline implementation
  - Updated `RtsClientInputGate` overlay layout profiling:
    - GUI scale <= 3 keeps render scale 1.0 and the existing horizontal layout.
    - GUI scale > 3 uses a local downscale toward visual scale 3.
    - GUI scale > 3 stacks panels vertically as storage over Craft.
    - GUI scale >= 5.5 reduces storage to two rows; scale 4/5 uses three rows.
  - `OverlayLayout` now carries independent storage and Craft panel positions
    and heights, so render and hit testing use the same coordinates.
  - Removed the force-collapse behavior from the high-scale profile; Craft now
    remains available in the stacked lower panel.

- Forge 1.20.1 sister
  - Ported the same storage-over-Craft stacked layout to:
    `sister-projects/rtsbuilding-forge-1.20.1/src/main/java/com/rtsbuilding/rtsbuilding/client/RtsClientInputGate.java`
  - Kept Forge-specific event types, scroll delta API, packet shim, and
    1.20.1 item comparison behavior.

- Validation
  - Mainline `compileJava`: SUCCESS
  - Forge 1.20.1 `compileJava`: SUCCESS
  - Forge wrapper note:
    - after a timed-out build left wrapper startup unhappy, pre-creating the
      empty Gradle 8.8 wrapper lock file allowed the offline compile to run.

[2026-05-27] Rebuilt Forge 1.20.1 as 0.0.1 beta
- User correction
  - The Forge 1.20.1 beta should be named `0.0.1`, not `0.01`.

- Versioning
  - Updated the 1.20.1 Forge sister `mod_version` to:
    - `0.0.1-beta-forge`

- Cleanup and build
  - Deleted the old jar outputs from the 1.20.1 sister `build/libs` directory.
  - Rebuilt the sister project offline with Java 21 and the Gradle 8.8 wrapper.
  - Result: BUILD SUCCESSFUL

- Output
  - Produced:
    - `sister-projects/rtsbuilding-forge-1.20.1/build/libs/rtsbuilding-build-from-above-forge-1.20.1-0.0.1-beta-forge.jar`
  - Verified the embedded `META-INF/mods.toml` version is:
    - `0.0.1-beta-forge`

[2026-05-27] Investigated inventory-open lag report
- User received external feedback that the mod can cause serious lag every
  time inventory opens in a large modpack, especially relevant for creative
  mode / modpack development.

- Findings
  - `RtsClientInputGate.onScreenRenderPost(...)` initialized the compact
    container overlay before checking `ClientRtsController.canUseStorageOverlay()`.
  - That meant a plain container/inventory open could trigger overlay screen
    sync and a storage-page request even when the storage overlay was not
    actually available.
  - Server-side storage page requests always rebuilt creative-tab contents via
    `CreativeModeTabs.tryRebuildTabContents(...)`.
  - In large modpacks, rebuilding creative tab contents is a likely hitch source,
    and the report's "only in creative / amount of blocks and stuff" description
    fits that path closely.

- Mainline fix
  - Moved the storage-overlay availability check before overlay screen sync in:
    `src/main/java/com/rtsbuilding/rtsbuilding/client/RtsClientInputGate.java`
  - Removed the redundant automatic craftables refresh from overlay screen sync;
    the existing storage-revision sync remains responsible for craftable refresh.
  - Added a quick empty-storage fast path in:
    `src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java`
  - Changed creative-tab content rebuild to run only when needed and only when
    operator-item visibility mode changes.
  - Keyed the item-to-creative-tab cache by normal/operator mode so switching
    permission state does not reuse the wrong tab membership.

- Forge 1.20.1 sister
  - Ported the same client gating and server creative-tab cache changes to:
    `sister-projects/rtsbuilding-forge-1.20.1/src/main/java/com/rtsbuilding/rtsbuilding/client/RtsClientInputGate.java`
    `sister-projects/rtsbuilding-forge-1.20.1/src/main/java/com/rtsbuilding/rtsbuilding/server/RtsStorageManager.java`

- Validation
  - Mainline `compileJava`: SUCCESS
  - Forge 1.20.1 `compileJava`: SUCCESS
  - Forge compile required running the offline Gradle wrapper outside the
    sandbox because the sandbox could not open the Gradle wrapper lock file.

[2026-05-27] Refined overlay bootstrap and creative-tab warm cache
- Follow-up concern
  - The performance gate should not prevent the compact storage overlay from
    working after a world reload when linked-storage state exists only on the
    server until the first page sync.
  - Creative-tab classification should be warmed during game/server startup
    instead of on the first inventory open.

- Client refinement
  - Added a one-shot overlay bootstrap request in `RtsClientInputGate`.
  - If the client does not yet know whether the storage overlay is usable, the
    first container render requests one storage page and marks that screen as
    initialized.
  - This preserves restored linked-storage overlay behavior without returning
    to the old "request on every inventory open" pattern.
  - The one-shot marker is reset on client logout so changing worlds in the
    same Minecraft process still gets a fresh bootstrap.

- Server refinement
  - Added `RtsStorageManager.warmCreativeTabCaches(MinecraftServer)`.
  - On server started, both normal and operator creative-tab modes are rebuilt
    once and item-to-tab membership is cached for all registered items.
  - Storage-page requests now skip creative-tab rebuild entirely when that mode
    was already warmed.

- Porting
  - Applied the same bootstrap and warm-cache path to the Forge 1.20.1 sister.

- Validation
  - Mainline `compileJava`: SUCCESS
  - Forge 1.20.1 `compileJava`: SUCCESS

[2026-05-27] Added storage scan feedback and de-spiked ultimine finishing
- User concern
  - Storage refresh/page requests can take visible time, especially after
    pressing the storage refresh/search/sort/page controls in large linked
    inventories.
  - Ultimine sometimes showed mining as complete, then froze for a few seconds
    before the full chain was actually broken.

- Storage scan UI
  - Added a lightweight client-side storage scan progress popup.
  - Any storage page request now starts the popup; the popup completes when the
    server storage page payload returns.
  - The popup is localized in English, Simplified Chinese, Traditional Chinese
    Taiwan, and Traditional Chinese Hong Kong.
  - This is intentionally UI-only and does not change the storage page protocol
    or overlay behavior.

- Ultimine server pacing
  - Found the hitch source: after the seed block reached 100%, the server broke
    every remaining ultimine target, absorbed drops, refreshed storage, and
    sometimes ran quest detection in the same tick.
  - Changed ultimine finishing to process chained targets in small batches
    across ticks.
  - The mining progress remains active until the chain is truly finished, then
    tools are refunded and storage refreshes once.
  - Normal single-block remote mining still refreshes storage only once.

- Porting
  - Applied the same changes to the Forge 1.20.1 sister project.

- Validation
  - Mainline 1.21.1 `compileJava`: SUCCESS
  - Forge 1.20.1 `compileJava`: SUCCESS

- Follow-up for the exact tester report
  - The tester clarified the freeze happens when opening the creative inventory
    or returning from a recipe viewer, not while using the RTS panel itself.
  - Even after the creative-tab cache fix, linked-storage overlay sync could
    still request a full storage page whenever a new container screen object
    opened.
  - Changed overlay screen bootstrap/sync to reuse the latest local storage
    page snapshot once one exists.
  - Opening creative inventory now only asks the server for a storage page if
    the client has no snapshot yet; explicit overlay actions such as search,
    sort, paging, and manual refresh still request fresh data.
  - Revalidated both mainline and Forge 1.20.1 `compileJava`: SUCCESS.

- Manual refresh button follow-up
  - Added an explicit `R` refresh button next to the `i` help button in both
    the main RTS storage panel and the compact inventory overlay.
  - Clicking `R` forces the current storage page to refresh and uses the
    existing storage scan feedback state.
  - The `R` button highlights while a storage scan is running.
  - Updated the main bottom-bar guide and overlay `i` help text in English,
    Simplified Chinese, Traditional Chinese Taiwan, and Traditional Chinese
    Hong Kong.
  - Revalidated language JSON files and both mainline / Forge 1.20.1
    `compileJava`: SUCCESS.
  - Built release jars after the refresh-button work:
    - Mainline 1.21.1 `build`: SUCCESS
      `build/libs/rtsbuilding-build-from-above-1.0.1-beta.jar`
    - Forge 1.20.1 `build`: SUCCESS
      `sister-projects/rtsbuilding-forge-1.20.1/build/libs/rtsbuilding-build-from-above-forge-1.20.1-0.0.1-beta-forge.jar`

- Overlay return-label translation fix
  - The Forge 1.20.1 sister resources were missing
    `screen.rtsbuilding.overlay.return_label`, so the compact overlay displayed
    the raw translation key.
  - Added the missing key in English, Simplified Chinese, Traditional Chinese
    Taiwan, and Traditional Chinese Hong Kong.
  - Revalidated sister language JSON files and rebuilt the Forge 1.20.1 jar:
    `sister-projects/rtsbuilding-forge-1.20.1/build/libs/rtsbuilding-build-from-above-forge-1.20.1-0.0.1-beta-forge.jar`

[2026-05-27] Version bump to 1.0.2-beta and 0.0.2-beta
- Updated versions:
  - Mainline 1.21.1 NeoForge: `1.0.2-beta`
  - Forge 1.20.1 sister: `0.0.2-beta`
- Built release jars:
  - `build/libs/rtsbuilding-build-from-above-1.0.2-beta.jar`
  - `sister-projects/rtsbuilding-forge-1.20.1/build/libs/rtsbuilding-build-from-above-forge-1.20.1-0.0.2-beta.jar`
- Added `update_report_1.0.2_beta.txt` with a concise release summary covering:
  - manual storage refresh button
  - reduced creative-inventory/container-open rescan lag
  - storage scan feedback
  - batched ultimine finishing
  - overlay localization fixes

[2026-05-28] Multiplayer camera compatibility mitigation
- Investigated a CurseForge multiplayer report mentioning RTS-mode visual
  stuttering, character POV flashing, iffy placement, and garbled audio.
- Confirmed the current camera/network path is main-thread based:
  - client input/camera work runs from client tick events
  - network handlers use `context.enqueueWork(...)`
  - server camera motion runs on the server logic thread
- Implemented a small mitigation in the 1.21.1 main project:
  - RTS camera move packets are now sent only when meaningful camera input is
    present, plus a zero-input idle heartbeat every 20 ticks
  - sub-epsilon decayed rotation values are snapped to zero so idle state can
    actually become idle
  - local mirror camera restore is throttled to at most once every 10 ticks if
    another mod/system changes `minecraft.getCameraEntity()`
- Rationale:
  - reduce per-player idle packet pressure in multiplayer
  - reduce server-side no-op camera snaps
  - avoid high-frequency camera ownership fights with camera/audio/POV mods
- Longer-term follow-up:
  - prefer the server-synced `RtsCameraEntity` when available and use the local
    mirror only as a short fallback/prediction aid.
- Validation:
  - build not run here; user plans to run builds locally.

[2026-05-28] GitHub publication and branch-policy decision
- Added GitHub/publication strategy to `AGENT.md`:
  - root `E:\RTSbuilding` is the 1.21.1 NeoForge mainline and should map to
    the default/main branch
  - local `sister-projects` is for local porting/comparison and should not be
    uploaded wholesale as part of the public source tree
  - Forge 1.20.1 should be published as a separate self-contained branch
    rooted at the Forge project, e.g. `forge-1.20.1`
  - 1.19.x is out of scope for the initial GitHub publication pass
  - deleted/experimental `sister-projects\rtsbuilding-neoforge-26.1` remains
    ignored and must not be restored or uploaded
  - generated/local artifacts should be excluded from source publication
- Mirrored the decision into `detailed_project.txt`.
- Validation:
  - documentation-only; no build required.

[2026-05-28] Carry-over handoff document
- Added `carry_over_2026-05-28.md` for starting a new chat without losing
  context.
- The handoff covers:
  - root project / Java 21 facts
  - GitHub branch strategy
  - latest multiplayer camera mitigation
  - local build commands
  - key files
  - Bilibili launch/community feedback
  - next feature priorities
  - dirty-worktree caution
- Mirrored the document creation into `detailed_project.txt`.
- Validation:
  - documentation-only; no build required.

[2026-05-28] Head-start camera option and onboarding/community links
- Added a top-right RTS gear setting for starting the RTS camera from the
  player's head position.
  - Default is OFF.
  - The setting is stored in `rtsbuilding-client-ui.json`.
  - The C2S toggle payload now carries this preference.
  - Server RTS camera sessions and S2C camera state now carry
    `closeRangeAllowed`.
  - When enabled, normal RTS camera start height uses player eye height and the
    old 8-block minimum camera distance is disabled on both server movement and
    client prediction.
- Added a delayed client chat onboarding reminder after joining a world:
  - tells players the RTS toggle key opens RTS Build/Storage mode
  - points users to Mods > RTSBuilding > Config for feedback links
  - provides clickable Discord and copyable QQ group info
  - includes a clickable "do not remind again" command, persisted per
    singleplayer world / multiplayer server key
- Added a shared `RtsCommunityLinks` constants file and displayed community
  links in the mod config screen:
  - Discord `https://discord.gg/9Pw6vZfAm`
  - QQ group `910318076`
- Updated English, Simplified Chinese, Traditional Chinese Taiwan, and
  Traditional Chinese Hong Kong language files.
- Validation:
  - language JSON parse: SUCCESS
  - build not run here; user plans to build locally

[2026-05-28] Infinite storage count display guard
- Fixed the AE/creative-storage style huge-count display path that could show
  values like `9223372036B` and overlap compact storage slots.
- Client item-count formatter now renders effectively infinite counts as
  `INF`.
- Storage hover tooltips in both the main RTS screen and compact inventory
  overlay now use the same compact count formatting.
- Server storage aggregation now sanitizes effectively infinite reported
  counts and uses saturated addition instead of raw `Long::sum` for linked
  item totals, namespace totals, fluid totals, craft availability summaries,
  funnel buffer summaries, matching-count helper paths, and recent-entry
  accumulation.
- Mirrored the same guard into the Forge 1.20.1 sister project.
- Validation:
  - `git diff --check`: SUCCESS
  - Gradle build/compile not run; user plans to build locally.

[2026-05-28] Configurable max RTS action radius
- Added `maxActionRadiusBlocks` common config in both mainline 1.21.1 and the
  Forge 1.20.1 sister project.
  - default: `128`
  - range: `48..512`
- When survival progression is disabled, RTS action radius now uses the config
  max directly instead of the old fixed 48-block radius.
- When survival progression is enabled, the radius ladder remains
  `16 -> 32 -> 48`, then a new `radius_max` node unlocks the configured max.
  - prerequisite: `radius_3`
  - cost: `minecraft:netherite_ingot x1`
  - `field_deployment` now depends on `radius_max`
- Added the max-radius field to the mod config screen and adjusted the skill
  cost list layout around it.
- Expanded the progression-screen horizontal layout to fit the new top-row
  node cleanly.
- Added English, Simplified Chinese, Traditional Chinese Taiwan, and
  Traditional Chinese Hong Kong localization for the new config and skill node.
- Validation:
  - all touched language JSON files parse successfully
  - no `DEFAULT_ACTION_RADIUS` references remain in mainline 1.21.1 or Forge
    1.20.1
  - Gradle build/compile not run; user plans to build locally.

[2026-05-28] Highest-priority feedback polish pass
- Fixed RTS remote menu cleanup for vanilla/container GUIs:
  - server session now records the RTS-opened remote menu `containerId`
  - RTS disable/logout explicitly closes that tracked menu before clearing
    compat validation state
  - tick cleanup drops the tracked id after the player closes/replaces the menu
  - intended to restore vanilla `removed`/`stopOpen` behavior for chest lids and
    similar open-state visuals
- Made creative-mode RTS remote breaking instant:
  - creative players now destroy the target block immediately when remote mine
    starts instead of entering the survival progress path
- Improved config-screen robustness:
  - wider panel cap
  - compact one-column fallback for narrow widths
  - clipped helper text and localized labels
  - explicit edit-box text colors for readability
- Added storage-panel empty states:
  - unbound storage now tells players to bind a chest or AE terminal to show
    inventory + storage
  - bound-empty storage now clarifies that hotbar use still works
- Improved undo/cancel discoverability:
  - undo/redo count tooltip shows `Ctrl+Z` / `Ctrl+Y`
  - Quick Build button/panel tooltips explain closing/canceling preview
  - top tutorial Quick Build topic now mentions canceling previews and undoing
    confirmed batches
  - updated English, Simplified Chinese, Traditional Chinese Taiwan, and
    Traditional Chinese Hong Kong text
- Validation:
  - `git diff --check` on touched files: SUCCESS except CRLF conversion
    warnings
  - all four touched language JSON files parse as UTF-8 JSON
  - `.\gradlew.bat compileJava --no-daemon --no-configuration-cache`: SUCCESS
    with existing EventBusSubscriber deprecation warnings

[2026-05-28] 1.0.3-beta first/second-wave feedback fixes
- Implemented the easier feedback waves before the harder remote-GUI
  compatibility investigation.
- Unbound storage now shows player main-inventory items in the RTS storage
  panel, and selected storage placement can extract from that same inventory
  fallback when no linked storage exists.
- Container overlay storage counts no longer mix in player inventory:
  - server storage pages only include player inventory in the normal inventory /
    RTS context
  - overlay bootstrap/screen switches force a fresh page so stale RTS snapshots
    do not carry inventory counts into ordinary container GUIs
  - item hover count text distinguishes inventory vs storage source
- Normal RTS mining no longer directly recovers tracked RTS-placed blocks by
  default:
  - added a persisted client setting to opt back into the risky behavior
  - sent the toggle through `C2SRtsMinePayload`
  - left explicit undo recovery on its separate path
- Added configurable RTS-internal mode key mappings:
  - Interact, Storage Link, Rotate, Funnel
  - defaults: `Q`, `E`, `R`, `F`
- Added English, Simplified Chinese, Traditional Chinese Taiwan, and
  Traditional Chinese Hong Kong text for the new key mappings, risky recovery
  setting, count-source tooltips, and unbound-storage backpack copy.
- Validation:
  - all four touched language JSON files parse as UTF-8 JSON
  - `git diff --check`: SUCCESS, with existing LF -> CRLF conversion warnings
  - `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`:
    SUCCESS, with existing `EventBusSubscriber` deprecation warnings

[2026-05-28] Version 1.0.3 and dual-target build script (superseded later)
- Initially bumped both ports toward `1.0.3` artifact parity; a later entry on
  the same day corrected the public staging versions to mainline
  `1.0.3-beta` and Forge 1.20.1 `0.0.3Beta`, then completed the Forge port of
  the first/second-wave behavior.
- Reworked `tools/build_all_ports.ps1` for current release builds:
  - default targets are now mainline 1.21.1 NeoForge + Forge 1.20.1
  - Forge 1.19.2 is skipped unless `-Include119` is passed explicitly
  - default Gradle mode is offline; pass `-Online` to allow dependency fetches
  - each target uses its own `GRADLE_USER_HOME`
  - supports `-Tasks compileJava` for a faster dual compile check
- Validation:
  - `powershell -NoProfile -ExecutionPolicy Bypass -File tools/build_all_ports.ps1 -DryRun`:
    SUCCESS
  - `powershell -NoProfile -ExecutionPolicy Bypass -File tools/build_all_ports.ps1 -Tasks compileJava`:
    SUCCESS for mainline 1.21.1 and Forge 1.20.1
  - warnings are existing deprecation warnings in both toolchains

[2026-05-28] Forge 1.20.1 synced to 1.0.3-beta first/second-wave behavior
- Corrected release staging versions:
  - mainline 1.21.1 NeoForge: `mod_version=1.0.3-beta`
  - Forge 1.20.1 sister: `mod_version=0.0.3Beta`
- Ported the mainline first/second-wave behavior changes into Forge 1.20.1:
  - unbound RTS storage shows and can place from player main-inventory items
  - ordinary container overlays force fresh storage pages and do not mix player
    backpack counts into container/linked-storage statistics
  - overlay/item hover count source text distinguishes inventory vs storage
  - ordinary container overlay can collapse to the same one-row strip as
    mainline, with localized drag labels instead of hard-coded `MOVE`
  - risky direct recovery of RTS-placed tracked blocks is persisted, opt-in,
    and sent through `C2SRtsMinePayload`
  - creative RTS remote mining destroys immediately instead of using survival
    progress
  - RTS-internal mode mappings for Interact / Storage Link / Rotate / Funnel
    are configurable, defaulting to `Q` / `E` / `R` / `F`
- Added the missing Forge 1.20.1 English, Simplified Chinese, Traditional
  Chinese Taiwan, and Traditional Chinese Hong Kong strings for:
  - RTS mode keybinds
  - unbound-storage backpack copy
  - count-source tooltips
  - Quick Build cancel / Ctrl+Z discoverability
  - risky placed-block recovery setting and tutorial
- Updated `detailed_project.txt` so the compact architecture map now records
  current mainline version `1.0.3-beta` and Forge 1.20.1 local port version
  `0.0.3Beta`.
- Validation:
  - all eight active language JSON files parse as UTF-8 JSON
  - `git diff --check` on the tracked mainline files found no whitespace
    errors, only existing LF -> CRLF working-copy warnings
  - `tools/build_all_ports.ps1 -Tasks compileJava`: SUCCESS for mainline
    1.21.1 and Forge 1.20.1
  - `tools/build_all_ports.ps1`: SUCCESS for both active targets
  - produced jars:
    - `build/libs/rtsbuilding-build-from-above-1.0.3-beta.jar`
    - `sister-projects/rtsbuilding-forge-1.20.1/build/libs/rtsbuilding-build-from-above-forge-1.20.1-0.0.3Beta.jar`

[2026-05-28] Onboarding mentions i-help and overlay collapse
- Updated the join/onboarding reminder text:
  - now explicitly tells players that small `i` buttons open tutorials for
    panels and overlays
  - still mentions that Discord/QQ are available from Mods > RTSBuilding >
    Config
- Reduced the RTS storage overlay footprint on ordinary container/crafting
  screens:
  - craft panel width 116 -> 104
  - storage panel width 172 -> 160
  - default storage rows 4 -> 3
- Added a whole-overlay collapse button next to the drag button:
  - collapsed overlay becomes a single horizontal strip
  - keeps drag, expand, quick slots, refresh, and info controls available
  - hides the full storage grid, return slots, and craft panel while collapsed
  - info remains available in collapsed mode so help is still discoverable
- Localized the overlay drag button:
  - English: `MOVE`
  - Simplified Chinese: `拖拽`
  - Traditional Chinese Taiwan: `拖曳`
  - Traditional Chinese Hong Kong: `拖拽`
  - overlay help move text now matches those labels
- Validation:
  - `git diff --check` on touched files: SUCCESS except CRLF conversion
    warnings
  - all four touched language JSON files parse as UTF-8 JSON
  - `.\gradlew.bat compileJava --no-daemon --no-configuration-cache`: SUCCESS
    with existing EventBusSubscriber deprecation warnings

[2026-05-29] Remote GUI close, config scroll, and overlay collapse correction
- Reworked the remote-menu close mitigation after in-game chest testing showed
  the first pass was not enough:
  - RTS-opened remote menus now remember the source `BlockPos` as well as the
    menu container id.
  - If the tracked menu id disappears on a later server tick, or RTS cleanup
    explicitly closes the menu, the server now sends a vanilla-style block event
    `1,0` plus a block update at the remembered position.
  - This is aimed at vanilla/open-count containers such as chests whose lid can
    visually remain open after the remote GUI is closed with Esc.
- Fixed the RTS settings tutorial overlap:
  - clicking the gear/settings `i` now closes the settings menu before opening
    the settings guide.
  - the settings guide is centered as a standalone information panel instead of
    trying to sit beside the settings menu where scaled UI could overlap.
- Corrected the ordinary container overlay collapsed state based on the latest
  desired behavior:
  - collapsed overlay keeps drag, expand, sort, direction, search, clear,
    refresh, info, and the first linked-storage row.
  - collapsed overlay no longer keeps or renders the quick-slot bar.
  - left/right click extraction and carried-stack deposit work on that first
    linked-storage row while collapsed.
- Fixed a likely config scroll desync:
  - `RtsModConfigScreen` now captures currently visible edit-box drafts before
    changing `scroll`.
  - rebuilding widgets after scroll no longer writes old visible edit boxes into
    the new row indices.
  - focus is cleared before scroll rebuild to avoid stale focused edit boxes.
- Validation:
  - `.\gradlew.bat --offline --no-daemon --no-configuration-cache compileJava`:
    SUCCESS with existing EventBusSubscriber deprecation warnings.

[2026-05-29] Forge 1.20.1 port of remote GUI / config / overlay fixes
- Ported the same fixes from mainline 1.21.1 into
  `sister-projects/rtsbuilding-forge-1.20.1`:
  - remote GUI tracks both remote menu id and source block position
  - Esc/menu-close/RTS cleanup forces a vanilla-style block close visual update
    for chest-like containers
  - settings `i` guide closes the settings menu and opens centered instead of
    overlapping the settings text
  - config screen captures visible edit-box drafts before changing scroll
  - collapsed container overlay keeps search + first linked-storage row and no
    longer keeps the quick-slot bar
- Tightened `closeTrackedRemoteMenu` in both 1.21.1 and 1.20.1 so a missing
  current `containerMenu` does not prevent the remembered source block from
  receiving the close visual update.
- Validation:
  - mainline 1.21.1 `compileJava`: SUCCESS after the tightened close cleanup
  - Forge 1.20.1 source diff check: no whitespace errors
  - Forge 1.20.1 compile could not start locally because Windows denied access
    to the Gradle wrapper lock file:
    `sister-projects/rtsbuilding-forge-1.20.1/.gradle-user-home/wrapper/dists/gradle-8.8-bin/.../gradle-8.8-bin.zip.lck`

[2026-05-29] Settings guide clipping and next-priority triage
- Fixed the settings information/tutorial panel overlap in both mainline
  1.21.1 and the Forge 1.20.1 sister port:
  - settings-context guide panels now try to sit beside the settings panel
    instead of centered over it
  - when side space is too tight, the guide falls back above/below the settings
    panel
  - guide body text is drawn inside a scissor-clipped body area so long
    translated lines cannot cover adjacent UI
  - guide topic titles are trimmed to the body width
- Recorded the new priority ordering:
  - P0 dedicated server/headless Linux safety, avoiding client GUI class loads
  - P1 survival-balance live toggle/progression cache invalidation
  - P2 settings-menu scrolling plus a default-on ordinary container overlay
    visibility setting
- Validation:
  - `git diff --check`: SUCCESS except existing CRLF conversion warnings
  - mainline 1.21.1 `compileJava`: SUCCESS with existing deprecation warnings
  - Forge 1.20.1 compile still could not start because the local Gradle wrapper
    lock file is denied by Windows.

[2026-05-29] Dedicated-server safety, live survival-balance unlock, and overlay setting
- Answered the debug-button status while touching the settings panel:
  - the client debug snapshot button already exists behind the debug setting
  - this pass preserved it and moved the settings controls into a scrollable
    area so the debug button remains reachable as more settings are added
- Reworked client-only classloading boundaries in mainline 1.21.1 and the
  Forge 1.20.1 sister port:
  - moved `BuilderMode` from the client package into `common`
  - split client config-screen registration into `RtsClientBootstrap`
  - split client mod-bus setup/renderer registration into
    `RtsClientModEvents`
  - moved S2C payload application into client-only
    `RtsClientNetworkHandlers`
  - routed S2C registrations through `RtsClientPayloadBridge` so common
    network registration does not directly load GUI/client handler classes on
    dedicated servers
- Fixed survival-balance disable semantics in both active versions:
  - when progression is disabled, server sync now sends disabled/empty lock
    state instead of preserving old unlocked/unlockable node collections
  - client state clears cached progression locks immediately when a disabled
    payload arrives or when local config toggles survival balance off
  - config save requests a fresh authoritative progression state afterward
- Added scrollable RTS gear settings in both active versions:
  - title, info, and close buttons stay fixed
  - controls scroll inside the settings panel
  - mouse wheel over the settings panel scrolls settings instead of zooming the
    RTS camera
  - sensitivity, UI scale, auto-store, head-start where present, placed-block
    recovery, debug, and the new overlay setting remain clickable after scroll
- Added a persisted client setting for ordinary container/crafting/machine RTS
  overlay visibility in both active versions:
  - default is on
  - disabling it hides the ordinary container overlay and releases its input
    interception
  - it does not affect the main RTS bottom storage panel or the remote GUI
    open/close lifecycle
  - existing overlay collapse behavior remains separate from this show/hide
    setting
- Validation:
  - no Gradle build/compile or dedicated-server launch was run because the user
    explicitly asked to run builds locally
  - active language JSON files for both versions parse as UTF-8 JSON
  - `rg` checks found no direct client GUI/Minecraft/KeyMapping references in
    common/server/network paths beyond intentional Dist-gated client bridges
  - `git diff --check` on touched source/resource/doc paths reported only
    existing LF -> CRLF working-copy warnings, not whitespace errors

[2026-05-29] NeoForge Dist gate compile fix
- Fixed the first 1.21.1 build error reported from local Gradle:
  - NeoForge 21.1.219 does not provide `net.neoforged.fml.DistExecutor`
  - mainline 1.21.1 now uses `net.neoforged.fml.loading.FMLEnvironment`
    for the client-dist gate in `RtsbuildingMod` and `RtsClientPayloadBridge`
  - Forge 1.20.1 keeps its existing Forge `DistExecutor` path
- Validation:
  - no Gradle compile/build was run in Codex, per user preference
  - the precise common/server/network client-GUI keyword check is clean for
    the mainline 1.21.1 paths
  - `git diff --check` on the two touched files reports only the existing
    LF -> CRLF warning

[2026-05-29] Collapsed container overlay storage row alignment
- Fixed the collapsed ordinary container overlay in both active versions:
  - the first linked-storage row now uses the old quickbar row Y offset when
    the overlay is collapsed
  - this keeps the storage item row inside the collapsed frame instead of
    drawing it below the panel after the quick-slot bar was removed
- Validation:
  - no Gradle build/compile was run in Codex, per user preference

[2026-05-29] 1.21.1 config-screen blur layer fix
- Investigated the config-screen blur problem against NeoForge/Minecraft 1.21
  screen rendering:
  - 1.21.1 `Screen.render` calls `renderBackground` first, and that path runs
    the vanilla blur effect before rendering widgets
  - `RtsModConfigScreen` was drawing its custom labels/row backgrounds before
    `super.render`, so `super.render` blurred those custom elements and then
    drew buttons/edit boxes sharply afterward
- Fixed the 1.21.1 config screen by rendering the vanilla background first,
  then drawing the RTS config panel/labels/rows, then rendering widgets without
  calling `super.render` a second time.
- Checked the Forge 1.20.1 direction:
  - 1.20.1 screen rendering does not use the same 1.21 background/blur order
    in this config screen path, so no matching 1.20.1 code change was made
- Validation:
  - no Gradle build/compile was run in Codex, per user preference
  - `git diff --check` on the touched config-screen file reports only the
    existing LF -> CRLF working-copy warning

[2026-05-29] RTS camera vertical controls and configurable core actions
- Added configurable RTS action key mappings in both active versions:
  - Interact / Place, default right mouse
  - Break / Mine, default left mouse
  - Rotate Drag, default right mouse
  - Pan Drag, default middle mouse
  - Pick Block, default middle mouse
  - Camera Up, default Space
  - Camera Down, default Left Shift
- Reworked `BuilderScreen` input dispatch in both active versions so core RTS
  actions use the new mappings instead of hard-coded world left/right/middle
  mouse behavior:
  - short press on the interact/place action still runs placement or block/entity
    interaction
  - drag on the rotate action wins after the drag threshold, preserving the old
    right-drag camera rotation default
  - short press on pick-block selects the targeted block for RTS placement
  - drag on pan wins for camera panning, preserving the old middle-drag default
  - mining hold/release now tracks the configured break action, including a
    keyboard-bound break action
- Added vertical camera input to the client/server camera move packet and both
  prediction paths:
  - `C2SRtsCameraMovePayload` now carries a vertical axis
  - `ClientRtsController` applies local vertical movement and sends it to the
    server
  - `RtsCameraManager` applies the same vertical movement server-side
  - when vertical micro-adjustment is active, the minimum-distance push-away
    clamp is bypassed so small indoor height changes are less likely to shove
    the camera upward toward roofs
  - sprint remains the fast-move modifier, leaving Shift free as the default
    camera-down action
- Updated the top-bar `i` tutorial and Controls-page key names in English,
  Simplified Chinese, Traditional Chinese, and Hong Kong Traditional Chinese
  for both active versions.
- Validation:
  - no Gradle build/compile was run in Codex, per user preference
  - all active language JSON files for both versions parse as UTF-8 JSON

[2026-05-29] RTS camera vertical key held-state fix
- Fixed the Space/Shift vertical camera input path in both active versions:
  - `BuilderScreen` now records camera-up/camera-down key pressed/released
    state while it consumes those key events
  - `ClientRtsController` reads that screen-held state in addition to the
    registered `KeyMapping.isDown()` state
  - release handling always clears the state even if a search box gains focus,
    avoiding stuck vertical movement after focus changes
- Validation:
  - no Gradle build/compile was run in Codex, per user preference

[2026-05-29] Join reminder new-version warning
- Updated the 1.21.1 client join/onboarding reminder:
  - adds a gold warning line explaining that 1.0.3 beta is a newly released
    version without broad player testing yet
  - tells players who hit crashes or other severe bugs to try 1.0.2 and report
    the issue in the community group
- Localized the new reminder line in English, Simplified Chinese, Traditional
  Chinese, and Hong Kong Traditional Chinese.
- Note:
  - the Forge 1.20.1 sister currently does not have the join reminder class
    wired, so this change affects the existing 1.21.1 reminder path only
- Validation:
  - no Gradle build/compile was run in Codex, per user preference

[2026-05-29] Hong Kong onboarding wording cleanup
- Updated the 1.21.1 `zh_hk` onboarding reminder text to match the `zh_tw`
  wording for the intro block, and cleaned the remaining obvious Cantonese
  particles from nearby Hong Kong Traditional UI strings.
- Reason:
  - avoid Cantonese-style phrasing in the Hong Kong Traditional onboarding
    reminder.
- Validation:
  - no Gradle build/compile was run in Codex, per user preference

[2026-05-29] Forge 1.20.1 parity pass for camera start and config screen
- Completed the Forge 1.20.1 sync for the 1.21.1 camera-start behavior:
  - `C2SRtsToggleCameraPayload` carries the start-at-player-head preference
  - `S2CRtsCameraStatePayload` carries close-range camera allowance
  - `RtsCameraManager` starts normal RTS and home-selection cameras at player
    eye height when requested, preserves the flag across restore/restart, and
    uses the relaxed minimum-distance clamp during close/vertical adjustment
  - `ClientRtsController` persists the start-at-player-head UI setting, reads
    the server close-range flag, and mirrors the mainline idle camera heartbeat
    plus camera-entity restore throttle
- Ported the mainline config screen layout to Forge 1.20.1:
  - responsive 520px panel sizing, compact layout, translated survival/team
    buttons, community Discord/QQ actions, explicit edit-box text colors
  - renders panel contents and widgets through one path instead of custom text
    plus `super.render`, so scrolling rebuilds left labels and right edit boxes
    together
- Added the missing Forge 1.20.1 `RtsCommunityLinks` helper and synchronized
  the non-onboarding config/head-start language keys across English, Simplified
  Chinese, Traditional Chinese, and Hong Kong Traditional Chinese.
- Validation:
  - no Gradle build/compile was run in Codex, per user preference
  - all active mainline and Forge language JSON files parse as UTF-8 JSON
  - language key parity is clean between 1.21.1 and Forge 1.20.1 after excluding
    the intentionally mainline-only `chat.rtsbuilding.intro.*` onboarding keys
  - `git diff --check` on touched files reports only the existing LF -> CRLF
    working-copy warning on the already-dirty main config screen file

[2026-05-29] Settings panel unscaled while editing RTS UI scale
- Fixed the RTS settings panel in both active versions so it no longer renders
  inside the main RTS UI-scale transform:
  - main RTS UI still uses the configured RTS UI scale
  - settings/gear panel is suppressed during the scaled render pass and then
    drawn once in normal screen coordinates
  - settings clicks, slider drag, and settings scroll are handled before the
    scaled input coordinate pass
- Reason:
  - changing the RTS UI scale from inside settings should not resize or distort
    the settings panel itself.
- Validation:
  - no Gradle build/compile was run in Codex, per user preference
